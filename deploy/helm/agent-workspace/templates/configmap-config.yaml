apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-workspace.fullname" . }}-config
  namespace: {{ include "agent-workspace.namespace" . }}
  labels:
    {{- include "agent-workspace.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.config | default "" | indent 4 }}
