{{- if .Values.redis.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-workspace.redisFullname" . }}
  namespace: {{ include "agent-workspace.namespace" . }}
  labels:
    {{- include "agent-workspace.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - name: tcp-redis
      port: 6379
      targetPort: tcp-redis
  selector:
    {{- include "agent-workspace.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
{{- end -}}
