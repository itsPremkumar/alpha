{{- if .Values.persistence.home.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "agent-workspace.homePVC" . }}
  namespace: {{ include "agent-workspace.namespace" . }}
  labels:
    {{- include "agent-workspace.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.home.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.home.size | quote }}
  {{- with .Values.persistence.home.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end -}}
