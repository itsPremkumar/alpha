# Alpha Implementation Matrix (P1)

Living status matrix for the production-grade implementation plan. One row per
subsystem, each linked to evidence. Status values:

- **✅ verified** — tested with cited evidence (file/command + result)
- **🟡 partial** — works, with a stated caveat or pending confirmation
- **⏳ in progress** — work running or applied, verification not yet reported
- **⬜ blocked** — waiting on an external action

Updated: 2026-09-24 · cycle base `3910838` · remote `main` @ `https://github.com/itsPremkumar/alpha.git`

---

## A. Verification gates

| Gate | Status | Evidence |
|---|---|---|
| Recovery suite (30 scenarios: gateway, frontend, launcher kill→watchdog replace, Layer-4 `-Once` restore, double-kill, maintenance + resume, provider independence) | ✅ | `logs/verify_recovery_last.json` — run #2: **30 PASS / 0 FAIL**, exit 0 |
| Backend unit/integration suite (full `pytest tests`) | ⏳ | background run, log target `logs/pytest_last.log`; triage of any failure is the next fix-queue item |
| Frontend typecheck (`tsc --noEmit`) | ✅ | exit 0 (run after BOM strip, 2026-09-23) |
| Frontend production build (`next build`) | ✅ | exit 0, 89 s, 4/4 static pages, First Load JS 197 kB |
| Electron unit tests | ✅ | 8/8 pass (`node --test electron/tests/desktop-utils.test.mjs`) |
| Title-middleware tests (incl. new real-runtime + concurrency tests) | ✅ | `test_title_generation.py` 9/9 + `test_title_middleware_core_logic.py` 62/62 = **71 passed** |
| PowerShell parse (19 `.ps1` files) | ✅ | 0 errors |
| Python `compileall` | ✅ | exit 0 |
| YAML parse (20 files) | ✅ | 20/20 valid |
| JSON parse (33 tracked `*.json`, `utf-8-sig`-tolerant) | ✅ | **33/33 valid** after tsconfig BOM fix |
| `scripts/prod_check.py --strict` | ✅ | **0 failures / 0 warnings** — “production ready” (now also compares harness pyproject) |
| `scripts/verify_versions.sh` (all version sources incl. harness) | ✅ | exit 0 in both modes via Git-bash login shell (prints all 5 figures incl. harness 2.1.0); `bash -n` exit 0 on bump+verify; note: `verify-versions.yml` gates v* tags only, so this local run is the push-cycle gate |
| Zero-unwired-features wiring audit (`scripts/audit_frontend_wiring.py`) | ✅ | **216/216 frontend call sites → real gateway routes (454), exit 0**; found + fixed 3 real defects (see fix-log #5) |
| Env config wiring audit (`scripts/audit_env_wiring.py`) | ✅ | **DEAD-SET 0 / SET-ONLY 0, exit 0** after two pattern fixes; no launcher sets a rename-family variable nothing reads; 57 READ-ONLY entries are optional code-defaulted knobs (incl. `AGENT_WORKSPACE_ENV` prod marker — label-only, unset by design in every shipped config) |
| Docker compose config (prod + dev) | ✅ | docker 29.8.0 / compose v5.5.0: both files validate (exit 0; unset-secret warnings are pre-`.env` interpolation only); no host-local absolute paths; images pinned `redis:7|8-alpine`, `nginx:alpine` (no `:latest`) |

## B. Self-healing runtime (four layers)

| Subsystem | Status | Evidence |
|---|---|---|
| Launcher + 2 s monitor (gateway/frontend supervision) | ✅ | recovery suite: launcher kill → watchdog replaced it (new PID observed) |
| Watchdog loop, kill → restore (Layer 4) | ✅ | recovery suite: watchdog kill → `-Once` task restored; double-kill scenario 166 s PASS |
| Maintenance mode (stays stopped) + `start.ps1` resume | ✅ | recovery suite: maintenance PASS + resume PASS (127 s) |
| Provider independence (empty `OPENROUTER` key) | ✅ | recovery suite PASS |
| Scheduled tasks: `Alpha_Autostart`, `Alpha_Watchdog` (-Once/5 min), `Alpha_TrayStatus` | ✅ | all `Ready`, `StartWhenAvailable` + `IgnoreNew` |
| Watchdog behaviour under load | ✅ | watchdog.log: readiness flaps to `starting` under CPU load are **deferred** (no restart loop), gateway uptime continuous (no restart observed across probes) |
| Live stack heartbeat | ✅ | `GET :8001/health` 200 `healthy`; `GET :8001/health/ready` 200 `database:ok checkpointer:ok`; frontend `:3000` listening; heartbeat files fresh |
| Tray state display (state-colored icon behind `^` chevron) | 🟡 | tray process + `logs/tray.log` verified running; **visual confirmation from user pending** |
| Unplanned full-stack recovery — **2 real events** | ✅ | OpenCode server restarts at **07:44** and **08:15** killed launcher+gateway+frontend (harness children); `watchdog.log` shows **Layer-4 `loop_recreate` + `full_restart` escalation** both times (`pid=13192 alive=False → repairing`, `escalation after 1 failing checks: gateway=down frontend=down launcher=dead`); stack healthy again within seconds, all tasks `Ready` — and the restarted gateway came up with the new config (`docs_enabled: false`, `version: 2.1.0` live) |

## C. Configuration & worldwide portability

| Item | Status | Evidence |
|---|---|---|
| `GATEWAY_ENABLE_DOCS=false` production default | ✅ | active in `.env` and now in `.env.example` (fresh installs via `install.ps1`/`install.sh`/`start.ps1` inherit it) — commit `3999abd`; `prod_check --strict` passes |
| `.env.production.example` template | ✅ | strict-UTF-8, no BOM (console mojibake was PowerShell-5.1 display only) |
| `frontend/tsconfig.json` UTF-8 BOM | ✅ | stripped (bytes now `123,10,32`) — commit `84e5a1d`; `tsc` exit 0 after |
| Version sources in lockstep (backend, **harness**, frontend, Chart) | ✅ | `bump_version.sh`, `verify_versions.sh`, `prod_check.py` all extended to `backend/packages/harness/pyproject.toml`; `prod_check --strict` prints 4× 2.1.0 exit 0; `verify_versions.sh` exit 0 both modes; `bash -n` clean; harness is a workspace dependency (`backend/pyproject.toml:113`) present in `uv.lock` |
| No hardcoded secrets / auth untouched | ✅ | auth not wired, modified, or redesigned this cycle; `.env` gitignored; `BETTER_AUTH_SECRET` present but never printed |

## D. Ops & API endpoints

| Endpoint | Status | Evidence |
|---|---|---|
| `GET /health`, `GET /health/ready` | ✅ | 200 / 200 with DB + checkpointer probes OK |
| `GET /api/ops/status` | ✅ | 200: `status ok`, `uptime_seconds`, `time_utc`, `docs_enabled` flag present |
| `GET /api/ops/version` | ✅ | **bug found + fixed**: resolver looked up a duplicated, non-existent dist tuple `("agent-workspace", "agent-workspace")` → always `"unknown"`. Fixed to `agent-workspace-harness → alpha → agent-workspace` with a chain-order regression test; **unit ✅ 16/16**; **deployment path ✅** (image ships harness dist via `uv sync --locked` workspace). **Live ✅**: `GET /api/ops/version` → `"version":"2.1.0"` after the watchdog-driven restart |
| `/docs` `/redoc` `/openapi.json` in production | ✅ | template/env default off; **live ✅**: `/api/ops/status` → `"docs_enabled":false` confirmed on the running gateway (restart picked up `.env`) |

## E. Test debt & honesty items

| Item | Status | Evidence |
|---|---|---|
| Stale TODO placeholders in `test_title_generation.py` | ✅ | replaced by real tests: real compiled graph + `InMemorySaver` persistence (invoke & ainvoke), thread-concurrency, interleaved async concurrency — commit `3910838`. Coverage map comment documents where each former TODO bullet is covered |
| Immediate fix queue #1 `GATEWAY_ENABLE_DOCS` warning | ✅ | closed (see C) |
| Immediate fix queue #2 tsconfig BOM | ✅ | closed (see C) |
| Immediate fix queue #3 title TODOs | ✅ | closed (see E) |
| Immediate fix queue #4 `channels/manager.py:119`, `memory/manager.py:216` notes | ✅ | audited: both are **accurate documentation strings, not bugs** — channel dedupe-TTL boundary (documented design note referencing upstream issue #4121) and the `supports_search` invariant error message. No code change; recorded as documented limitations |
| Immediate fix queue #5 P2 baseline triage | ⏳ | frontend/electron portions green; backend full-suite result pending |
| Debt scan (TODO/placeholder/`@ts-ignore`/`console.log`) | ✅ | 0 `@ts-ignore`, 0 frontend `console.log`; fresh repo-wide sweep: **58 TODO/FIXME/XXX/HACK hits, 0 genuine unfinished items** (all are kanban `TaskStatus.TODO`, `TodoMiddleware` names, LLM `{TODO: …}` prompt fill-ins, test data, `ADR-XXX` slug templates) |

## F. Blocked / user actions

| Item | Status | Action |
|---|---|---|
| Reboot verification | ⬜ | after reboot run: `powershell -ExecutionPolicy Bypass -File scripts\verify_reboot.ps1` |
| Tray icon visual check | ⬜ | confirm state-colored circle behind the `^` chevron (hover/click/menu) |
| Controlled gateway restart | ✅ | completed via the 07:44/08:15 watchdog escalations (gateway relaunched with current `.env`); live probes green: `docs_enabled: false`, `version: "2.1.0"` |

---

## Session fix log (root cause → test)

1. **Fresh installs shipped with Swagger docs enabled** — `.env.example` had `GATEWAY_ENABLE_DOCS=false` commented out and every installer copies `.env.example` → `.env`. Root cause: production-hostile template default. Fix: active `false` in template + local `.env`; evidence `prod_check --strict` 0/0. Commit `3999abd`.
2. **`frontend/tsconfig.json` carried a UTF-8 BOM** — flagged by JSON gate. Fix: stripped; evidence `tsc --noEmit` exit 0. Commit `84e5a1d`.
3. **Stale TODO “add tests” block** — three of four promised areas were already covered elsewhere; concurrency + real-runtime persistence were genuinely missing. Fix: added them (compiled `create_agent` + `InMemorySaver` rebound-graph read; 24-thread sync isolation; scrambled-delay async binding) — 71/71 green. Commit `3910838`.
4. **`/api/ops/version` always `"unknown"`** — `_resolve_gateway_version()` iterated a duplicated tuple of dist names that never exist; also the harness pyproject (the installed carrier of the release version) was missing from `bump_version.sh`/`verify_versions.sh`/`prod_check.py`, so any correct lookup would have drifted at the next release. Fix: lookup chain `agent-workspace-harness → alpha → agent-workspace`, harness added to all three gates, docs (`ops.py`, gateway `AGENTS.md`) updated, regression test pins the chain order. Verification ✅: 16/16 tests, `prod_check --strict` 0/0, `verify_versions.sh` exit 0 both modes, `bash -n` clean; live probe after the controlled restart.
5. **Three unwired/shadowed frontend routes** — found by the new `scripts/audit_frontend_wiring.py` (every frontend call site vs. the imported FastAPI route table):
   (a) **channel status panel permanently empty**: backend declared the list route as `@router.get("/")` (canonical `/api/channels/`) while the UI requests `/api/channels` → Starlette 307 → `apiFetch` sets `redirect: "error"` → fetch throws → `channelStatus()` swallowed it as `[]`. Root cause: lone slash-spelling among `""` siblings (swarms, subagents). Fix: register both spellings in `channels.py`.
   (b) **dead `/subagents/live` fallback**: only possible match was `GET /api/subagents/{name}` (a single-bot lookup), never a registry; primary `GET /api/subagents/control` (`subagent_control.py:224`) exists. Fix: removed the fallback; unreachable gateway honestly yields `[]`.
   (c) **`/api/swarms/{id}/${action}` false positive**: `${action}` is a closed TS union; all four values (`pause|resume|cancel|step`) have real routes (`swarms.py:94–125`). Fix: documented enum-exception in the audit that re-expands and re-verifies every value each run.
   Verification ✅: audit **216/216 exit 0**; backend **97/97** (`test_channels_router` + `test_auth_middleware` + `test_csrf_middleware` + `test_feature_manifest_wiring`); frontend `tsc` exit 0 + **51/51** tests; `prod_check --strict` 0/0.
6. **Seven mangled-rename duplicate expressions + wrong default-port doc in the desktop launcher** — same family as fix #4 (a rename collapsed `X || Y` pairs into `X || X`): `electron/main.js` set duplicate object keys (`AGENT_WORKSPACE_PROJECT_ROOT`/`CONFIG_PATH`/`HOME` in `spawnBackend`, `AGENT_WORKSPACE_INTERNAL_GATEWAY_BASE_URL` in both frontend spawns, `AGENT_WORKSPACE_AUTH_DISABLED` twice in `applyDesktopAuthMode`), read `process.env.AGENT_WORKSPACE_ENV` twice in `isExplicitProdEnv`, had a tautological `data.service === 'x' || data.service === 'x'` in `isAgentWorkspaceGateway`, and documented `--gateway-port` default as 8001 while the desktop actually owns **8201** (`desktop-config.json`, contradicting its own header); `frontend/next.config.mjs:9` also read the same env var twice. The duplicates were behavior-neutral (identical duplicate keys, last-wins) but are the exact pattern that caused bug #4, and the port doc was simply wrong. Fix: all seven deduped, doc corrected to 8201. Verification ✅: `node --check` on both files exit 0, electron **8/8**, adjacent-duplicate scan empty, assignment re-greps show singles, `next build` exit 0 (4/4 pages, First Load JS 197 kB) against the edited `next.config.mjs`.
7. **Docs promised env knobs/names that nothing reads** — found by the new `scripts/audit_env_wiring.py` (SET-vs-READ sweep of `AGENT_WORKSPACE_*`/`ALPHA_*` across launchers, `.env*`, compose, docs) plus manual adjudication of its SET-ONLY list:
   (a) **README headless/CI setup was dead**: `ALPHA_SETUP_PROVIDER`/`ALPHA_SETUP_API_KEY` (README:536) — the wizard reads `AGENT_WORKSPACE_SETUP_PROVIDER`/`AGENT_WORKSPACE_SETUP_API_KEY` (`scripts/wizard/noninteractive.py:95,152`) and nothing translates between them, so documented non-interactive setup silently ignored the provider and key. Fix: README corrected to the real names.
   (b) **`AGENT_WORKSPACE_DEV_BUNDLER` never had a reader** (`git log -S` empty across all paths): claimed by `docs/CONFIGURATION.md`, `AGENTS.md`, `docs/DEVELOPMENT.md`. Real mechanism: `pnpm dev --turbopack`. Fix: all three docs now state the flag.
   (c) **`AGENT_WORKSPACE_LOG_LEVEL` never had a reader**: real mechanism is `log_level:` in `config.yaml` (`AppConfig` field, `app_config.py:212`, applied by `apply_logging_level`, restart-required per `reload_boundary.py`). Fix: CONFIGURATION.md points at the real knob.
   (d) **`SKIP_FRONTEND_BUILD` works but is not an `.env` key** (bash never reads `.env`): consumed by `Makefile:159` and `serve.sh` flag `-skip-frontend-build`; Windows `start.ps1` already reuses an existing `.next` build (`start.ps1:539`). Fix: CONFIGURATION.md states the real invocation forms.
   Verification ✅: env audit **DEAD-SET 0 / SET-ONLY 0 exit 0** (after tightening: JS-only env-object sets, shell/make bare-`$` reads, `env.get(...)` reads); frontend wiring regression **216/216 exit 0**; `prod_check --strict` **0/0**. `AGENT_WORKSPACE_ENV` reviewed as label-only (SDK environment tag + monitor display), unset by design everywhere — recorded informational, no change.
8. **Swarm runs could never fail honestly + group members never retried** — deep swarm audit found a 6-defect cluster, each verified failing before the fix:
   (a) terminal-state overwrite: `mark_completed`/`mark_failed` let a late result rewrite an already-terminal task, and PENDING/QUEUED tasks whose dependencies could never run kept `finished` false forever → first-terminal-result-wins guards + `fail_unrunnable_tasks()` emitting `SWARM_STRANDED_TASKS_FAILED` (coordinator `step()` also reconciles the ready==0/running==0/finished==false deadlock before every dispatch);
   (b) runner exited on pause (killing the whole run) and only checked the watchdog once per wave → pause now parks the runner in a resume loop and the watchdog ticks every poll; `SPECULATIVE_BACKUP_LAUNCHED` emitted a success event **without launching anything**, and a backup failure demoted the original run → a real backup race now launches, and `SPECULATIVE_BACKUP_FAILED` never demotes the original;
   (c) aggregator reported `partial_success` even with **zero** completed tasks and router `/run-async` returned fake `started_async` on terminal plans → `failed` when 0 completed + ≥1 failed, three-way final event (`SWARM_COMPLETED`/`SWARM_FAILED`/`SWARM_PARTIAL`), HTTP 409 on terminal plans; duplicate `start_background_swarm` spawned a second run → module-level dedup registry;
   (d) groups runner retried **only infrastructure exceptions** — a FAILED/TIMED_OUT/empty-output member result (the commonest failure: the model call itself) was recorded first-hit despite `max_retries=2` → results now raise into the retry machinery with backoff; CANCELLED still never retries.
   Verification ✅: 4 new swarm regressions (stranded-dep terminal status, pause/resume liveness, terminal-state immutability, idempotent background start) + groups retry regression (attempt1=FAILED → attempt2=COMPLETED; asserts `retry_counts==1` + final output = retry's output) — batches **43/43** (swarm engine/advanced/company/enterprise/groups/kanban) + **31/31** (group chat/bots/attendance/discipline/mesh) + **7/7** (`test_group_runs`). Commits `dc1d977`, `8ac5850`.
9. **The self-learning fork was dead five ways (and its tests had never passed)** — `learning_fork_middleware._execute_fork`: (a) `from alpha.tools.builtins import add_memory, recall_memory` raised ImportError on every execution — neither tool exists anywhere in the repo (real names: `memory_add`/`memory_search` in `alpha.agents.memory.tools`); (b) the handler awaited the **sync** `manager.add(...)` passing `user_id`/`trace_id` positionally where the signature is keyword-only — TypeError on every memory write; (c) the read branch called `manager.recall(...)` — no such method (real API `asearch`/`search`); (d) `from alpha.skills.proposals import get_skill_proposal_store` — no such function (the store is built as `SkillProposalStore(proposals_root())`); (e) handlers resolved managers via **local** imports, bypassing the module-level names the integration tests patch. This suite lives in `packages/harness/tests`, which no gate runs — 5 tests sat red invisibly; they additionally patched `langgraph.config.get_config` instead of the middleware's own binding, let `AsyncMock` make the synchronous `bind_tools` return a coroutine (the fork died on `'coroutine' has no attribute 'ainvoke'`), and asserted *generic* warnings that matched unrelated isolated failures. Fix: real tool names through whitelist/prompt/import/handler, `await manager.aadd(..., user_id=, trace_id=)` / `await manager.asearch(...)`, module-level patchable accessors, tests repaired with specific warning assertions. Verification ✅: `test_learning_fork` **20/20** (was 5 failed). Commit `f04099a`.
10. **user_model observed five tool names that do not exist** — `handle_tool_call` tracked `{read_file, str_replace, write_file, add_memory, propose_skill}`; only `propose_skill` is a registered `@tool` here (full inventory collected), so file/skill/memory observations were **never captured** — the personalization provider learned from proposals alone. Fix: `OBSERVED_TOOL_NAMES` = real `@tool` names (`memory_add`, `memory_search`, `session_search`, `propose_skill`, `skill_manage`, `invoke_python_skill`, `code_mode`, `python_repl`, `present_files`); tests renamed to a tracked real name. Verification ✅: `test_user_model` green in the 5-file batch. Commit `f04099a`.
11. **Cold `import alpha.subagents.executor` circular-crashed** — `alpha.authz.tool_filter` imported `alpha.tools.mcp_metadata` at module level: executor → authz → tools → builtins → `task_tool`/`self_improvement_tool` → `alpha.subagents.__getattr__` → partially-initialized executor (the whole-suite mock in `tests/conftest.py:29-41` exists only to dodge this edge; a clean `python -c "import alpha.subagents.executor"` reproduced the crash). Single edge: the import's only user is `_filter_mcp_tools_by_server` → moved inside the function. Verification ✅: cold probes `alpha.subagents.executor` / `alpha.authz` / `alpha.tools` all exit 0 (executor-first crashed before); new `tests/test_cold_imports.py` pins 5 module cold-imports in fresh subprocesses. Commit `f04099a`.
12. **All three swarm workers were fake-success machines** — `SpecialistBotWorker`, `EphemeralSubagentWorker` and `CodingWorktreeWorker` echoed the task objective back as their `summary`, recorded a `model` they never invoked (ephemeral defaulted to `fast-model`, a name resolvable nowhere in the repo), fabricated evidence (`confidence: 0.95`, `verified: True`), and returned `status: success` unconditionally — including with zero chat models configured; the worktree worker claimed to author code while producing no patch file. Fix: every worker resolves its model through the `_resolve_model` seam (no configured models raises `RuntimeError`, which feeds the runner's existing `mark_failed` path into an honest task/plan failure), runs one real invocation, reports the model's actual output as the summary (exact bytes — a cosmetic `strip()` was caught mangling the patch file's trailing newline), and records the model that really ran in evidence; the worktree worker fails fast before provisioning the worktree and writes the model-authored diff to `{task_id}.patch`. `confidence` had no consumer anywhere (grep-verified), so dropping it breaks nothing; full-runner tests bind a deterministic offline model through the same seam. Verification: new `test_swarm_worker_execution.py` **6/6** (real-output equality, model-override wiring, no-models fail-loud, patch-file-written, runner failure propagation incl. `TASK_FAILED` event) + `test_swarm_advanced_features.py` **13/13** + AST dup gate 0. Commit `bf57dc8`.
13. **Deliberation fabricated its rosters, confidences and judge rulings (and inverted every proponent win)** — Stage-4a honesty audit: the router invented placeholder rosters (`candidate-1`, `advocate-alpha`, `lead-model`, `security-lead`, `chairman-synthesizer`) that resolve to no configured model; Council/Debate/Ensemble could report success with zero model calls (an empty council scored a fabricated `0.8`, Debate hardcoded a "Proponent" winner); SINGLE/ENSEMBLE confidence numbers were invented constants; the verifier claimed `verified` without ever running `code_test_command`. Fix: rosters now come from `AppConfig.models` through one `invocation.py` seam — a forced strategy with <2 models raises `RuntimeError` (gateway 503), AUTO honestly down-ranks COUNCIL/DEBATE to ENSEMBLE and says so in the rationale; every stage-1 claim, rubric review, debate turn (each rebuttal sees the opponent's actual prior argument) and judge ruling is a real invocation with one retry then an honest skip; empty reviews score 0.0 / `review_count: 0`; the ruling is parsed from model output (`None` → "Unresolved", minority dissent = the losing side's real closing, judge fallback to `models[0]` disclosed); SINGLE discloses a 0.5 baseline, ENSEMBLE uses real token-overlap consensus with a documented `0.4 + 0.5*(consensus/100)` cap-0.9 formula; the verifier actually executes `code_test_command` (`deterministic_pass` +0.08/HIGH, `deterministic_fail` −0.25/CONTESTED with the output tail, penalty statuses on timeout/OSError, `consensus_supported` when no command is set). Two latent spelling bugs were exposed by the rewritten teeth tests: an h-typo in the judge regex/prompt/stub made `parse_judge_verdict` return a 10-char misspelling so `winner == "proponent"` was always false — **every proponent win rendered as "(Critic)"** with inverted rankings and minority report (root-caused by byte-level hex dumps after hand-typed edits proved impossible; fixed by a fragment-assembled replacement script) — and a dropped-P typo in `_PROPONENT_SYSTEM` (`PROP_MATCH False`, proven by hex: `50 52 4f 4f 4e 45 4e 54`) silently routed proponent turns to the stub's default branch. Frontend `runCouncil` now picks `final_answer` first instead of always JSON-dumping. Verification ✅: `test_deliberation_engine.py` **13/13** (single-seam stub; teeth: no-models honest fail, verifier real-execution pass/fail, no fabricated confidence, honest ruling labels), `test_system_one.py` **34/34**, `tsc --noEmit` exit 0, AST dup gate **0**. Commit `a08863c`.

14. **Six of seven cognitive-plan dispatchers were fake-success machines, and the research engine faked its citations** � Stage-4b bridge honesty audit: `planning/bridge.py` returned canned `status="completed"` with invented metrics for SWARM (`sources_inspected: 12`), BOT_PROFILE, MOA (`consensus_reached: True`), DEEP_RESEARCH (`confidence_score: 0.98` + never-written artifact names), DEEP_THINK, SUBAGENT, and DIRECT (`coordination_overhead_seconds`); meanwhile `DeepResearchEngine` silently defaulted to mock search/fetch providers that fabricated `example.org` "verified citations", and `overall_confidence` was hardcoded `0.92`. Root cause: the bridge was written against the dispatch *schema*, not the subsystems, and the research engine treated mocks as a safe default. Fix: every dispatcher now calls its real subsystem � SWARM returns the real coordinator checkpoint file; BOT_PROFILE runs real `execute_handoff` and reports `status="dispatched"` (handoff accepted ? task done) with the real `plan.markdown_report` as contract; MOA runs `MasterDeliberationEngine.deliberate(strategy=ENSEMBLE)` and relays real `DeliberationResult` fields; DEEP_RESEARCH runs the engine over live backends and derives counts from the actual report; DEEP_THINK/DIRECT make one real model call (DEEP_THINK trace verbatim; fabricated `reasoning_steps`/`self_critique_passed`/`confidence_score` and `coordination_overhead_seconds` dropped); SUBAGENT runs real `HierarchicalDelegationEngine.delegate` and maps contract status SUCCESS/PARTIAL_PROGRESS/UNRECOVERABLE_ERROR ? completed/partial/failed (new `partial`); a central try/except returns honest `status="failed"` + `"{Type}: {msg}"` + `details.error_type`. Research honesty: new `alpha/research/backends.py` resolves live ddgs (keyless) search + httpx fetch with an honest `RuntimeError` when unloadable; mocks renamed public `mock_search`/`mock_fetch` and explicit-only; `overall_confidence` is now the honest mean of per-source confidences. Verification.: targeted batch **24/24** (`test_cognitive_plan_mode` + `test_deep_research_engine` + `test_deep_research_tool`, rewritten tests pin real behavior instead of fabricated constants), `test_deliberation_engine` **13/13**, AST dup gate **0**, `py_compile` 0. Gateway `/dispatch` returns failed results in-body (schema-supported) instead of 500ing. Commit `fdaa7ea`.
15. **Stage-4c delegation contracts still permitted fabricated offline success** — subagent wave honesty: seam-runner contracts, UNRECOVERABLE_ERROR propagation, and honest oracles/stamps across 6 deep agents + their consumers, so no agent can report offline success it did not produce. Verification ✅: 68 targeted tests green + ruff clean + AST dup gate 0 (central re-run). Commit `e22fb85`.
16. **The repository had no self-evolving GitHub architecture (Phase 1)** — feature-manifest loader, `identity.json` agentId, GitHub release-check state machine with JSONL ledger, `{project_identity_section}` injected into the lead prompt, 3 additive evolution-router endpoints, SettingsSection Workspace Profile card. Verification ✅: 46 new tests green (central re-run). Commit `94c4e61`.
17. **Governance confidences were fabricated heuristics** — votes/deliberation carried hard-coded confidence numbers (frontend `?? 0.85/0.6` seeds, enterprise defaults) with no method disclosure → votes now carry `confidence_method`+`note`, unverified values render the disclosed neutral 0.5, the verification tool stops fabricating test evidence, frontend seed constants removed. Verification ✅: 22 tests incl. the new disclosure test + AST dup gate 0. Commit `b7e8f3e`.
18. **No keyless LLM fallback existed for retryable provider failures** — new `alpha/models/free_router` package: 7 anonymous provider specs behind one HTTP seam, single-flight catalog refresh with tri-state health + cooldown exponential backoff/jitter, atomic health cache, `ChatFreeLLM(BaseChatModel)` with `bind_tools`/honest `usage_metadata`/single-chunk streaming, `FreeLLMUnavailableError` carrying real `(provider, class+status)` attempt labels; `GET /api/models/free/catalog` on the existing models router (observed health only, no availability promises); config `alpha-free` entry + `fallbacks: [alpha-free]` on union-alpha. Verification ✅: **26/26** new tests + regression quartet **160** green, ruff 0, dup gate 0. Commit `cac8d0d`.
19. **Skill-evolution config existed but no engine stood behind it** — evidence-gated evolution engine: versioned candidates (active `SKILL.md` never mutated pre-promotion), honest `checks_passed/checks_total` scoring, declared subprocess suites admin-gated against a staged copy, moderation through one seam that reports `not_configured` instead of a fake pass, `auto_promote` default OFF, `security_fail_closed` rejects evaluation errors, TOCTOU baseline-hash guard, atomic promote + rollback that refuses to clobber post-promotion edits; 6 additive workshop endpoints (propose/list/get/evaluate/promote/rollback) with honest 404/403/409/422 mapping. Verification ✅: **15/15** + central re-run green, ruff 0, dup gate 0. Commit `b121ff9`.
20. **Skills had no retrieval scoring and no relationship graph** — `retrieval.py` lexical-heuristic scoring disclosing `score_method`, the full weights table and an explicit "no embeddings, no network call" note; catalog `search()` uses it for tier tie-breaks and to surface min-score candidates the regex missed; `SkillGraph` with 6 typed directed edges, neutral 0.5 baseline confidence never inferred from evidence counts, atomic save/load, corrupt file → honest empty graph, `suggest_chains` with disclosed graph-lexical-heuristic scores; 2 additive endpoints registered before the `{skill_name}` catch-all. Verification ✅: **21 new** + regressions 47+64+4 green (central re-run), ruff 0, dup gate 0. Commit `4fa9a79`.
21. **Token budgets were global-only and workflows had no human gates** — scope hierarchy (global→project→goal→workflow→node→agent) with `budget_scope` contextmanager, `effective_scope_limits` = min(own, remaining ancestors) per dimension + a `bounders` report, ancestor charging in the middleware, scope exhaustion → honest `BUDGET_EXHAUSTED` + real consumption event while the unbound path stays byte-identical; DAG per-workflow/per-node budgets + HITL `requires_approval` wired to the file-backed ApprovalQueue (`WAITING_APPROVAL` → approve resumes / reject → `REJECTED` / deadline → `APPROVAL_TIMED_OUT`, dependents `SKIPPED` and never run) with snapshot/restore persistence; cost-governor scope sub-budgets + inherited circuit breakers evaluated pre-charge (the crossing charge is recorded, real spend never erased; 5 legacy call sites unchanged). Verification ✅: central re-run **35/35** green (incl. 10+4 zero-regression), ruff 0, dup gate 0. Commit `f96dad6`.
22. **The experience bank had no FACT/TIP kinds, evidence gate or hygiene** — `ExperienceKind` (EPISODE/FACT/TIP) with disclosed neutral confidence 0.5 that hygiene can only *lower* via Laplace over real counts (formula+counts written to `hygiene_note`/audit/report — never inflates); evidence-required `add()` for every kind + pattern-only secret/PII denylist (AWS/GitHub/Slack/OpenAI shapes, PEM, JWT — labels only, matched content never echoed, rejections `{stored:false,reason}`); `relevant_for()` retrieval with `score_method=lexical_overlap_v1` + per-item reason; `run_hygiene()` expiry → dedup/merge (Jaccard ≥ 0.9 with rollback on persist refusal) → down-rank → review refresh, every mutation audited in-record + report + sidecar jsonl; experience tool extended with `type`/`evidence`/`limit` and honest refusals. Verification ✅: central re-run **15/15** green, ruff 0, dup gate 0, tool schemas 116/116. Commit `ecc4f1a`.
23. **Secrets were persisted verbatim into memory stores** — a pre-memory-write redaction filter (`alpha.security.memory_redaction`) now runs through one seam before any memory write. Verification ✅: central re-run **34/34**, ruff 0, dup gate 0. Commit `5a1f401`.
24. **No unified execution-mode switch (plan/code) existed** — gap-7: one `runtime/execution_mode.py` switch wired end-to-end (UI → API → storage → runtime), honest 422/403/persist-failure-500 payloads verified live, zero edits to policy/app/manifest. Verification ✅: Gate-1 **90 passed**, regressions **76+7+1**, manifest wiring **13**. Commit `316af9f` (row detail: TASK_LIST §4 row 7).
25. **RSI plan Wave 1 (6 disjoint units) landed** — lineage+archive, evaluator manifest, real hidden holdout (`run_holdout_gate`), durable state + kill-switch guarded `run_rsi_cycle`, opportunity miner (observe-only), strategy memory. Bulk carried by the `316af9f` sweep (disclosed: sweep performed by another actor), with per-unit tails `8e19098` (A4), `e7e0de4` (D3), `6eb74b3` (A2 determinism hardening). Verification ✅: central gate re-runs **66/66 + 42/42 + 20/20**, ruff 0, dup gate 0; every module read line-by-line centrally (TASK_LIST §5).
26. **Fabricated confidence defaults + router autonomy opt-in** — disclosed neutral-0.5 defaults for `enterprise.py`/`enterprise/models.py`/`rfc.py` seeds/`memory.py:562` beliefs; `GateRequest.autonomous_mode` HTTP default flipped to opt-in **False** (engine pin untouched). Verification ✅: central re-run **85 passed**, ruff 0, dup gate 0. Commit `d4b7af5`.
27. **RSI protected paths were convention, not policy** — WP-B3: code-constant protected-path policy with import-time validation and fail-closed deny that holds even with `reviewed=True`; human-only review channel. Verification ✅: **15/15** central re-run, ruff 0, dup gate 0. Commit `eb778c7`.
28. **No agent-status surface + theme system in the UI** — `AgentStatusPanel` bound to real `/api/plan-mode/mode`, `/supervision/fleet`, `/subagents/control` with loading→unavailable→error honesty (zero fabricated numbers, enforced by test); watchdog "all healthy" lie fixed; typed `?view=` routing. Verification ✅: `tsc --noEmit` exit 0, `node --test` **62/62** (baseline 51 → +11). Commit `7ce9060`.
29. **Cycle budgets were absent (RSI WP-D2)** — budget controller with measured-only accounting (true figures everywhere, candidate-payload override pin, clock-seam determinism, abort payload carries no score/confidence/pass), additive `state.budget` key. Verification ✅: central re-run **51 passed**, ruff 0, dup gate 0. Commit `8b96b4d`.
30. **No evidence-backed candidate factory (RSI WP-B2)** — populations with imported `_normalize_evidence`/`FORBIDDEN_SURFACES` (no second lists), lineage-before-file, atomic writes, imported-payload-hash dedup, forbidden-check-first; no fabricated score/confidence/improvement/diversity fields in serialization; disclosed heuristic prior with exploration never disabled. Verification ✅: central re-run **24 passed**, ruff 0, dup gate 0; independent read-only review corroborated all 5 honesty pins. Commit `950716a`.
31. **Candidate workspaces lacked isolation + a main-branch guard (RSI WP-B1)** — `rsi/workspace.py`: mechanical main-branch validation **before any git call**, single protected-path seam (absent → real ImportError text), copy-fallback disclosed as `assurance=lower` in the record and every evidence line, orphan report-not-drop. Verification ✅: central re-run **64 passed**, ruff 0, dup gate 0; disclosed limitations (dir-naming drift, `review_required` disclosed-not-blocked, production-layout mutating-git blocked fail-closed by SelfRepoGuard). Commit `0ed64ce`.
32. **No ops observability (event-loop liveness, metrics, readiness detail, queue health)** — OpenClaw port: `alpha/ops/event_loop.py` (missing delay figure → `no_signal`, never `ok`), `alpha/ops/metrics.py` (bounded Prometheus registry; `render_prometheus` skips declared-but-empty series so `remove_all()` honestly means absent; series pin = 2 user + cap-exempt drop counter), `gateway/ops_readiness.py` (failing[] detail, always-200, `no_signal ≠ failure`; docstring corrected to actual auth posture), `scheduler/queue_health.py` (payload-free counts/ages, registered before `{task_id}`); additive `/api/ops/{event-loop,readiness,metrics}` + `/api/scheduled-tasks/queue-health`; orphan allowlist entries added with reasons. Verification ✅: central re-run **54 passed** (incl. `test_no_orphan_modules`), ruff 0, dup gate 0 (first run 4 failed → 4 root-cause fixes, never test edits). Commit `3844da7`.
33. **No keyless web/deep search backend** — AgentEye port: `keyless_web_search_tool.py` (8-engine httpx-first keyless chain → optional ddgs fallback; honest `ok`/`no_results`/`failed`; `missing_package` names the real package; failures emit no research fields) + `deep_web_search_tool.py` (expansion → dedup → lexical rank → findings/citations/local-verify, confidence clamped ≤1.0) + registry hooks. Verification ✅: **9 offline tests**, ruff 0 (I001×2 proven pre-existing at HEAD), dup gate 0, tool schemas 118 ok, official manifest regen + wiring tests passed (tools 119→121) — `contracts/feature_manifest.json` intentionally still uncommitted until its remaining referents (multimodal + workflows routers) land. Commit `fa328d8`.
34. **Fresh-checkout orphan gate would flag Wave-1 RSI modules** — `rsi/__init__.py` carried no exports for `archive`/`opportunity`/`state`/`strategy_memory` in the committed tree (hunks sat uncommitted as a shared file). Fix: +24 additive exports committed path-scoped. Verification ✅: additive-only diff read line-by-line (imports + `__all__` only, no behavior change). Commit `d561b8a`.
35. **Hermes learning-loop ports (4 spec items)** — S1 skill-creation nudge, M1 session-search safety bounds, M2 turn-counter persistence nudge, E1 skill-usage evidence bridge ported against the real landed APIs (no invented seams; evidence reads real `propose_skill` usage). Verification ✅: central gates **38 passed** + **13 wiring**, ruff 0, dup 0, orphan 6 (4 allowlist entries with verifiable reasons). Commit `153bb0f`.
36. **The dynamic workflow layer canonized fabricated execution (and the other agent's tests enshrined it)** — DY-R1/R2/R3 remediation + workstream landing: all 7 executor fabrication sites (MAP/RACE/QUORUM/COMPENSATION/BOT/default + fake evidence strings) rewritten to fail honestly through the single `node_runner` seam with real `node_failed` reasons; DY-R2 repaired the lease call-site (real `bot_name`, ERROR-level traceback surfacing) + edge-aware base graph with honest unsupported-op rejection + non-vacuous OCC bookkeeping; DY-R3 removed the phantom `/boost` suggestion (catalog-validated only), mapped real `RetryPolicy`/`NodeType` per category, deleted mock skill/MCP bodies; router test adjudicated centrally (module-seam bound execution + honest-unbound REST pin carrying the real `no node_runner bound...` reason + HITL approval roundtrip — the old test canonized fabricated completion); root-cause fixes at landing: restored the `templates.generate_default_soul` re-export (its removal was breaking `clone_bot` mid-node) and StrEnum conversions with a format-usage audit (only `.value`-embedded f-strings). Verification ✅: **58 passed** (engine/bot/router/layer/lease/dag-edges/orphan), ruff 0, dup 0, orphan 6. Commit `7ed591f`.
37. **RSI Wave-3 C2b human review gate** — pending-only records, no self-approval (AST pin: the module never resolves its own requests and exposes no approve/reject function), verbatim reviewer provenance, fail-closed decisions on every gap (missing/corrupt record, channel mismatch, missing reviewer identity, evidence lost after opening), WP-B3 deny still closes even with an approved review. Verification ✅: **25 passed** + dependency parity **42 passed** (lineage/protected-paths/approvals), ruff 0, dup 0. Commit `3a415e1`.
38. **RSI Wave-3 C1 shadow evaluation harness** — three-state comparison (improved/regressed/inconclusive) naming every regressing fixture in `deltas` (never averaged away); fixture error or missing metric ⇒ inconclusive with the real error text and NO numeric score; `evidence_kind=measured` only when every fixture executed; content-addressed atomic records (no wall clock, deterministic) with fail-closed integrity reads on tamper/truncation/cross-file copy; constructor-pinned `channel=shadow` + offline-not-live disclosure; record-only, never an action. Verification ✅: **74 passed** (incl. `test_rsi_workspace` regression), ruff 0, dup 0. Commit `0175150`.
39. **RSI Wave-3 C2a promotion evidence bundle** — nine plan-named files carried verbatim with a sha256 provenance index + exactly-one append-only ledger event; `evidence_kind` whitelist enforced at write with honest `kind_source` (absent ⇒ `unverified`, never back-filled `measured`); `meets_evidence_standard` gates KIND only (simulated never gates; verdicts belong to the source modules); `verify_bundle` detects corruption/deletion/strays/tampered-index/path-traversal with both real digests; ledger append failure degrades disclosed, index write raises. Verification ✅: **80 passed** (bundle + lineage + identity + holdout), ruff 0, dup 0, orphan 6 (new allowlist entry with C2c serialization-point reason). Commit `a7364de`.
40. **Queue-health endpoint had no honesty suite** — 24 tests: recursive payload-leak proof (key walk + serialized sentinel), bounded-sample `queued_sample_capped` flag (global truth honestly unknown outside the sample; out-of-sample sentinel row never claimed as oldest), cap flip exactly at `QUEUE_SAMPLE_LIMIT`, independent poller/run/queue verdicts (`not_configured` scheduler is ready, not degraded), route-order pin before the `{task_id}` wildcard with both handlers serving their own shapes, auth 401/403 at decorator and handler level, gauge absence treated as honest no-signal; disclosed pre-existing `test_thread_id_route_contract` failure isolated (its file had no uncommitted diff). Verification ✅: **24 passed**, ruff 0, dup 0. Commit `44abec8`.
41. **Heartbeat and retrospective were fabrication machines** — honesty batch: no-scan/no-release fallbacks → `None` (no more `99.0`/`"v2.1.0"`/`98.5` presented as measurements); `SecurityScanReport.security_score` + telemetry `security_posture_score`/`holdout_pass_rate_percent`/`latest_release_version` nullable with zero fabricated defaults (the single real constructor always passes a real value; heartbeat passes `None` when nothing exists to measure); frontend renders "No scan"/"No active release" on null with matching types; retrospective synthesis wired to the real `get_postmortem_engine` → `list_postmortems()` → `error_summary` (the nonexistent `get_postmortem_store` import, swallowed `except: pass`, and nonexistent `symptom` key meant heuristics could NEVER see a real postmortem — the generic fallback always fired) with load/queue failures logged at WARNING carrying the real text; two dead heartbeat locals removed (F841); `test_thread_id_route_contract` hard-coded sweep root-cause fixed (`agent_messages` `{thread_id}` routes were missing from the runtime 422 sweep). Verification ✅: **86 passed** + enterprise re-run **15 passed** after dead-block removal, ruff 0, dup 0, `tsc --noEmit` 0, lib tests **62/62**; new pins `test_retrospective_postmortem_wiring` (3 tests: real postmortem → synthesis with real error text, load/queue failures disclosed). Commit `ff88366`.
42. **`evolve_bot` guaranteed a reputation improvement on every call** — the queued `cloning.py:203` finding: `reputation_score=min(1.0, source + 0.1)` bumped a fixed +0.1 regardless of the `performance_delta` sign, laundering unverified caller-claimed telemetry (a free-form dict from `BotEvolveApiRequest`) into a score. Root-cause fix: reputation now **inherits the source profile unchanged** and moves only through observed task outcomes via the single existing authority (`alpha.bots.performance` — success boost scaled by real `quality_score`, real failure penalty); new `reputation_basis` metadata key discloses the inheritance provenance. The canonizing assertion (`evolved.reputation_score > 0.8`) replaced with honest pins (exact inheritance + provenance disclosure). Verification ✅: **22 passed** (`test_bot_dynamic_workflow` + `test_bots_extended_inventory` regression), ruff 0, dup 0. Commit `013e795`.
43. **Settings rendered a fabricated probe count and fought the theme system** — `probeAll().catch(() => [])` collapsed a FAILED probe request into an empty list, rendering a false `0/0 subsystems online` badge; loadData now carries an explicit ok/list envelope + a `probesUnavailable` flag, and the badge renders three honest states: `Checking subsystems…` while in flight, `Subsystem status unavailable` on failure, real count only when probes actually ran. Theme: SettingsSection resolved an unstored theme as `dark` while `ThemeController` resolves it as `system` (OS preference), so the checked card could disagree with the applied theme; the load effect now reads `THEME_STORAGE_KEY` through `isThemeMode` with a `system` fallback, `handleThemeChange` delegates to the shared `applyThemeMode` (system resolution + classList + colorScheme — the hand-rolled toggle skipped both), and a third `System (Default)` card exposes the real three-mode state (`Dark Theme (Default)` relabeled — dark is no longer the unstored default). Verification ✅: `tsc --noEmit` exit 0, `node --test src/lib/*.test.mjs` **83/83**. Commit `59a1d9e`.
44. **RSI Wave-3 C2c: promotion decisions had no composer (wave's serialization point)** — `rsi/promotion.py` `decide(candidate_id)` evaluates a closed 6-gate set (lineage, bundle_integrity, evidence_standard, holdout, human_review, evolution_route) with no short-circuit; every gate's reason travels VERBATIM from the landed module that produced it (C2a `verify_bundle`/`meets_evidence_standard` over the real finalized index kinds, real `holdout_gate` tuple, C2b `review_decision`, lineage store incl. skipped-lines corruption disclosure); `promoted` = AND of all entries, failing entries join as `<gate>: <reason>`, a failed composition appends an explicit `not routed: …` disclosure and the engine is never invoked; routing through the module seam → `evolution/promotion_route` → real `EvolutionEngine.gate` with `autonomous_mode=False` pinned (the landed `GateRequest` contract, pinned by `GateRequest()` defaults + `inspect.signature` + the real `missing candidate or benchmark` verdict); simulated never gates, unverified enters as landed 0.5-neutral text, gate exceptions fail closed as `failed closed: Type: text` logged at WARNING; decision serializes no confidence/score/rating/timestamp (literal forbidden-key + fabricated-default pins). Central fix-at-landing: routing entry now enforces the module's own no-empty-reason invariant (+ 16th pin test). Disclosed incident: a duplicate C2c dispatch (main-agent retry) produced a second, never-centrally-reviewed implementation (`promotion_dispatch.py` + `evolution/__init__` re-export) — strays discarded uncommitted; the reviewed implementation is the committed one. Verification ✅: **114 passed** (16 C2c + orphan 6 + bundle/review/identity/confidence parity), ruff 0, dup 0. Commit `0b5bfcf`.
45. **Dynamic workflow REST had no kernel: execution raced, failed runs hung RUNNING, and no paradigm routed through the DWE** — P1 + DY-R4 (`564f004`): new `alpha/orchestrator` kernel modules composing the real engine through its public API — `ExecutionKernel` (per-run `threading.Lock` claim serializing dispatch/patch/approval so OCC rejections are deterministic; fail-closed policy drives a run left RUNNING with failed nodes to FAILED with an honest `workflow_failed` event naming the ids — compensating the engine gap without editing `runtime.py`); `ExecutorRegistry` (`build_runner()` returns `None` when empty so the engine's exact `no node_runner bound to execute node '...'` refusal survives; default binding `alpha.local.digest` only — `sha256(run_id\nnode_id\nprompt)`, `tokens_used: 0`, evidence independently recomputable; raising/malformed/unresolvable executors fail honestly with the real traceback); `mode_mapper` (all 7 paradigms → typed DWE graphs, AGENT-vs-BOT kind gated by mode per plan §13, swarm returns the explicit not-expressible verdict verbatim — never a static fan-out misrepresenting a swarm); `replay` (event-log fold onto a fresh engine with zero re-emission + disclosed payload gaps); REST: start/step/patch/approval through the kernel, optional `mode` on run create (invalid → 400 pre-run), new `POST /api/workflows/turns` (TurnOutcome verbatim; mapper refusal → 400 with the exact reason) + `POST /api/workflows/runs/{id}/replay` (covered-field comparison, real values both sides, `events_emitted_during_replay == 0`). Tests: unbound fail-closed pin KEPT (binds nothing, asserts the engine's exact refusal), digest evidence recomputed from the documented formula (never read from state), threaded REST serialization + concurrent-patch OCC with the engine's exact violation message, approval/replay fold disclosures. Disclosed: duplicate dispatch of this task (two sessions) reconciled by the survivor via mtime with no clobbering; 9 `runtime.py` bugs reported not edited → §9 open row. Verification ✅: **78 passed** (router + 6 orchestrator suites), ruff 0, dup 0; agent-run regression 34 + orphan 6; orphan re-run 6 centrally after the gateway batch. Commit `564f004`.
46. **The voice wave's real-smoke bugs and provenance gaps** — voice completion (`5155124`): three bugs only a live smoke could catch, found + fixed — `_rapidocr` handed a 1-D uint8 array (raises `LoadImageError`; now raw `bytes`), openWakeWord 0.6 does not auto-download in `__init__` (explicit `download_models()` + deliberate `inference_framework="onnx"` with the Windows/py3.12 tflite-runtime rationale documented) and post-warm-up `np.float32` scores were discarded as wrong-shape (numpy scalar types now accepted); STT success payload gains the plan §5 `language` field; wake `transcript` events now name the STT engine+tier that actually produced the text (was attributed to the wake scorer); `voice:` config block appended to `config.example.yaml` + live `config.yaml` mirroring `voice_config.py` defaults exactly (no config_version bump); frontend VoiceControls/Composer mic/MessageItem speaker/SettingsSection Voice & Speakers card render ONLY observed capabilities, with the autoplay preference honestly labeled "saved in this browser; automatic playback is not wired in this build". Disclosed: two adjacent mic controls in Composer (both functional); ~43 s cold `capabilities_report()` once per process; deferred autoplay consumer = documented one-liner for ChatView (outside the agent's MAY-EDIT); live HTTP/WS smokes deferred to the gateway-restart window. Verification ✅: **103 passed** (chain/router/wakeword/cold-imports/manifest wiring) + orphan 6 separately = recorded 109, ruff 0, dup 0, `tsc --noEmit` 0, lib tests **83/83** (voice.test.mjs 21). Commit `5155124`.
47. **The workflows router was import-unreachable at runtime behind a stale allowlist entry** — gateway batch (`4fbd133`): `app.py` now imports + mounts `app.gateway.routers.workflows` (the held hunk; multimodal was already mounted at HEAD), making the whole P1 REST surface (start/step/patch/approval/turns/replay) actually served; the `app.gateway.routers.workflows` `ALLOWED_ORPHANS` entry removed in the SAME commit so wiring and proof land atomically (root-cause removal, never suppression); `contracts/feature_manifest.json` regenerated via the official script only — lists both `app.gateway.routers.multimodal` and `app.gateway.routers.workflows` + the tools 119→121 delta, `tools` array never hand-edited. Verification ✅: `test_no_orphan_modules` re-run centrally AFTER the entry removal with the mount live — **6 passed** (3:33); manifest wiring green inside the voice gate. Commit `4fbd133`.
48. **RSI had no stabilization window, no immutable release store, and no §54 failure taxonomy** — Wave-4 (`13c446e`): three NEW sibling modules composed by `rsi/promotion.py`, never duplicated. §42 `alpha/rsi/cooldown.py`: `record_promotion` writes EXACTLY `source_change_id`/`cycle_id`/`promotion_timestamp`/`cooldown_until` (literal-pinned) via `atomic_write_json`; `cooldown_state`/`evaluate_cooldown` compute real remaining seconds from the injected `RSI_COOLDOWN_CLOCK` (ceil-displayed — window never understated), `never_promoted`/`cooling_down`/`ready` from arithmetic only; corrupt/unreadable record → fail-closed real text, bytes never repaired; `RSI_COOLDOWN_SECONDS` validated (invalid raises — never silent default); a failed decision writes NO cooldown. §26/§43 `alpha/rsi/releases.py`: create-once immutable `releases/vNNN/` dirs with the exact §43 manifest; order-as-contract — `decision.json`+`release.json` written (atomic) FIRST, the ONE `current.json` flipped LAST, so the pointer never names a half-written dir; ids from on-disk `max+1` (real store, never a clock); `commit` = disclosed literal `"unknown"` (no git subprocess by fence); `artifacts` = bundle index's verified sha256 digests recomputed from disk; `evaluator_manifest` = real `build_manifest()` behind a state seam (one real build memoized per session, never faked); dangling/corrupt pointer → `ReleaseStoreError` fail-closed, never re-created; only a `promoted` decision payload may release. §54 `alpha/rsi/errors.py`: `RSI_ERROR_CODES` = literal E001–E018 + this build's ONE designated-disclosed `E000 Unclassified`; real signals only (isinstance on the genuinely-consumed `StaticScanBlockedError`→E008, `ReleaseStoreError` E018-vs-E013 split, ordered keywords grounded in landed texts, real exception classes, else honest E000 — `TimeoutError` admitted unknown, never misfiled; provider outage→E001, never candidate-defect); `classify_gate` context rules with `None` ONLY for the derived `not routed:` (never double-count); codes decorate ONLY composed `decision.reason` + WARNING logs — gate entries stay byte-for-byte verbatim (C2c contract exact-equality pinned). `promotion.py`: `cooldown` gate inserted BEFORE `evolution_route` (cooling candidate never routed — routing seam pinned to raise if invoked); fully-green decisions materialize release FIRST then cooldown with disclosed `release_store`/`cooldown_record` failure entries (real text + code + honest partial-state notes) flipping `promoted=False`; promoted reason remains the engine's verbatim verdict; consumed-vs-cited honesty for `variation_operator`/`avo_lineage`/`self_improvement` documented in the docstring. Disclosed: post-routing bookkeeping entries sit outside the closed `GATE_ORDER` by design; engine ledger may record promoted before materialization fails (engine.py not editable — divergence disclosed, `decide()` reports `promoted=False`); stale docstrings in non-editable `promotion_route.py`; manifest memo ~10 s/session (real bytes); E000 is this build's designation, never attributed to the spec; two latent kwarg bugs caught+fixed before the suite ran. Verification ✅: central re-run **40 passed** (14 wave4 + 16 promotion + 10 evidence_bundle, 1:46), ruff 0 on all 6 files, `ast_dup_gate_dups=0`, orphan **6 passed with NO allowlist edit** (new modules genuinely reachable through `promotion.py`). Commit `13c446e`.
49. **Audit batch A closed seven HIGH fabrication sites across enterprise/daemon/arena** — `b21d62b`: perpetual daemon no longer marks zero-work tasks `completed` with invented counts/progress (honest 0 + `execution_note`); discovery latency profiles stop serving hardcoded figures as telemetry (honest-empty with disclosure); council `96.4` constant verdict moved to the router with the score and `evidence_kind=simulated` caveat carried verbatim (`holdout_passed` no longer a bare HTTP lie); governance ROI floor `<0.1 → 0.85` deleted; arena stops appending caller-asserted `simulated_pass` runs as real and drops the zero-attempt `90/12/85` leaderboard defaults; recovery `HOT_REPLACE` no longer claims a hot-swap + rehydration that never runs. Verification ✅: central re-run **54+1 passed**, ruff 0, dup 0 (the +1 = documented pre-existing environmental failure). Commit `b21d62b`.
50. **Audit batch B1 removed fabricated epistemic receipts and bot metrics** — `de6f1ce`: CI receipts ("All 29 tests passed in 27.21s", "CI green receipt verified") no longer seeded as FACT@0.98 → HYPOTHESIS/neutral with the evidence strings deleted; zero-run bots stop reporting `success_rate=100.0`/reputation `1.0` → `None` + disclosure; duplicate `POST /{name}/clone` (`bots.py:398` shadowing `:812`) disambiguated to `/{name}/clone-engine` with a dual-route HTTP proof + frontend call-target verification (auth untouched). Verification ✅: central re-run **136 passed**, ruff 0, dup 0. Commit `de6f1ce`.
51. **Docs claimed unmeasured sub-50ms halt latency and two UI defaults lied** — `da5c8d2`: the sub-50ms emergency-stop claim (no measurement anywhere) dropped from `docs/SECURITY.md` rather than softened into another guess; MessageItem guards confidence rendering against NaN%; SettingsSection resolves the unstored theme as `"system"` (matching ThemeController) instead of `"dark"`. Verification ✅: path-scoped commit of S3-owned files only (3 files, +8/−4); shared other-agent docs held for end-landing. Commit `da5c8d2`.
52. **All 11 §9 `runtime.py` engine gaps were real root causes** — `0bb2871`: fail-closed on failed nodes with a COMPENSATING-deferral disclosure, real-node stagnation recovery (never a phantom target), journaled `_set_run_status` (no eager status lies), deepcopy event snapshots (no payload aliasing across listeners), quiet-sink patched-graph replay (post-replay dispatch uses the registered graph), approval events carry per-node `node_status`, denial appends `failed_nodes`, `node_iteration` events journal loop honesty; the six edited disclosure pins were inverted to the fixed behavior (they fail loudly if `runtime.py` regresses). Verification ✅: central re-run **129 passed** (13 files), ruff 0, dup 0; new suites `test_runtime_fail_closed.py` (8) + `test_runtime_event_integrity.py` (7). Commit `0bb2871`.
53. **Five frontend sections + six lib functions rendered failed loads as measured empty states** — `2385c41` (wave-2 S1-C, F13–F17 + residue): per-surface error flags with ErrorBox + retry (runs detail, memory's five tabs, kanban projects/server-board, messages per-list, WorkspaceVitals three-state gateway badge); `api.ts` gains the `FetchResult<T>` contract (`{ok:false,error}`, never `[]`) with the `[]` wrappers kept as thin LEGACY-COMPAT shims documented ONLY for the fenced ChatView (throwing by default would abort its un-awaited bootstrap); root cause required de-swallowing four free libs (comm/inbox/teamops/threads-ext) because component-level catches could never fire while libs returned `[]` internally; owned caller `threads-ext.ts` switched to the honest Result API. Verification ✅: new `load-failure-honesty.test.mjs` **20 tests** (Result-vs-wrapper pins, rejection pins, component source pins); central `tsc --noEmit` exit 0, `node --test` **110/110**; zero pre-existing tests pinned the dishonest behaviors (grep-verified — no inversions). Commit `2385c41`.
54. **TTS autoplay had no default-OFF consumer gate** — `656a8ac` (S4): `speak()` performs one honest playback (real synthesis/playback errors, SPEAKING marked via the session registry without hijacking an armed/capturing/processing session, object URL always revoked); `autoplaySpeak()` is THE preference gate — absent storage ⇔ disabled, so the speak implementation is never invoked without explicit opt-in, and every failure is handed to `onFailure` for visible disclosure (never a silent swallow, never rejects); `chat-request-error` harness injects the module-scope bindings and pins `autoplay === 0` across every request/error/abort scenario. Verification ✅: +2 voice tests (default-OFF + opt-in-with-disclosure); central `tsc --noEmit` exit 0, `node --test` **110/110**. Commit `656a8ac`.
55. **Three residual audit findings leaked unverified numbers into production output** — `991b7ad` (R1/R3/R4): container runner host fallback prepended `Path(sys.executable).parent` to the subprocess PATH before `shell=True` so bare `python` resolves on hosts with no system Python (docker branch + caller command untouched; pinned by the read-only `test_ai_workforce_phase6` going from exit 1 "'python' is not recognized" to green); frontend fleet reputation stops fabricating 1.00/0.00 (normalizeBot default 1→null, `computeFleetHealth` averages only measured scores and returns null when none are, `number | null` types make tsc enforce the null-handling, four components render "unverified"/"none measured"); roster tool stops leaking object-`None` as measurements (`None%` success, bare `Reputation: None`, zero-run `0.0s` average, `@None` handles → unverified/none). Verification ✅: central **36 passed** (test_ai_workforce_phase6 + test_advanced_bot_tools + test_bots_dm), ruff 0, dup 0, tsc 0, node **110/110**; R2's system-monitor hunks share their files with batch B2 and ride B2's landing. Commit `991b7ad`.
56. **Four slash-command handlers claimed verified/healthy/compact outcomes without executing anything** — wave-2 S1-A (`c86b660`): `/security-review`'s canned "All security invariants verified… `secure: True`, `privilege_ring: 0`" replaced by 6 real in-process probes (SafetyGuard destructive-command block/allow, sensitive-path block/allow, `find_secret_shape` denylist, SkillScan over synthetic PEM-poisoned/prompt-override/clean skill dirs, sandbox env-policy rules + built-env leak scan) with `_derive_secure` fail-closed (empty/any-fail/any-unknown → False) and the two networked checks disclosed verbatim as "Not verified in this run (no verdict claimed)" — excluded from the verdict, never counted pass or fail; fabricated `privilege_ring` removed (swept: zero consumers); `/skill:test` computes all four checks for real (shared `_validate_skill_frontmatter` + registry-name match, actual container-path resolution, live `scan_skill_dir` — scanner errors → unknown/never-True) and root-causes why every created skill failed validation (`tags:` not in `ALLOWED_FRONTMATTER_PROPERTIES`, removed from the `/skill:create` template); `/doctor`'s four hardcoded `ok=True` → real probes (requires-python ≥3.12 compare, real config load + resolved path — the old detail claimed a nonexistent `config.yaml (vdefault)` version attr, model count, skills-registry walk, sandbox provider import+subclass smoke) with `_derive_doctor_status` (unknown never counts toward READY, empty → fail-closed `not_ready`); `/compact` fails closed (`status=error, completed=False, implemented=False`) naming the real seam `POST /api/threads/{id}/compact` instead of summarizing nothing. Verification ✅: central **13 passed** (105.13s), ruff 0, dup 0; the four constant-True pins replaced by derivation pins (`passed == all(checks.ok)`, `status == _derive_doctor_status(checks)`, `secure == _derive_secure(checks)`, same over HTTP) + negative pins (injected failing/unknown check → non-ready/`secure=false`; fabricated-string absence pins) and the fixture now isolates `AGENT_WORKSPACE_SKILLS_PATH` to a temp root. Commit `c86b660`.
57. **A kill score of "perfect" was reported for runs that never executed** — `ba6fac3` (audit S1-B, with the oracle/syntax-guard/sdlc siblings): `MutationTestingEngine.run_mutation_audit` returned `kill_score=1.0` both when no mutants were generated and when every test run RAISED (so zero tests executed still scored 1.0). `kill_score` is now `float | None` — None with a `disclosure` stating why, test-runner exceptions counted as `errored` and excluded from the score (not treated as survived), and `total_mutants` reporting the mutants actually generated. `OracleResponse.confidence` stopped defaulting to 0.95 and is derived from the fraction of a curated domain's trigger keywords that actually match (generic fallback: 0.0, disclosed). `BenchmarkTaskResult`'s `tool_precision`/`verification_score` stop defaulting to a perfect 1.0. The AST syntax guard fails closed on empty JSON (previously waved through) and on missing PyYAML (previously every YAML file "passed" unparsed). The two fabricated tools are deleted: `run_mutation_testing_audit` (its "test runner" was an identity lambda that always returned True) and `run_speculative_synthesis_tournament` (scored "test results" by string length). Verification ✅: 28 passed across the five affected suites + 7 in the SDLC engine's suite, dup 0, 0 new ruff.
58. **The token treasury was being fed invented per-cycle burns** — `46dbca8`: every heartbeat invented department burns (`4500` when tasks advanced, `1200` when none did, plus fixed `800/600/700/500` baselines and `400` for every dynamic department) and `record_token_burn` computed `burn_rate_tpm` and CIRCUIT BREAKERS from them — so fiction could block real spending. A sprint step reports tasks, not tokens, so the heartbeat now records only real counts and discloses `token_burn_measurement: "measured" | "unavailable"`. Same batch: a bootstrapped org no longer claims exactly 3 sleeping bots with "active = total − 3" (counts now come from the attendance ledger, with an `attendance_measured` marker so "nobody has reported" never reads as "everyone is idle"); KPI seeds carry `basis="seed_demo_data"` until a real update flips them to `measured` (root cause also fixed: `KPIEngine` aliased the shared DEFAULT list, so one engine's reading mutated every other engine's seeds); the `999999.0` heartbeat sentinel became `None` + `heartbeat_parse_error`; `overall_health_percent`/`ExecutiveDigest` stopped seeding 100.0/95.0. The UI mirrors all of it (AVO button fails closed instead of relying on removed server defaults, benchmark baseline explicit, `system_latency_p95_ms: number | null`). Verification ✅: 71 passed, then 53 re-measured after the lead's token-burn + attendance additions; tsc 0, node 110/110; ruff 0, dup 0.
59. **The retrospective invented performance and the discover endpoint invented findings** — `4179e34`: `run_retrospective` defaulted `completed_tasks_count=5`/`resolved_incidents_count=1` (so any caller passing nothing published five executed tasks and one auto-remediated incident), invented three specific playbook changes per cycle (including a threshold change that never happened), and named every department lead "calibrated" although nothing adjusts profiles. All three are now measured-or-absent with explicit disclosures, and the KPI summary prints "not yet measured" instead of "Active Bots: 0". `POST /company/discover-work` invented two findings ("Automated security patch available for urllib3", "Memory leak reported…") with impact scores when no signals were supplied; it now discovers nothing and returns `signals_source`/`disclosure`. Verification ✅: 21 passed; disclosed test inversion (the old pins required ≥ 2 invented playbooks and ≥ 2 "calibrated" bots); dup 0.
60. **A degraded memory read was dressed up as a successful one** — `f882542`: when the configured provider read failed or returned nothing, the lead-agent prompt substituted the cognitive side channel, producing a success-shaped `<memory>` block that hid the degradation from every caller (and a bare `except: pass` swallowed the enrichment error). Cognitive blocks may now only enrich content the backend actually returned. Separately, `asyncio.wait_for(asyncio.to_thread(...))` CANCELS its wrapped future, which propagates into the executor job — a still-queued injection was discarded before any worker ran, making "did the injection start" a race against the deadline. `_timeboxed_to_thread` bounds only the wait: a queued or running worker always finishes, and its late outcome is disclosed by callback instead of becoming an unretrieved-task warning or being mistaken for this call's result. Verification ✅: 77 passed across the three suites; three test pins corrected (two mocks now outlive the worker they feed; a policy pin that encoded a 10ms race is now deterministic); disclosed inversion (a duplicate-tool-name pin that certified a state the registry cannot produce); prompt.py's 10 pre-existing E501s left alone (reflowing prompt text would change what the model reads).
61. **The System-1 fast path existed but nothing called it; the loop path had no reflex** — `85b7629` after `0dcae51`: Module B landed as a library and Module C's five desktop tools landed UNREGISTERED (unreachable in production) until `0d673c7` closed that. This commit wires the reflex into the engine: it may PROPOSE an early loop stop, and the deterministic runtime acts only when the proposal comes with agreeing evidence (real evidence from prior iterations, nothing failed), journaling every consideration and naming the cause (`stopped_by="system1_reflex_agreement"`). A reflex refusal never blocks a loop and a layer failure is disclosed, never raised into the run. Verification ✅: 18 passed (5 new pins + the workflow router and persistence regressions); ruff 0, dup 0.
62. **Dynamic-workflow runs were process-local and could not survive a restart** — `650437e` (W-N1): run state lived in module singletons, so a restart lost every event and a fresh process could not see a live run. `DurableEventLog` appends every event to an fsync'd JSONL log with writer-assigned sequence numbers and DOC-A §13.3 idempotency keys, projects runs atomically, and hydrates a fresh engine. Its refusals are explicit: an unpersisted append RAISES; a corrupt tail is disclosed with its line number and further appends are REFUSED (an O(1) tail check catches damage that arrives after the writer cached its sequence — a bug found and fixed during this wave's own review); a projection behind the log is NOT installed; a corrupt projection is reported and left exactly as found. `PlanGraphStore` adds append-only graph-revision history with compare-and-set (a stale writer is rejected, a duplicate revision refused, a corrupt file is a loud error). The dispatcher gained a durable sink so all 22+ emit sites are journaled without touching runtime code — and three silent `except: pass` handlers became visible disclosures, including a bus import of `get_global_bus` that NEVER EXISTED (workflow events silently never reached the bus at all). REST: `/system/durability`, `/runs/{id}/events/durable`, `/runs/{id}/project`, `/hydrate`, `/{workflow_id}/plans`. Verification ✅: 37 passed including a kill-and-resume proof (process A journals and projects; a brand-new engine B with no shared memory hydrates the run, definition and graph) and the pre-existing P1 kernel router suite; ruff 0, dup 0.
63. **Four capabilities were implemented but not registered; the manifest was stale** — `883b44d` + `0d673c7`: the shared catalog was deliberately held while sibling mission packages were in flight, because a committed entry naming a missing module breaks a fresh checkout. With every module landed, `intent_goal_engine`, `system1_reflex`, `os_computer_use` and `durable_workflow_state` register together with the officially regenerated manifest (tools 121 → 127, routers 57), and the two dormant-engine `ALLOWED_ORPHANS` entries land so a fresh checkout passes the orphan gate. Verification ✅: orphan gate 6 passed (334.70s), manifest wiring 13 passed (362.13s), `check_tool_schemas.py` 124/124.
64. **The UI claimed health the backend never measured** — `23ce45f` (batch B2 + R2): the 1/5/15 load average was served as `cpu_percent`; a quality-gate criterion with no evaluable tokens auto-PASSED; suggestion-generation failure returned `200 []`, indistinguishable from a genuine empty result (now 502, while a real empty list is still 200); a Brier score of `0.0` ("perfect") was returned for an empty history (now `None`, with corrupt-record drops counted); a rubric with zero criteria returned `APPROVED` (now ABSTAINS as WARNING with a diagnostic — an unevaluated rubric can never read as approval); benchmark scores defaulted to perfect; a swallowed AttributeError in the system monitor rendered as a healthy machine (now null + degraded); and the monitor UI renders per-widget unavailable/retry instead of host-property claims. Verification ✅: 164 passed across nine suites; tsc 0, node 110/110; ruff 0, dup 0; one disclosed inversion (the batch's own plan mapped a model error to a 200-empty body; central review rejected it).
65. **Bootstrapped performance, discovery and calibration were fiction at the REST and engine edges** — `0de236d` (audit S1-D): `AVOIterateBody`/`MetaBenchmarkBody` defaulted their MEASURED fields, so `POST {}` committed a VersionRecord built from invented numbers (composite 0.948 from 0.88/0.92 defaults); the defaults are gone — an empty or partial body is a 422 that commits nothing, and caller values flow through unchanged. Startup-seeded beliefs were seeded at near-certain confidence (0.98/0.95/0.96/0.92) with a `pinned` tag; they are now modest 0.5–0.7 assumptions carrying `source="bootstrap-assumption"`, empty evidence ledgers, no `pinned` tag, and zero usage counts on seeded skills — provenance surviving a save/reload. Verification ✅: 26 passed including the projects-router regression; dup 0; disclosed inversion (no existing test pinned the old values, so these replace an absence of coverage); 3 pre-existing UP042 left alone (the StrEnum migration would change `str()` output repo-wide).
66. **Ephemeral specialists could outlive their lease on a typo, and a corrupt store read as "no specialists"** — `a905d97` (Module D1): the lease store swallowed every read error and continued with an EMPTY set — a TTL sweep would conclude every leased bot is gone while it still runs; it now raises `EphemeralLeaseStoreError`, as does a failed persist (a spawn that cannot record its lease must not report success). An unparseable `expires_at` is treated as EXPIRED with `archive_reason="invalid_expiry"` instead of "not expired", and a real elapsed TTL is now distinguishable from that failure. Skill publishing gained audit-before-activation: the static security scan runs on the exact bytes BEFORE any file is created in the skills-discovery root, and blocked content returns 422 with the real scanner reason. New real-engine proofs cover all three clone modes (isolated memory namespace, directive injection + real lease on disk, generation bump + lineage chain), deterministic TTL expiry, the MCP mount lifecycle and the skill-authoring round trip. Verification ✅: 11 passed (re-run after the hardening), ruff 0, dup 0.
67. **The free-model catalog, project scope and TTS autoplay were built but not reachable from chat** — `06d7a08`: `lib/freeModels.ts` (new) is an honest catalog client — it never fabricates a provider, reports `healthy: null` when the server did not measure health, and separates `eligible: false` from `eligibilityKnown: false` so unknown eligibility is disclosed rather than asserted. ChatView consumes it (initial load + a real-probe refresh), states the real reason on every failure path, adds in-chat project scope, and wires the TTS autoplay call site behind the default-OFF gate with a VISIBLE failure notice — firing only on a final, non-aborted assistant reply. Docs: README/FAQ/llms.txt refresh, with the FAQ's pinned tool count corrected 121 → 127 against the regenerated manifest. Verification ✅: tsc 0, node 110/110 (the freeModels suite is inside that 110).
68. **[PENDING] Deep Agents integration wave** - user directive 2026-09-24: audit Alpha against `references/ALPHA_DEEPAGENTS_INTEGRATION_GUIDE.md` (52 sections; section 44 mapping, 48 must-have/high-value lists, 49 ten immediately-reusable ideas) and implement only the genuine gaps (virtual workspace + composite storage, artifact offload, context compaction, progressive-disclosure layered skills, scoped persistent memory, isolated/parallel/async subagents, approval+resume, rubric verification, model profiles, durable checkpoints, event observability); zero orphan modules, zero unwired features; catalog/manifest fenced while W-N3 runs (registrations reported to the lead). Dispatched; commit pending central review + gates.
69. **[PENDING] Real-task end-to-end live run** - user directive: after full suite + recovery 30/30 + one gateway-restart window, run Alpha on a REAL task through UI -> API -> runtime -> tools -> persistence -> logs -> recovery, verify every hop honestly, fix whatever surfaces. Gated (see TASK_LIST section 2 + section 8).
70. **[IN PROGRESS] Electron build + easiest Windows install (Hermes-referenced)** - phase-1 research RECEIVED and lead spot-checked: recommendation = keep/harden the EXISTING per-user NSIS installer (bundled Node 22.17.0 + uv + backend + Next.js standalone; no UAC; identity-verified health; tree-kill shutdown) - already architecture-complete and ahead of Hermes 8 MB shim+script flow; real gaps: stale exe (17:51 vs win-unpacked 23:14), no latest.yml/auto-update, unsigned/SmartScreen, first-launch Python download, missing LICENSE (USER DECISION), docs mismatches (root README installer filename + electron/README tag example, both fixed here); phase-2 E1 regenerate installer + electron npm test DISPATCHED, E2 clean-profile install/run smoke gated on the suite gateway-restart window, E4-E8 decisions queued. Commit pending phase 2.
71. **[PENDING] Memory system verification + advanced scheduler/cron memory (Hermes-style)** - full memory-path audit (write -> persist -> retrieve -> prompt -> UI) with root-cause fixes, plus durable job memory: every scheduled/cron run journals real outcomes to scoped memory and later runs consume prior context before acting, measured-or-None with disclosures only (Hermes reference: temp clone hermes-agent). Dispatched; commit pending central review + gates.

72. **W-N3 read-only discovery registries landed (the runtime half of the one-file-index idea)** - `b331c04` (9 files, +1155): new `alpha/workflow/registry/` package = `CapabilityDescriptor`/`RegistryHealth` protocol (fail-closed `RegistryUnavailable`; `health="unverified"` + `version=None` + `unavailable => reason` enforced by TEST), read-only honest views over capabilities catalog (isolated per-entry `find_spec`), `BUILTIN_TOOLS`, skill storage, MCP config (no transport), memory modules (no store), `WorkflowRegistry` facade with never-raising isolated `health()` + locked singleton; catalog shared-append +6 verified entry-alone; central gates ruff 0 / dup 0 / 14 passed (119.52s) / agent 20 passed incl orphan (354s); disclosed: own-test Path-portability fix, loader-mediated (not eager) import chain, BUILTIN-only scope, blueprint leftover command-capability parity test owed to wave P1.

73. **Deep Agents guide wave landed (genuine gaps only, audit-first)** - `64201ee` (5 files, +893): `alpha/deepagent/{__init__,workspace,composite}.py` storage-only `VirtualWorkspace` Protocol + `LocalWorkspace` (fail-closed path security before IO: `..`/colon/symlink-escape rejected; atomic writes; `to_thread`; utf-8; measured sizes) + `CompositeWorkspace` (declaration-order first-match, deny-route `/secrets` fail-closed, dup/glob prefixes rejected, `_refix` re-mapping bug fixed by its own test) + honest `WorkspaceUnsupported.execute` pointing at sandbox bash + 12 real tmp-disk tests; `sandbox/__init__` reachability seam re-exports 12 names (orphan 6 + manifest-wiring 13 passed by agent; central ruff 0 / dup 0 / 12 passed 51.50s re-run); no catalog/manifest/allowlist edits; backlog report-only: delete tool, GPU routing, marketplace, Agent Protocol, team skills, event-name adoption.

74. **Memory system verified end-to-end + Hermes-style scheduler/cron memory landed** - `0fd04bc` (6 files, +1442): Part A `test_memory_roundtrip_e2e.py` (9 tests, real DeerMem store, storage never mocked): write -> disk bytes (.md fact + memory.json) -> fresh-manager retrieve -> `<memory>` injection gate -> HTTP API; corrupt store => typed error => HTTP 500 stable detail, bytes never repaired; degraded read => ERROR log with original exc_info, no success-shaped block; fail_closed re-raises; Part B `job_memory.py` (JobMemoryStore: append-only JSONL, fsync+advisory lock, typed corrupt-error naming file+line, read-time-only retention with verbatim disclosure, NO OUTCOME RECORDED honesty) + `service.py` dispatch-before-launch fail-closed journaling, measured-or-null durations, requeued/launch_failed statuses, post-launch write failure => ERROR + capped prompt disclosures (never fake entries), None-default keeps prompts byte-identical; disclosed 2-line app.py wiring. Central gates: ruff 0, dup 0, 179 passed (688.93s); agent: 20+9 new, 150+150 regressions, orphan 6; initial test failures were genuine findings fixed not weakened.

75. **W-B frontend exposure: Workflows / Goals / Missions / Checkpoints / Jobs** - `dc578fd` (8 files, +2890): 34 previously-unconsumed backend routes now have real UI clients + a 3-panel section; honesty pins tested (`matches_live:false` stays false, `writable:false` stays false, `test_passed:null` renders "tests not run", `events_emitted_during_replay:0` stays 0, null job timestamps never become epoch 0, failed calls reject — no fabricated rows); central gates: tsc 0 project-wide (run twice), 29/29 lib tests (agent + lead); lead wiring: NavTabs + workspace-view + ChatView lazy/ternary; disclosures: 4 backend gaps reported honestly in-UI (no list-runs / list-plans / list-attempts routes, in-memory checkpoints, jobs auth 403).

76. **W-C frontend exposure: Skill Workshop / Evolution / Policy / Benchmarks** - `0925d36` (12 files, +2671): 26 previously-unconsumed backend routes now have real UI clients + a 4-panel Forge section; autonomy pinned off by omission (`gateEvolutionCandidate` defaults `autonomous_mode:false, human_approved:false`; `promoteSkillEvolution` requires explicit `approve:true`); limits clamp to router ceilings (500/200); 39 tests incl. 26 contract pins (client paths asserted against real router decorators); central gates: tsc 0 independent + 39/39 double-run; lead wiring NavTabs/workspace-view/ChatView. Disclosures: 4 routers emit no `basis`/`evidence_kind` (chips render nothing — no fabricated labels); benchmark results + policy approvals are in-process gateway state (UI states it verbatim); own pre-restart fixture bug fixed to match verified backend contract.

77. **W-A: AutonomySupervisor + Sentinel exposed end-to-end (API + durable journal + UI + shared error-honesty fix)** - `c5e1be8` + `f2ce940` (14 files, +1975): 4 new `/api/autonomy/*` routes (status / sentinel reports / sentinel run / sentinel signals) mounted in the real app (verified by importing the app and listing routes); new `runtime/sentinel/report_store.py` (append-only fsync JSONL journal, advisory lock, typed corrupt-error with file+line, bytes never repaired, capped read + verbatim disclosures) wired at the single `sentinel_tick` choke point (supervisor ticks and API ticks share it); `SupervisorSection.tsx` + `lib/supervisor.ts` show all 7 loops with real counters (sentinel/perpetual/review_queue/skill_curator/enterprise_heartbeat/swarm_status/free_models_sync), disabled-by-default shown as disabled, never-run as "never", parked reasons verbatim, report history with real fixed/reverted/escalated counts, Run observe pass with auto-heal opt-in OFF, observe-only signal list; root-cause fix: `ApiClientError.detail` now carries the gateway detail (FastAPI `detail`/`error`) into every error surface - previously ALL "verbatim server error" claims were false (only "Request failed (HTTP N)." reached the UI); gates: ruff 0, ast_dup 0, backend tests 26/26, tsc 0, FULL frontend lib suite 191/191, live mount check OK. Disclosures: subagent rate limits forced lead-executed frontend; loop default remains disabled in config (shown honestly); W-A session lost after backend completion.

78. **W-D: Protocols (A2A / agent-messages / delivery ledger / input polish) + MoA exposed, fabricated free-registry rejected** - `2fc87f9` (13 files, +2611): `GET /api/models/moa` + `POST /api/models/moa/run` (engine redaction, per-candidate authz reuse, MAX_ADVISORS cap, honest `evidence_kind` real/simulated/failed), 7 lead-authored backend tests incl. a root-fix (advisor error text now redacted — a provider error with an API key could previously leak), `ProtocolsSection` + 2 clients + 22 tests; free-model intervention: reverted a fabricated 652-line provider registry (invented quotas/models/retirement dates stamped "verified", provider deletions that broke 3 existing blockrun failover tests, a 404-contract weakening in `get_model_config`) and kept only the honest slice (pre-existing `free_models_sync_tick` loop now registered, real discovery augmentation of `list_models`, HEAD live free-catalog refresh, operator probe script); manifest regenerated by the official generator (58 routers / 7 loops); gates: ruff 0, dup 0, backend 33/33, tsc 0, lib suite 213/213.

79. **P5: the five Module-A spec commands now have real handlers** - `fb7a7fc` (5 files, +741): `/boost` (deterministic thinking-mode derivation preview, no side effects), `/schedule` (real cron job via the cron manager; 5-field cron or exact `every Nm|Nh|Nd`; inexpressible schedules rejected with reasons), `/grill-me` (one real oneshot-LLM turn, model text verbatim, honest empty/failure paths), `/teamwork-preview` (read-only: real roster file + group-run service + swarm coordinator, every unreadable source disclosed), `/self-heal` (diagnosis only: estop state, durable Sentinel journal with verbatim errors, corrupt journal fail-closed, `applied_fix: false`, real repair endpoints named) + 5 core catalog entries + bindings; 23 new tests (75/75 with the intent suite); gates ruff 0 / dup 0 / orphan 6; disclosed: intent-resolver expectations flipped registered=False->True (behaviour otherwise identical) and a platform-incorrect sandbox timeout test was fixed (`4c0658d`, it spawned a nonexistent /bin/sh on Windows).

80. **P3: the dormant evidence + ledger packages now have real consumers** - `0c36d45` (6 files, +412): ESTOP action path writes real intent/receipt pairs (measured timestamps, real outcomes, warning-logged when the ledger is broken instead of silently dropping); Finish-First verifier middleware records the real violation as observation evidence (fail-safe); both packages gain runtime-home default stores; NEW direct `EvidenceStore` suite (disk round trip, validation, ownership, candidate/evaluation/promotion, corrupt-state refusal) + 6 call-site proofs; gates ruff 0 / dup 0 / 69 + 8 passed; lead-found bug: receipt `started_at` sampled before `record_intent` violated the ledger invariant and the broad `except` hid it until the assertion was made strict.

81. **P4: memory clarity (honest degraded reads, canonical tool, documented tree boundary)** - `2adc39b` (7 files): OpenViking `fail_open` no longer returns a bare `""`/`[]` that read as "no memories" — degraded context/search carry an explicit `degraded_memory_unavailable` disclosure with the real exception text (the `raise` policy and healthy paths unchanged, pinned); the previously unreachable `search_session_memory` tool (FTS5+BM25) is now exported and registered (verified live, 129 tools) with the old bash name kept as an alias; both memory trees document their boundary and the no-cross-import rule; 4 new honesty tests reusing the sibling suite doubles; two existing fail-open assertions updated to pin the disclosure (not weakened); gates ruff 0 / dup 0 / 40 passed. Disclosed: `test_durable_context_middleware` fails on this host from pre-existing local-config bleed (`voice.tts.voice=None`), unrelated to P4.

82. **P6 slice 1: evidence + action-ledger REST exposure** - `1ecddae` (3 files, +301): `GET /api/evidence/records|candidates` and `/api/action-ledger/intents|receipts` make the P3-wired surfaces visible; owner_id required on every route (owners never merged), corrupt store fails closed with the real reason, readable-empty answers count 0, invalid filters 422; 6 contract tests, ruff 0 / dup 0. P6 audit finding: the plan's "worktree capability" premise is outdated — `alpha/sandbox/worktrees.py` already exists, so that item is exposure not construction. Remaining P6 slices: kanban<->workflow mirror, forge<->traces, swarm<->KPI (measured-only), worktree exposure.

83. **Full-suite triage cluster 1: the 4 missing harness doc pages** - `1bd5312` (4 files, +319): all 28 `test_middleware_documentation` failures shared one root cause - the four pages the test pins did not exist; the four pages were written to the test's exact contract (it `exec`s the example) with the pipeline order verified against the real `build_middlewares` (built-ins -> SkillToolPolicy -> custom+configured extensions -> TerminalResponse -> ModelLengthFinishReason -> FinishFirst -> UserModel -> Clarification); 34/34 pass (was 6/34). Stage-4 totals: 19348 passed / 150 failed / 10 errors.

84. **Triage clusters 2-7: ~103 of the 150 stage-4 failures fixed at root cause** - 238b32d, c88d05, ca14215, 8af9f37, 8dc79ee, 2b5b2a7 (+ the time-bomb/machine-speed batch): real product bugs found and fixed = unquoted Windows path substitution in LocalSandbox (a path with a space silently split the command), 7 channel commands missing from the reserved slash-skill set (a skill named 	eam would shadow /team), and 68 implemented projects routes default-denied for PAT callers despite their @require_permission guards (verified 0 unguarded before widening the enumerated allowlist); real test-infra bugs = Git Bash present but off PATH (29 failures), hard-coded pre-rebrand brand strings, POSIX mode bits asserted on Windows, a subprocess env that dropped SystemRoot (Winsock WinError 10106), a compose mount parser that split on the :- in ${VAR:-default}, a frozen NOW literal that made a queue-age assertion a time bomb, and fixed sleeps that assumed machine speed; a real doc gap = 4 pinned harness doc pages never written. Disclosed: a concurrent session is mid-flight in the same repo with outers/swarms.py temporarily non-importable, so the lead verified in an isolated worktree at HEAD and left the dirty tree untouched; the endgame is blocked until that tree settles.

85. **Review guard repaired: a safety guard had been silently broken since the rebrand** - 9a92ae5: the comment-density check called _extract_new_content, a method that exists nowhere (the call site survived the agent_workspace->alpha rename, the method did not), so every permitted write was applied and then raised AttributeError while the density policy never ran; also enforce_on_extensions was dead config (density applied to operator-excluded extensions) and enforcement was gated on isinstance(result, ToolMessage) so it vanished for any other return type. Repaired with content read from the tool's own arguments; verified the enabled gate is correctly applied at assembly and left alone. 30/30 tests, ruff 0. LangGraph Studio tests (1F+5E) classified environmental with evidence (pinned langgraph-api 0.10.0 dev server never serves on this host; loopback TCP verified working; reproduces idle and with the repo's documented encoding env).

86. **Triage batch: the remaining named clusters, root-caused one by one** - `d635d06`, `4dd8941`, `5301189`, `e12ad87`, `89e0a7b` (+ `9013372`/`e7fcd9f`): **agent guidance (3)** - two expected-but-absent frontend guides meant the guidance contract silently did not cover the frontend at all (both now real documents, incl. the `Data Flow` section the split-section test pins), three real harness guides were unregistered, and root + backend guides were BOTH over their normalized hard budgets (trimmed 20314/32259 B by removing duplicated or test-pinned narrative; the feature-manifest counts were stale at 116/55/40 vs the generator's actual 127/58/41) -> 12/12. **gateway services (3)** - the start-run tests hand-built a `SimpleNamespace` body instead of `RunCreateRequest`, so every later model field (`autonomous`, then `acceptance_criteria`) raised inside `start_run` before any assertion; bodies are now derived from the real model's defaults with overrides still allowed, so the anti-forgery tests keep sending their deliberately forged payloads -> 159/159. **middleware ordering (1)** - the guard was RIGHT and the test was wrong: patching five different middlewares to one shared `FakeMiddleware` made every stub match the `SandboxAuditMiddleware` invariant (isinstance-based), so the guard read the four stubs legitimately outside the receipt layer as violations; the real chain was verified correct (ToolReceipt 7, SandboxAudit 8, ReadBeforeWrite 9, ToolErrorHandling 10) -> 39/39. **skills mount (1)** - asserted a POSIX `users/noob/...` substring against a real Windows bind-mount source; now parses `src=` and compares `Path`s -> 21/21. **windows launcher (1)** - searched for `& node ...next build`, but start.ps1 runs the node binary it resolved and validated (`& $nodePath`), so the build-failure guard was not being checked at all; now anchors on the `next build` argument, additionally asserts the line is a real invocation -> 9/9. Disclosed pre-existing: `test_gateway_services.py` carried 33 `I001` import-order findings at HEAD (verified by checking the committed blob in-tree under the same ruff config), cleared mechanically in the same commit; `test_client_live_policy::test_ci_unit_test_workflow_runs_duration_aware_shards` is the deliberately deferred `.test_durations` artifact, produced by the final settled-tip full-suite run and never fabricated.

87. **OPEN, root-caused, not papered over: the gateway lifespan gates readiness on a cold import cascade** - `test_lifespan_does_not_wait_for_retrieval_rebuild_before_serving` (3.2-12.6s vs a 1.0s bound) and `test_shutdown_is_bounded_when_channel_stop_hangs` (12.5-14.2s vs 7.0s) remain RED. Measured, not guessed: `faulthandler` + a main-thread stack sampler show the lifespan triggers a large lazy-import cascade INSIDE the readiness path - `alpha/agents/features.py:11` (5.1s), `alpha/models/factory.py:4` (3.5s), `alpha/mcp/session_pool.py:50` (1.6s), `alpha/persistence/engine.py:17`, `app/gateway/routers/__init__.py:1`, `app/gateway/app.py:22` - ~75s of an 80s wall, each import pathologically slow on this Windows host (Defender over a large site-packages). The same scenario with the graph already loaded finishes startup in 0.42s, so the retrieval-warm property itself is sound. Two mechanisms make the bound unreachable rather than merely tight: (a) those imports happen inside `asyncio.to_thread` calls that the lifespan awaits, and `to_thread` work is not cancellable, so `asyncio.wait_for(..., timeout=1.0)` cannot fire until the worker returns; (b) the first-call import cost lands inside the measured window. NOT fixed here, deliberately: a test-side import warm-up was tried and REVERTED because it did not make the tests pass and would have been test-massaging; the product fix is to hoist this import graph off the readiness path (module-scope/`create_app` import, or a non-awaited pre-warm task like the existing `retrieval_warm_task`) and then re-measure. That changes real startup semantics, so it belongs to a settled tree with a full-suite re-run behind it, not to a triage commit.
88. **P6 slice 4 (worktree exposure) is a security design decision, not a wiring task - NOT built on a guess** - `alpha/sandbox/worktrees.py` is not an orphan: `WorktreeManager` (`create_worktree` / `remove_worktree` / `list_worktrees` / `worktree_context`) is consumed by `projects/worktree_hook.py`, `swarm/worker.py`, `rsi/workspace.py`, `safety/self_repo_guard.py`, the benchmark arena, three `projects/*` engines and the company archetypes, and is covered by `tests/test_managed_worktrees.py`. It has no REST surface and no UI, which is what the P6 plan listed as "exposure not construction". Before building it, reading the module showed the reason it was never exposed: the manager is **repo-global and has no owner/user concept at all** (it is constructed from a repo root, not from a request identity), and `remove_worktree(..., delete_branch=True)` deletes a git branch - i.e. the capability is destructive and unscoped. Putting that behind an HTTP route without an owner model and a fail-closed authz decision would reproduce exactly the gap closed in `2b5b2a7` (68 implemented projects routes that were default-denied for token callers), and ~10 internal subsystems already depend on the current synchronous semantics, so a careless change to lifecycle/cleanup would destabilise them. Deferred deliberately with the specific questions recorded: (a) is managed-worktree mutation operator-internal (loopback/CLI only, no route) or per-owner, (b) if per-owner, what is the owner->repo-root mapping and can two owners ever address the same worktree, (c) is `delete_branch` ever permitted over HTTP, and (d) does the UI get read-only `list_worktrees` with create/remove kept internal. No route, client or section was added on speculation.
89. **Concurrent batch frozen, landed, and merged with origin/main - budgets restored, exact committed tree verified** - `a4cead1` + `8a42e0e`: the second session's 219-file in-flight workspace (+28,686/-2,464) was staged only after the quiet monitor reported no writes for 4 minutes, gated by ruff auto-fix of the 11 findings its edits introduced (3 files), a gateway import smoke, and 45/45 targeted tests at snapshot time; no secrets, no >1 MB files, `references/*` excluded (0 staged). The merge pulled origin's 14 new commits: matrix conflict resolved as the union (local row 83 + origin rows 84-88 -> rows 1..88, no dups, no gaps), `backend/AGENTS.md` took origin's compressed budget pointer, and the batch's 11 new contract sections - which blew the hard budgets on root AGENTS.md (20,480 B) and backend AGENTS.md (32,768 B) - were relocated VERBATIM (nothing deleted) into scoped guides with headroom: voice -> `backend/app/gateway/AGENTS.md`, lion companion -> `frontend/AGENTS.md`, swarm v2 + dynamic workflow + auto-update -> `backend/packages/harness/alpha/AGENTS.md`, System One/Laya -> `backend/packages/harness/alpha/models/AGENTS.md`; also restored the `### Host System Monitor` heading the batch's edit had swallowed and compacted the root make lines (root prose -165 B). Budget checker: 0 over on all 29 guidance files (root 20,419/20,480; backend 32,260/32,768; gateway 48,698/49,152). Gate on the EXACT committed tree (fresh worktree @ 8a42e0e): guidance 12/12; contract battery 128/128; health 45/45; frontend tsc exit 0 + node 246/246. Disclosed, not smoothed over: the batch's full-suite status is unverified by the lead; 22 of its post-snapshot edits (plus 2 new untracked paths) stay unstaged for that session to land; the 5 UP022 findings are pre-existing at the origin baseline (left as found); `test_config_version::test_version_26_config_upgrades_to_checkpoint_channel_mode` flaked once under concurrent CPU load and passed standalone and on battery rerun (load-sensitive, root-cause check queued); fresh-checkout `pnpm typecheck` requires the gitignored generated `next-env.d.ts` - pre-existing at origin (same .gitignore rule, same `alpha.png` import, no committed `*.png` declaration), which is also a CI typecheck-gate gap (the workflow typechecks before any typegen/build).

90. **Row 87 CLOSED: the lifespan readiness/shutdown path is stall-free - two measured root causes fixed at source** - `0858b75` (2 files, +196/-4): (1) **psutil's Windows probes hold the GIL for the whole call**, so the monitor's "background" threads froze every thread including the event loop - thread-level spy: `swap_memory` first call 4.638s (warm 0.026s) on `alpha-system-monitor` plus `disk_partitions` 28.6 -> 17.2 -> 31.9s on EVERY call on `alpha-system-monitor-disks` = `startup_elapsed 23.531s`, with `asyncio.wait_for` timeouts firing ~20-25s late (row 87's uncancellable-`to_thread` mechanism was this GIL freeze, not only imports); fixed by pre-warming `swap_memory` at module import (one-time cost) and running the partition enumeration in a CHILD interpreter - the refresh worker now waits on a pipe with the GIL released (370 heartbeat ticks kept running during a 10.9s child probe), a 60s timeout keeps the last-known table, and the in-process call remains only as a fast-failure fallback (fail-open semantics unchanged). (2) **The lazy-import cascade row 87 measured is hoisted off the readiness path**: the lifespan's deferred module graph is pre-warmed at `import app.gateway.app` (module tail, after `app = create_app()`, cycle-safe, per-module fail-open so an unavailable module stays a logged non-fatal at the same call site as before), and the 34-entry capability catalog - 3.190s on first lifespan load - is pre-warmed fail-open at gateway boot while `alpha.capabilities` keeps its lazy contract for library consumers (load now 0.015s). Also the system-monitor stop is bounded to a 0.5s grace (daemon sampler drains on its own) instead of joining an in-flight multi-second collect. Evidence (cold, targeted): `test_gateway_lifespan_shutdown.py` **13/13 passed (107.96s)** - was 3 RED (`does_not_wait` ~20-25s vs a 1.0s bound, `preserves_flush` warm-finished-early calling `manager.close`, `shutdown_is_bounded` 12.5-14.2s vs 7.0s); the 3 previously-red tests 3/3 (95.37s); ruff 0; dup gate 0. Disclosed: the 7.0s bound now runs with ~1.1s of slack (startup still carries ~1s of pre-channel runtime work: config/projection/tiktoken/LangGraph/monitor), the fix commit was verified in the isolated alpha-clean worktree and cherry-picked as `0858b75`, and the row-87 full-suite re-measure (with `--store-durations` for the deferred `.test_durations` artifact) remains queued behind the settled-tip endgame run.

91. **Concurrent batch landed: System-One execution hardening, workflow replay integrity, and the native lion companion** - `60c576f` (37 files, +3,376/-1,302): system_one gained a generation fence, a single half-open probe, a bounded total deadline and fail-closed `meets`; workflow replay deep-copies `_base_graph` so a replay cannot mutate the installed graph, the run-graph install fix stops a broken install being papered over, and exceptions re-raise to the journaling boundary so failed runs journal honestly; browser/jev-policy, orchestrator replay and tool-selection hardening ride with strengthened system-one tests (every test diff reviewed line-by-line - strengthening only; one retargeted laya assertion restored in row 92). The lion companion was extracted into `frontend/src/components/lion-pet/` (state model, CSS, activity adapter, tests) plus a native electron pet window (`electron/lib/lion-pet-window.js`) behind a bounded preload bridge. Gates: ruff 0 (12 changed .py), dup gate 0, tsc 0, pnpm lint 0, pnpm build 0, frontend unit 247/247, electron 5/5, secrets scan 0 hits (new files <=35.7 KB), guidance growth `7f1fe23..526718d` = 0 errors/1 warning (models/AGENTS.md chain 88,798 - warning-only; CI runs without `--strict-warnings`, exit 0 proven). Disclosed: the targeted backend battery ran 460 passed with the 1 failure = the golden closed in row 92 (full-suite re-run stays with the settled-tip endgame run); the batch's full-suite status is otherwise unverified by the lead.

92. **Finish-First evidence recording fixed end-to-end; replay SSE golden re-recorded per the documented key-free protocol; laya state-bound coverage restored** - `c7659e6`: P3 (`0c36d45`) added the `_record_finish_first_evidence(..., had_code_writes=...)` call against a keyword-only `code_writes` parameter, so EVERY code-write-without-verification run raised TypeError and `test_replay_golden` was red from that commit on (the notice branch itself predates P3 - `d5bcec7`; the fixture had never been re-recorded since the initial import). Converted the scan end-to-end to `code_write_count` (the def-side contract `code_writes=1` is pinned by `test_dormant_wiring_p3`). With the crash gone the branch correctly returns its state update, so the replay emits exactly one extra `values` event vs the stale golden: regenerated through the documented `AGENT_WORKSPACE_WRITE_GOLDEN=1` path (`backend/docs/REPLAY_E2E.md` step 3), the fixture diff is EXACTLY that one event (14 added lines, standard state keys, nothing else) and a clean run without the write flag passes twice (~130s each). Assertions untouched; the miss-hash guard still pins prompt/graph drift. Gates: middleware battery 22/22, ruff 0. `526718d` restores `test_laya_abstains_before_http_when_state_exceeds_budget` (original assertions) beside the newer char-bound test.

93. **Wave-1 parallel fixes + the stale branding pin** - `a052388` (frontend/package.json): `typecheck` now runs `next typegen && tsc --noEmit` (new `typegen` script) so a fresh checkout resolves generated route types (W2 evidence: TS2307 exit 1 before / exit 0 after; lint 0; unit tests green; build 0; generated files stay gitignored). Disclosed pre-existing CI gaps surfaced, NOT touched: `lint-check.yml:105` invokes a `pnpm format` script that has never existed (real-runner evidence `gh run view 36097140545` - fails CI before typecheck), `agent-guidance`/`lint-backend` red on main (the guidance checker's 10 absolute-mode chain errors are pre-existing at `7f1fe23` and at `8a42e0e`; the CI gate is growth-relative), `frontend-unit-tests.yml:46` calls `make test` with no `frontend/Makefile` rule. `09f9728` (scripts/config-upgrade.sh + test_config_version.py): the config-version test ran the real script whose `uv run python` bootstrapped a full cold venv inside the fixed 120s deadline (124.9s measured cold vs 9.2s warm, 4.2s provisioned) - the root cause of the load-sensitive one-shot TimeoutExpired; the script honors `CONFIG_UPGRADE_PYTHON` when set (the test passes `sys.executable`, which provably imports yaml at collection) with the default branch byte-for-byte equivalent (RC=0 verified on both branches); assertions and the 120s timeout unchanged. Evidence: 10/10 on four cold runs including under 3-way CPU load, ruff 0, dup 0; an in-main rerun during landing was collection-blocked by the in-flight memory-L1 circular import (that work's owner is actively editing), so W1's own worktree runs are the verification basis pending the post-memory full suite. `51154ac` (frontend/tests/branding.test.mjs): `pnpm test:branding` had been red pre-existing - layout.tsx evolved from the linear `title: branding.name` (initial commit) to the `{ default, template }` object form without updating the pin; the pin now targets `default: branding.name` AND additionally asserts the template suffix consumes `${branding.name}` (strictly more coverage than before) -> 4/4, exit 0.

94. **L1 typed working memory landed end-to-end (TencentDB-Agent-Memory contracts, MIT), with four real bugs found and fixed during central review** - `cc5ac5f` (24 files, +5,286/-6): 14-module `alpha/agents/memory/l1/` (scene segmentation + typed extraction with priority bands incl. the `-1` strict-order sentinel, batch conflict detection store/skip/update/merge with cross-type merge + timestamp unions, per-user record + credit quotas, append-only per-run generation log, retention sweep, persona synthesis, hybrid BM25 + cosine recall, tolerant parsers with CLOSED status sets, corrupt-document preservation) + `L1MemoryMiddleware` (turn capture) + recall injection into the lead agent's `<memory>` block + the `memory.l1` config section in `config.example.yaml` (`config_version` 48 -> 49) + `docs/THIRD_PARTY_MEMORY_NOTICES.md` (per-file upstream map) + `docs/MEMORY_TYPES.md` (canonical type taxonomy x Alpha coverage). Zero-dormant by construction AND by test: the two-level `l1_enabled` gate is checked at registration and again at capture, and `test_every_l1_config_key_has_a_reader` pins that every config key is actually read. Registration is in BOTH assembly paths (`lead_agent/agent.py` + `agents/factory.py`). Bugs the new tests caught and central review fixed at the root: (1) the conflict-prompt builder read `id` while candidate dicts carry `record_id`, so EVERY dedup run with existing records raised KeyError (fixed with a distinct `to_candidate_dict()` shape); (2) missing merge targets now degrade to `store` exactly as the module contract documents - the code contradicted its own docstring; (3) an explicitly supplied `app_config` governed the L1 gate but read the GLOBAL config's store, so embedded/standalone callers silently got an empty block (`get_bound_l1_pipeline()` now binds the caller's config); (4) the new test module referenced `pytest` and `L1MemoryMiddleware` without importing them. Gates: ruff 0 on the memory scope, dup gate 0, env DEAD-SET 0, 113/113 targeted tests green (72 of them new L1 tests). Disclosures: the wider memory battery measured 214 passed / 1 failed BEFORE the final test fix included in this commit (that test is green in the targeted run; the full battery re-runs in the settled-tip endgame pass); `prompt.py` carries 10 E501 findings that exist identically in the HEAD blob (verified) and were left as found because rewording system-prompt prose is a behavior change outside this commit; the generated `contracts/feature_manifest.json` was deliberately NOT staged - the working-tree regen attaches the L1 middleware to a `tool_error_handling_middleware:build_lead_runtime_middlewares` wiring point that does not exist in that file, and manifest edits are official-regen-only (P7 will own it); `config.example.yaml` was committed by index surgery carrying ONLY the version bump + the `l1:` block, so the concurrent session's computer-action hunk stays theirs (their schema change will need `config_version` 50).

95. **Memory-type research complete; the six missing types are being built as separate, disjoint packages** - Internet research (CoALA arXiv:2309.02427; "Memory in the Age of AI Agents" arXiv:2512.13564; the 2026 framework surveys) converges on working / episodic / semantic / procedural as the core taxonomy, with prospective, spatial, affective, autobiographical, entity-graph, shared-collective, scene-contextual, directive and reflective as the established extensions. Measured against this repository, Alpha ALREADY has eight types fully wired (working scratchpad, flat + hierarchical episodic, SPO semantic-with-beliefs, procedural with success/failure statistics, spatio-temporal with interval validity, Hebbian associative, sleep consolidation + Ebbinghaus decay, hybrid BM25 + vector + graph + RRF retrieval) plus the new L1 layer. The honest gaps - prospective, affective, narrative, entity-linked, social/shared, scenario-conditioned recall - are recorded in `docs/MEMORY_TYPES.md` rows 14-19 and dispatched as four parallel subagents (prospective, affective, narrative, entities), each owning exactly ONE new package (`alpha/memory/<type>/`) with its own config, gates and hermetic tests and NO shared-file edits; the central agent applies the lifecycle/recall wiring itself so nothing collides with the concurrent System One session and nothing lands dormant. Licenses were verified BEFORE any reuse was considered (`gh api repos/<owner>/<repo>/license`): mem0 Apache-2.0, Letta Apache-2.0, Graphiti Apache-2.0, LangMem MIT, Cognee Apache-2.0, MemOS Apache-2.0, Memobase Apache-2.0, basic-memory **AGPL-3.0 (copyleft - ideas only, never copied)**. No code from any of those projects is in the tree; the only ported source remains TencentDB-Agent-Memory (MIT).
96. **Config plane closed: all ten wave-2 memory subsystems are now reachable from the host schema, with a drift guard that makes silent divergence impossible** - `5fe514b` (5 files, +585/-9): the ten new packages each shipped a default-OFF config, but `MemoryConfig` exposed only `l1`, so no operator could enable a type without editing code - every new subsystem was dormant by construction. All ten (`affective`, `entities`, `evaluation`, `fabric`, `fusion`, `narrative`, `policy`, `prospective`, `scenarios`, `social`) are promoted as first-class `memory.<type>` sections, and the `config.example.yaml` blocks are GENERATED from the pydantic models rather than transcribed (the first hand-generated pass silently flipped the shipped `l1.enabled: true` to the schema default `false`; the generator now carries the one sanctioned product override explicitly, so a fresh install keeps typed working memory live). This was only safe because of the landed lazy-facade conversion: a subprocess test pins the import direction - the shared schema may import a subsystem CONFIG but must never drag that subsystem's engine/store/runtime in, and all ten roots resolve through the shared PEP 562 helper. The new `test_memory_config_promotion.py` (36 cases) is the standing guard: every section exists, defaults OFF, survives `load_memory_config_from_dict`, and is present in `_SHARED_FIELDS` (a missing entry there would have migrated the section into `backend_config` on load - the exact silent-swallow failure the L1 row documented); `config.example.yaml` must equal the schema defaults key for key against a non-extensible one-entry override list, and any *other* `enabled: true` in the shipped example fails the test, so a subsystem cannot be switched on for every operator by accident. `EvaluationConfig` is a frozen stdlib dataclass validating in `__post_init__`; the guard reads it via `dataclasses.fields` while pydantic still enforces its ranges through the promoted field (verified: an out-of-range threshold is rejected with `ValidationError`). Gates: ruff 0, promotion 36/36, `test_config_version` 11/11, `test_memory_config_l1` 4/4, repo duplicate-expression gate 0, L1 prompt-injection 9/9 (the foreign `peer_network/service.py` parse error that broke them was fixed by its owner, not by this line). `config.example.yaml` committed by index surgery carrying ONLY `config_version` 49->50 plus the generated sections, so the concurrent computer-action and agent-eye hunks stay uncommitted and theirs. Measured, disclosed: the shared schema import is 21.3s cold on this Defender-scanned host with the ten sections vs a 15.0s pydantic+L1-paths baseline; it is paid once at gateway boot behind the row-90 pre-warm, not on the readiness path. Docs corrected to the project's own rule: `docs/MEMORY_TYPES.md` now says BUILT-but-not-yet-wired for the six wave types (never "HAVE" before a lifecycle path exists) and adds the cross-cutting substrate table (policy/fabric/fusion/evaluation), and `docs/THIRD_PARTY_MEMORY_NOTICES.md` records that all ten packages are original Alpha code with design lineage cited and zero third-party source copied - the LongMemEval-shaped cases in the evaluation harness are original, with no LoCoMo/LongMemEval/MemBench dataset bundled.
97. **Memory wave 2 landed: ten packages (ten commits), the config plane, the recall seam, and a real test race root-caused** - `903cfe8`, `a0597b1`, `3f36493`, `aa41406`, `52d5163`, `d97921d`, `dc36946`, `0da8a62`, `ae05b15`, `03083c4` (one commit per memory type, each ruff-gated before the commit), plus `538a314` (test fix), `9084f32` (recall seam), `26e2532` + `ce502c4` (docs index), config plane in row 96. **Packages**: prospective (18 tests), affective (35), entities (25), social (22), narrative (22), policy (55), fusion (40), scenarios (28), fabric (24), evaluation (28) = 297 hermetic cases, re-run together in one combined battery (404 passed / 1 failed -> the failure below was root-caused and fixed, then the affected file re-run green). Every package is default-OFF, owns its storage, imports no sibling memory package, and exposes its config through the shared lazy-facade mechanism; every one ships a reader test proving each config key is actually read. **Real test bug found and fixed, not retried** (`538a314`): `test_pipeline_capture_is_gated_and_flush_runs_synchronously` failed once in the combined battery with `flush() == 0` instead of 1. Root cause: the suite's `make_config` sets `debounce_seconds: 0.0`, so `capture()` arms `threading.Timer(0.0, _drain)` and the timer thread RACES the test's own `flush()`; on an idle machine flush wins, under load the timer wins and correctly drains the job first. The product was never wrong - `flush()` returning 0 with nothing pending is right - and the debounce path it accidentally covered had no test at all. The sync test now uses a 30s debounce (same assertions, deterministic, not weakened) and a new `test_pipeline_debounce_timer_drains_without_flush` pins the previously untested timer path with a bounded deadline poll instead of a fixed sleep; 3/3 consecutive green runs under the same parallel load that produced the failure. **The recall seam** (`9084f32`) is the piece that makes the whole wave safe: `alpha/memory/recall_composition.py` renders every ENABLED surface and the lead prompt gained exactly ONE guarded call site, so adding a type is no longer a patch to a contested shared file. Its four load-bearing properties are each pinned: default-OFF is invisible (types off => `<memory>` byte-identical, no heading, no notice), the two-level gate holds (a type's own flag cannot outrank `memory.enabled`; `injection_enabled: false` still returns nothing), one broken type cannot break the turn (failure recorded with the exception TYPE, other surfaces still render, no replacement text invented - verified end-to-end through the real `_get_memory_context` with every renderer raising), and the composed block is bounded with truncation DISCLOSED rather than silent. L1 is deliberately excluded from the composed set: its prompt path is already landed, gated and tested with its own bound-pipeline resolution, and folding it in would double-render a verified seam. Gates: recall composition 9/9, prompt wiring 6/6, and with the existing lead-prompt / memory-injection / config suites 120 passed; `prompt.py` still carries exactly its 10 pre-existing E501 findings (unchanged count, none in added lines). **Docs index** (`26e2532`, cherry-picked from an agent worktree and verified on the real tree): a deterministic official generator plus a read-only JSON drift gate, 40 documents, 0 drift against a clean checkout of HEAD. Its own test had a real design flaw caught at landing: it compared the generator's output for the WORKING DIRECTORY against the committed index, so an unrelated untracked document from a concurrent session made it red (observed live - the generator correctly exited 2 with "unclassified document"); the test now materialises the git-TRACKED docs and compares those (`ce502c4`), preserving exactness while being immune to in-flight work. **Disclosed / deferred**: (a) the CI drift-gate branch (`agent/ci-drift-gates`, 8 commits) is verified green on its own tree - its gate exits 0 and its committed `contracts/feature_manifest.json` is byte-identical to its own generator's output, so the manifest is generator-produced, not hand-edited - but it CANNOT be cherry-picked yet because a concurrent session has an uncommitted, legitimately regenerated manifest in the working tree and that path must not be touched; it lands the moment that owner commits. (b) Capture/ingestion wiring, the policy->fabric admission chain, fusion and scenario routing are still NOT wired, and `docs/MEMORY_TYPES.md` continues to say so - this row closes the RECALL half of the wave only. (c) All eleven stores remain process-local with no cross-process coordination and no on-disk format version/migration path; a dedicated store-kit + migration agent is building that substrate now, with adoption delivered as per-store patches rather than a big-bang rewrite.
