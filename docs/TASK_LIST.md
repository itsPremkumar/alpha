# Alpha — Master Task List

> Living document, maintained by the main agent and updated as work lands.
> Nothing is marked done without evidence (commit SHA, test log tail, or
> subagent session report). New implementation plans are appended here as
> they are created.

**Last updated:** 2026-09-24 — session `ses_f364c593bffemBW1Ja8InOsVw9`
**Legend:** `[x]` done+verified · `[~]` in progress (owner named) · `[ ]` pending/blocked (blocker named)

---

## 1. Committed & pushed this wave (centralized review → staged commits)

- [x] `e22fb85` — fix(subagents): stage-4c honest delegation contracts (no fabricated offline success; 68 targeted tests)
- [x] `94c4e61` — feat(evolution): repository-awareness phase 1 (manifest loader, identity.json agentId, GitHub release check state machine, JSONL ledger, `{project_identity_section}` in lead prompt, +3 evolution routes, SettingsSection Workspace Profile card; 46 tests)
- [x] `b7e8f3e` — fix(governance): disclosed heuristic confidences (votes carry confidence_method/note, unverified→0.5 neutral, tool stops fabricating test evidence, frontend seeds removed; 22 tests)
- [x] `cac8d0d` — feat(models): keyless free-LLM router (tri-state health, cooldown failover, ChatFreeLLM; 26/26 + regression quartet 160 green)
- [x] `b121ff9` — feat(skills): evolution engine with honest evaluation and gated promotion (15/15 + central re-run)
- [x] `4fa9a79` — feat(skills): scored lexical retrieval + skill relationship graph (21/21 + regressions 47+64+4)
- [x] `f96dad6` — feat(budgets): hierarchical scoped token budgets + HITL workflow gates (35/35 central re-run)
- [x] `ecc4f1a` — feat(experience): FACT/TIP experience bank with evidence gate and hygiene (15/15 central re-run)
- [x] pushed to `origin/main`: `f1c7670..ecc4f1a` (8 commits, in sync); matrix entries **#15–#22** + this file land in the following docs commit and are pushed immediately after.
- [x] `d4b7af5` — fix(honesty): neutral 0.5 confidence defaults + GateRequest autonomy opt-in (6 files; central re-run 85 passed, ruff 0, dup 0)
- [x] `eb778c7` — feat(rsi): code-constant protected-path policy with fail-closed review gate (WP-B3; central re-run 15 passed, ruff 0, dup 0)
- [x] `7ce9060` — feat(frontend): agent-status panel with honest loading/error states + theme system (central re-run: tsc exit 0, node:test 62/62)
- [x] `8b96b4d` — feat(rsi): cycle budget controller with measured-only accounting + additive state key (WP-D2; central re-run 51 passed, ruff 0, dup 0)
- [x] `950716a` — feat(rsi): evidence-backed candidate factory with lineage + honest dedup (WP-B2; central re-run 24 passed, ruff 0, dup 0)
- [x] `0ed64ce` — feat(rsi): isolated candidate workspaces with mechanical main-branch guard (WP-B1; central re-run 64 passed, ruff 0, dup 0)
- [x] `3844da7` — feat(ops): event-loop liveness sampler, bounded metrics registry, readiness detail, queue health (OpenClaw port; central re-run 54 passed incl. no_orphan, ruff 0, dup 0)
- [x] `fa328d8` — feat(tools): keyless web + deep search ported from AgentEye (honest ok/no_results/failed, 9 offline tests; `contracts/feature_manifest.json` intentionally still uncommitted — held for the batch landing its remaining referents: multimodal + workflows routers)
- [x] `d561b8a` — feat(rsi): package-level exports for wave-1 modules (archive, opportunity, state, strategy_memory; keeps fresh-checkout orphan gate consistent)
- [x] `49a561a` — docs: wave-2/3 landing rows, DY remediation + mapper status, fix-log entries 23-34
- [x] `153bb0f` — feat(hermes): S1/M1/M2/E1 ports — skill-creation nudge, session-search safety bounds, turn-counter persistence nudge, skill-usage evidence bridge (9 files +1103; central gates 38 passed + 13 wiring, ruff 0, dup 0, orphan 6)
- [x] `7ed591f` — feat(workflow): dynamic workflow honesty kernel + patch layer + bot clone/evolve + workflows router (32 files +6257; DY-R1/R2/R3 workstream: 7 fabrication sites fail honestly when unbound, adjudicated router test with honest-unbound REST pin + HITL roundtrip, StrEnum/lint hygiene, templates re-export root-cause fix; 58 passed, ruff 0, dup 0, orphan 6)
- [x] `3a415e1` — feat(rsi): C2b human review gate (pending-only records, no-self-approval AST pin, verbatim reviewer provenance, fail-closed decisions; 25 passed + 42 dependency parity, ruff 0, dup 0)
- [x] `0175150` — feat(rsi): C1 shadow evaluation harness (three-state comparison, fail-closed fixture errors, content-addressed atomic records; 74 passed, ruff 0, dup 0)
- [x] `a7364de` — feat(rsi): C2a promotion evidence bundle (verbatim nine-file provenance + sha256 index + exactly-one ledger event; 80 passed, ruff 0, dup 0, orphan 6 with new allowlist entry)
- [x] `44abec8` — test(scheduler): queue-health honesty suite (payload redaction, honest cap flags, dual verdicts, route order, auth; 24 passed, ruff 0, dup 0)
- [x] `ff88366` — fix(enterprise,evolution): fabricated telemetry defaults removed (None fallbacks + nullable models + frontend "No scan"/"No active release"), retrospective wired to real postmortem API + WARNING disclosure, dead heartbeat locals removed, thread_id sweep root-cause fixed (agent_messages); 86 passed + 15 re-run, ruff 0, dup 0, tsc 0, 62/62 lib tests
- [x] `013e795` — fix(bots): evolve_bot no longer inflates reputation — inherited score earned only via observed outcomes (22 passed, ruff 0, dup 0; matrix #42)
- [x] `59a1d9e` — fix(frontend): SettingsSection probe failure renders honest unavailable state + theme cards aligned with the theme system's system-preference default (tsc 0, 83/83 lib tests; matrix #43)
- [x] `0b5bfcf` — feat(rsi): C2c promotion decision composer — closed 6-gate set with verbatim gate reasons, not-routed disclosure, real `EvolutionEngine.gate` wiring with `autonomous_mode=False` pin; central fix-at-landing no-empty-reason guard + pin (114 passed incl. orphan/parity, ruff 0, dup 0; matrix #44; duplicate-dispatch strays discarded, disclosed in §2)
- [x] `564f004` — feat(workflows): P1 execution kernel + DY-R4 7-paradigm mode integration — ExecutionKernel (per-run claim, fail-closed over failed nodes), ExecutorRegistry (honest unbound refusal preserved, recomputable digest evidence), mode-mapper (AGENT/BOT kind per mode; swarm honest not-expressible), replay fold; new `POST /turns` + `POST /runs/{id}/replay`; unbound fail-closed pin KEPT (78 passed, ruff 0, dup 0; matrix #45; duplicate-dispatch reconciliation + 9 runtime.py bugs disclosed in §2/§9)
- [x] `5155124` — feat(voice): mic/speaker/wake-word wave completion — wakeword ONNX/download/np.float32 fixes, rapidocr raw-bytes fix, STT `language` field + transcript engine+tier provenance, observed-only Voice & Speakers UI with autoplay disclaimer, `voice:` config block in example+live configs (103+6 passed, ruff 0, dup 0, tsc 0, 83/83 lib tests; matrix #46)
- [x] `4fbd133` — feat(gateway): workflows router mounted in `app.py` + `app.gateway.routers.workflows` orphan allowlist entry removed root-cause + official manifest regen (both new routers listed, tools 119→121); orphan re-run AFTER entry removal: 6 passed (matrix #47)
- [x] `b21d62b` — fix(honesty): audit batch A — daemon honest zero-progress, discovery latency profiles honest-empty, council holdout fixed at the router (score + `evidence_kind=simulated` verbatim), ROI floor deleted, arena canned runs + zero-attempt leaderboard defaults → honest disclosure, recovery HOT_REPLACE claim dropped (central 54+1 passed, ruff 0, dup 0)
- [x] `de6f1ce` — fix(honesty): audit batch B1 — fabricated CI receipts → HYPOTHESIS/neutral, zero-run success/reputation → `None`+disclosure, duplicate `POST /{name}/clone` disambiguated to `/{name}/clone-engine` with dual-route HTTP proof + frontend call-target verification (central 136 passed, ruff 0, dup 0)
- [x] `da5c8d2` — fix(docs+honesty): docs-truth batch — unmeasured sub-50ms halt claim dropped from `docs/SECURITY.md`, MessageItem NaN guard, SettingsSection `"system"` default truth (3 files, +8/−4)
- [x] `0bb2871` — fix(workflow): root-cause all 11 §9 `runtime.py` engine gaps + replay/loop alignment — fail-closed with COMPENSATING deferral, real-node stagnation recovery, journaled `_set_run_status`, deepcopy event snapshots, quiet-sink patched-graph replay, approval `node_status`/denial folds, `node_iteration` honesty; new `test_runtime_fail_closed.py` (8) + `test_runtime_event_integrity.py` (7) (central 129 passed, ruff 0, dup 0; 11 files, +1106/−153)
- [x] `2385c41` — fix(honesty): wave-2 audit S1-C — frontend fetch-failure disclosure: per-section ErrorBox/retry flags replacing catch-to-empty, `api.ts` `FetchResult<T>` contract with LEGACY-COMPAT wrappers fenced to ChatView, root-cause lib de-swallow (comm/inbox/teamops/threads-ext reject instead of returning `[]`), WorkspaceVitals probe three-state; new `load-failure-honesty.test.mjs` (20 tests) (central tsc 0, node 110/110)
- [x] `656a8ac` — feat(voice): TTS autoplay consumer — default-OFF `autoplaySpeak` gate (speak impl never invoked without explicit opt-in), honest `speak()` (real synthesis/playback errors, SPEAKING state marked without hijacking armed sessions, object URL always revoked), failures disclosed via `onFailure`; default-OFF pins in voice + chat-request harnesses (central tsc 0, node 110/110)
- [x] `991b7ad` — fix(honesty): residual batch R1/R3/R4 — container host-fallback PATH parity (`sys.executable` dir prepended before `shell=True`; docker branch untouched), fleet reputation null-honesty (measured-only averages, `number | null` types, "unverified"/"none measured" renders instead of fabricated 1.00/0.00), roster `None%`/`Reputation: None`/zero-run `0.0s`/`@None` leaks fixed (central 36 passed, ruff 0, dup 0, tsc 0, node 110/110; R2's system-monitor hunks held with B2 — shared files)
- [x] `c86b660` — fix(honesty): wave-2 audit S1-A — `/security-review` 6 real in-process probes + fail-closed `_derive_secure` + "Not verified in this run (no verdict claimed)" for the 2 networked checks, fabricated `privilege_ring` removed; `/skill:test` real frontmatter validator + `scan_skill_dir` + root-cause `tags:` template fix; `/doctor` real probes + `_derive_doctor_status` (unknown never counts); `/compact` honest not-implemented naming the real API seam; 4 constant-True pins → derivation/negative pins (central 13 passed, ruff 0, dup 0)
- [x] `0448a50` — feat(orchestration): Master Mission Module A — intent/goal perception engine + durable goal decomposer (tri-state `registered` honesty against the real command catalog, honest GENERAL fallback on tie/no-signal, fail-closed corrupt-goal store, evidence-gated COMPLETED, single `ALLOWED_TRANSITIONS` enforcement point; 4 files +1760; central 52 passed, ruff 0, dup 0; shared `capabilities/catalog.py` held for the final registrations commit — fresh-checkout orphan evidence rides the absolute imports in `orchestration/__init__.py`)
- [x] `0dcae51` — feat(system1): Master Mission Module B — System-1 reflex harness (local free CPU classifier; choice/score/noul decisions; tool pruning + loop-termination seams; 6 files +1686; central 25 passed, ruff 0, dup 0)
- [x] `0c96673` — feat(computer-use): Master Mission Module C — accessibility-tree grounding, sentinel-guarded input dispatch, screenshots, 5 builtin tools (7 files +2680; central 46 passed, tool schemas 124/124, ruff 0, dup 0; fakes-only tests, no live OS access claimed)
- [x] `f882542` — fix(honesty): degraded memory reads stop fabricating `<memory>` context (cognitive enrichment only when the backend returned content; silent `except: pass` now logs) + non-cancelling timeout timebox (a queued `to_thread` worker is never discarded; late outcomes disclosed) + 3 test-pin corrections incl. a disclosed inversion (77 passed, ruff 0 on modified files, dup 0; prompt.py's 10 pre-existing E501s disclosed)
- [x] `a905d97` — fix(bots): fail-closed ephemeral-lease store (corrupt store and failed persists now RAISE; unparseable TTL = expired with `invalid_expiry`) + audit-before-activation skill publishing (static scan before any file lands; blocked → 422) + D1 proofs for all 3 clone modes, MCP mount, skill round-trip (6 files +750; central 11 passed, ruff 0, dup 0)
- [x] `0de236d` — fix(honesty): AVO/meta-benchmark bodies lose their measured-value defaults (`POST {}` → 422, nothing committed; caller values flow through) + bootstrap beliefs become 0.5–0.7 sourced assumptions with empty evidence ledgers and no `pinned` tag (6 files +343; central 26 passed incl. projects-router regression, dup 0; 3 pre-existing UP042 disclosed)
- [x] `650437e` — feat(workflow): W-N1 durable state — append-only JSONL event log (fsync'd, idempotency keys, corrupt-tail disclosure + append refusal), run projections, graph-revision store with optimistic concurrency, kill-and-resume hydration, additive REST surface (`/system/durability`, `/runs/{id}/events/durable`, `/runs/{id}/project`, `/hydrate`, `/{wf}/plans`), dispatcher sink seam + 3 silent handlers made visible (8 files +1515; central 37 passed incl. the P1 kernel router suite, ruff 0, dup 0)
- [x] `ba6fac3` — fix(honesty): `kill_score`/oracle `confidence`/benchmark scores become nullable when unmeasured; the AST syntax guard fails closed on empty JSON and missing PyYAML; the two fabricated tools (`run_mutation_testing_audit`, `run_speculative_synthesis_tournament`) deleted; the SDLC docstring's unmeasured "Sub-50ms" SLO removed (9 files +246/−84; central 28 + 7 passed, dup 0, 0 new ruff)
- [x] `46dbca8` — fix(honesty): fabricated numbers stop being served as measurements — invented per-cycle token burns that drove treasury circuit breakers removed (measured-only + `token_burn_measurement` disclosure), bootstrapped attendance measured from the real ledger (`attendance_measured` marker), KPI seeds labelled `seed_demo_data` with a deep-copy aliasing fix, heartbeat `999999.0` sentinel → `None` + parse-error flag, health percent no longer seeded; UI mirrors all of it (AVO button fails closed, benchmark baseline explicit, `number | null` latency) (20 files +335/−74; central 71 passed then 53 re-measured after the lead's additions, tsc 0, node 110/110, ruff 0, dup 0)
- [x] `85b7629` — feat(workflow): System-1 reflex hook in the loop path — advisory proposal acted on only with deterministic evidence agreement (`stopped_by="system1_reflex_agreement"`), every consideration journaled, layer errors disclosed and non-fatal (2 files +256; central 18 passed incl. workflow router + persistence, ruff 0, dup 0)
- [x] `883b44d` — feat(capabilities): the four held Master-Mission registrations land together with the officially regenerated manifest (tools 121→127, routers 57) (2 files; orphan gate 6 passed, wiring 13 passed)
- [x] `23ce45f` — fix(honesty): batch B2 + R2 residuals — load-avg no longer served as `cpu_percent`, degenerate acceptance criteria fail with a reason and block a pass, suggestion-generation failures are 502 (a genuine empty result is still 200), Brier `None` on empty history with `dropped_records`, a zero-criteria rubric abstains (WARNING) instead of approving, benchmark scores nullable, system-monitor AttributeError → null + degraded, per-widget unavailable/retry in the UI (15 files +658/−127; central 164 passed, tsc 0, node 110/110, ruff 0, dup 0)
- [x] `0d673c7` — fix(tools): Module C's five desktop tools registered (they were committed but unreachable — an unwired feature by omission), `verify_web_ui_visual_regression` exported, the two dormant-engine orphan-allowlist entries landed (a fresh checkout would otherwise fail that gate), and the audit wave's two remaining test pins land incl. a disclosed oracle inversion (5 files +91; central 15 passed, tool schemas 124/124, orphan 6, wiring 13, dup 0; 6 pre-existing ruff findings disclosed)
- [x] `06d7a08` — feat(chat): free-model catalog in chat (honest client: `healthy: null` and `eligibilityKnown` disclosed), in-chat project scope, TTS autoplay call site (default-OFF gate, visible failure notice) + README/FAQ/llms.txt refresh with the manifest count corrected 121→127 (7 files +428; tsc 0, node 110/110)
- [x] `4179e34` — fix(honesty): the retrospective stops inventing performance (measured-only counts, no invented playbooks or "calibrated" bots, KPI summary says "not yet measured") and `discover-work` stops inventing findings when no signals are supplied (3 files +55/−27; central 21 passed, dup 0; disclosed test inversion of the `>= 2 invented playbooks` pin)
- [x] all of the above pushed to `origin/main` (in sync through `4179e34`)

## 2. In flight right now (parallel)

- [x] **pre-memory secret filter** — DONE, centrally verified + committed `5a1f401` (34/34 central re-run, ruff 0, dup 0; orphan gate no longer flags `memory_redaction` — the remaining flag `alpha.rsi.state` belongs to RSI A4's pending stage-persistence wiring)
- [x] **Voice & multimodal feature** subagent `ses_f31c2a451ffemUDOb17zjh4oPI` — **REPORT RECEIVED, central review DONE (honest), LANDED `5155124`** (phases P1–P6 of `references/ALPHA_VOICE_MULTIMODAL_PLAN.md`; central gates 103+6 passed / ruff 0 / dup 0 / tsc 0 / 83/83; `voice:` config block in example+live configs + gateway batch `4fbd133` followed; live HTTP/WS smokes deferred to the gateway-restart window — see §6)
- [x] **RSI Wave-3 C2c (wave's last unit)** `ses_f2fce5adfffeGOfKvPTNs3sjy0` — **REPORT RECEIVED, central line-by-line review DONE (honest), LANDED `0b5bfcf`**: `rsi/promotion.py` composer (closed 6-gate set, no short-circuit, verbatim gate reasons, explicit `not routed:` disclosure, `autonomous_mode=False` pin on the real `EvolutionEngine.gate` via the landed `GateRequest` contract) + `evolution/promotion_route.py` + 16 tests; central fix-at-landing: no-empty-reason invariant extended to the routing entry + pin test; central gates **114 passed** (16 C2c + orphan 6 + bundle/review/identity/confidence parity), ruff 0, dup 0 · **DISCLOSED DUPLICATE DISPATCH (main-agent retry error)**: a second C2c agent `ses_f2fd9e9adffeK1PZzALJkSmpg1` also reported (25 tests + `promotion_dispatch.py` + `evolution/__init__` re-export); never centrally reviewed → its strays discarded uncommitted (`__init__` reverted, `promotion_dispatch.py` deleted) — the reviewed implementation is the committed one
- [x] **Dynamic Workflow P1 kernel + DY-R4 mode integration** subagent `ses_f2fce5ac9ffeiH9p0ZT2hYLqwp` — **REPORT RECEIVED, central line-by-line review DONE (honest), LANDED `564f004`**: `orchestrator/{loop,executors,mode_mapper,replay}.py` kernel composing the real DWE through its public API only (per-run claim serializing dispatch/patch/approval, fail-closed over failed nodes with an honest `workflow_failed` naming the ids, executor registry preserving the engine's exact unbound refusal with recomputable `alpha.local.digest` evidence, 7-paradigm mode-mapper with AGENT-vs-BOT kind per mode + explicit honest swarm not-expressible, event-log replay fold with zero re-emission), REST routed through the kernel + new `POST /api/workflows/turns` + `POST /runs/{id}/replay`; unbound fail-closed pin KEPT; central gates **78 passed**, ruff 0, dup 0 · **DISCLOSED DUPLICATE DISPATCH (same root cause as C2c)**: an earlier P1 session `ses_f2fd9e99bffetz3BZANQ2xrG8B` also reported; the surviving session adopted its files verified-by-mtime with no clobbering (reconciliation disclosed in its report) — one implementation committed · 9 `runtime.py` bugs reported (not edited) → §9 open row · gateway mount + orphan-entry removal landed `4fbd133`
- [x] **RSI Wave-4 (un-inventoried spec features — Wave-3 scope decision: implement all)** subagent `ses_f2fa40239ffeUmmkJY2iTdni8H` — **REPORT RECEIVED, central line-by-line review DONE (honest), LANDED `13c446e`**: NEW `rsi/cooldown.py` (§42 exact four-field store, injected clock, real boundary arithmetic, corrupt→fail-closed bytes untouched, env window validated not defaulted) + NEW `rsi/releases.py` (§26/§43 create-once immutable `vNNN/` dirs, release-first pointer-flipped-last, ids from on-disk max+1, `commit: "unknown"` disclosed no-git, artifacts = verified bundle sha256s, real memoized `build_manifest()`, broken pointer fail-closed) + NEW `rsi/errors.py` (§54 literal E001–E018 + designated-disclosed E000, real-signal classification incl. genuine `StaticScanBlockedError`→E008, provider outage→E001 never candidate-defect, `None` only for the derived `not routed:`) + gate-append to `rsi/promotion.py` (`cooldown` before `evolution_route` so a cooling candidate is never routed; release→cooldown materialization with disclosed `release_store`/`cooldown_record` failure entries flipping `promoted=False`; §54 codes on composed reason + logs ONLY, gate entries byte-for-byte verbatim) + one-line `EXPECTED_GATES` literal + NEW `test_rsi_wave4.py` (14 tests, literal field/manifest/taxonomy pins, binding-identity checks, byte-for-byte reason rebuild); central gates **40 passed / ruff 0 / dup 0 / orphan 6 (no allowlist edit)**; 6 disclosed deviations adjudicated acceptable (post-routing bookkeeping entries outside the closed `GATE_ORDER` by design; engine-ledger divergence disclosed not hidden; E000 = build designation never attributed to spec); two latent kwarg bugs caught+fixed pre-suite → **WAVE 4 LANDED; RSI §8-ADDENDUM WAVE-4 SCOPE CLOSED**
- [x] **Fresh read-only honesty audit** `ses_f2f34f1feffe43owT8JoZNO5DM` (explore) — **REPORT RECEIVED: 22 NEW findings** (13 fabricated-number · 2 swallowed-exception · 3 silent-success · 1 unwired route · 2 fail-open · 1 frontend honesty · **0 hardcoded secrets**); auth bypass/CSRF/PAT/readiness verified fail-closed (bypass env-gated, refused under `ENVIRONMENT=prod`, loud warning); coverage = read-traversal of alpha core subpackages + all 59 routers + gateway core (broken grep/glob → full-read method, unread residue disclosed by the agent); all 8 HIGH findings central line-by-line VERIFIED before dispatch; KPI-seed finding folded into the fabricated-number fixer already running
- [x] **Fabricated-number fixer (queued hits)** `ses_f2f2d517cffeWr7A5Ccge3Iw52` — session lost with no report; the lead reviewed all 20 files' diffs line-by-line, **closed the two root causes it never reached** (invented per-cycle token burns feeding treasury circuit breakers; fabricated 3-sleeping-bot attendance contradicting the ledger) and **LANDED `46dbca8`** (71 passed, then 53 re-measured after those additions; tsc 0, node 110/110; ruff 0, dup 0). Covered: telemetry defaults → None, bootstrap health/model defaults nullable, KPI seeds → `basis="seed_demo_data"` flipping to `measured` (+ a real `KPIEngine` deep-copy aliasing fix), `999999.0` heartbeat sentinel → None + parse-error flag, UP047 → PEP 695, and the UI mirrors (AVO button fails closed, benchmark baseline explicit, `number | null` latency)
- [x] **Runtime engine-gap batch** `ses_f2f751076ffeX3m1jqOpqf4cJh` — the 11 §9 `runtime.py` gaps (+ minimal `orchestrator/{loop,replay}.py`) — **REPORT RECEIVED + central line-by-line review DONE (honest; 0 inversions) → LANDED `0bb2871`** — all 11 gaps root-cause-fixed (fail-closed COMPENSATING deferral, real-node stagnation recovery, journaled `_set_run_status`, deepcopy snapshots, quiet-sink patched-graph replay, approval `node_status`/denial folds, `node_iteration` honesty); gap 11 fixed not deferred; six disclosure pins inverted to the fixed behavior; central gates **129 passed / ruff 0 / dup 0** (11 files +1106/−153; new `test_runtime_fail_closed.py` + `test_runtime_event_integrity.py`)
- [x] **Audit batch A (enterprise/daemon/arena — HIGHs)** `ses_f2f20b87bfferXra2PAiSyoVMp` — daemon silent zero-work task completion; discovery hardcoded latency profiles served as telemetry; council `96.4` constant → HTTP `holdout_passed=true` lie; governance ROI floor `<0.1 → 0.85`; arena canned `simulated_pass` runs + zero-attempt `90/12/85` leaderboard defaults; recovery HOT_REPLACE false success message; owns perpetual + `enterprise/{discovery,council,governance}` + `benchmarks/arena` + `supervision/recovery` + `{enterprise,benchmarks}` routers + their tests (fenced from `test_enterprise_autonomous_software_company.py` → central fallback pins) — **REPORT RECEIVED + central review DONE → LANDED `b21d62b`** (central 54+1 passed, ruff 0, dup 0)
- [x] **Audit batch B1 (epistemics/bots)** `ses_f2f20b86cffesgE7wbHg82N9LE` — fabricated CI receipts seeded FACT@0.98 → HYPOTHESIS/neutral, evidence strings deleted; zero-run bots `success_rate=100.0`/reputation `1.0` → `None`+disclosure; duplicate `POST /{name}/clone` (`bots.py:398` vs `:812`) shadowing the clone-engine → disambiguate to `/{name}/clone-engine` + dual-route HTTP proof + frontend call-target verification; owns `epistemics/engine` + `bots/performance` + `routers/bots` + tests — **REPORT RECEIVED + central review DONE (zero inversions) → LANDED `de6f1ce`** (central 136 passed, ruff 0, dup 0)
- [x] **Audit batch B2 (low-sev nine)** `ses_f2f20b858ffeesEmD3UncXe4hy` — ops-monitor load-avg served as `cpu_percent` → real psutil or disclosed-unavailable; quality-gate empty-criterion auto-pass → fail/skip with reason; suggestions LLM failure → 200-empty → honest error status; system_monitor AttributeError → zero-filled "data" → null+degraded; calibration Brier `0.0` on empty → `None` + corrupt-record drop count surfaced; rubric zero-criteria `APPROVED` → abstain; evaluation `1.0/1.0` perfect defaults → `None`; `SystemMonitorSection.tsx` swallowed fetches → per-widget unavailable/retry (never host-property claims); owns those + `systemMonitor.ts` — **REPORT RECEIVED + central review DONE (1 inversion disclosed: suggestions model-error → HTTPException 502) → LANDED `23ce45f`** (central 164 passed, tsc 0, node 110/110, ruff 0, dup 0; the deferral is over — the fixer's pin landed in `46dbca8`, and R2's 3 shared files rode this landing)
- [x] **Docs-truth batch S3** — sub-50ms halt claim dropped from `docs/SECURITY.md`, MessageItem NaN guard, SettingsSection `"system"` default — **REPORT RECEIVED + verified → LANDED `da5c8d2`** (3 files, +8/−4)
- [x] **Wave-2 audit S1-C (frontend fetch-failure, F13–F17 + WorkspaceVitals residue)** `ses_f2edd0a8bffeYVNbOLTktZfErO` — **REPORT RECEIVED + verified → LANDED `2385c41`** — per-section ErrorBox/retry flags replacing catch-to-empty, `api.ts` `FetchResult<T>` contract + LEGACY-COMPAT wrappers fenced to ChatView, root-cause lib de-swallow (comm/inbox/teamops/threads-ext), WorkspaceVitals three-state; new `load-failure-honesty.test.mjs` (20 tests); zero pre-existing tests pinned the dishonest behaviors; central tsc 0, node 110/110
- [x] **S4 voice-autoplay** `ses_f2f07ce1affe3gyCExC37tw0SG` — **REPORT RECEIVED + verified → LANDED `656a8ac`** — default-OFF `autoplaySpeak` gate (speak impl never invoked without explicit opt-in), honest `speak()` (real errors, SPEAKING marked without hijacking armed sessions, URL revoked), failures disclosed via `onFailure`; central tsc 0, node 110/110
- [x] **Residual R1–R4** `ses_f2ef00786ffex5lEVbWIwKRpIV` — **REPORT RECEIVED + central review/gates DONE → LANDED `991b7ad`** — container host-fallback PATH parity, fleet reputation null-honesty (`number | null` + "unverified" renders), roster `None%`/`@None` leaks; central 36 passed, ruff 0, dup 0, tsc 0, node 110/110; R2's 3 shared files later rode B2's landing (`23ce45f`)
- [x] **Wave-2 audit S1-A (backend command honesty, F1–F5)** `ses_f2edd0ab2ffejF9u1Nn0UKFHLT` — **REPORT RECEIVED + central line-by-line review DONE (honest) + central gates 13 passed / ruff 0 / dup 0 → LANDED `c86b660`** — `/security-review` 6 real in-process probes + fail-closed `_derive_secure` + "Not verified in this run (no verdict claimed)" for the 2 networked checks, fabricated `privilege_ring` removed; `/skill:test` real validator + `scan_skill_dir` + root-cause `tags:` template fix; `/doctor` real probes + derived status (unknown never counts); `/compact` honest not-implemented naming the real API seam; 4 constant-True pins → derivation/negative pins (disclosed)
- [x] **Wave-2 audit S1-B (measurement honesty residue, F6/F8–F11/F19/sdlc)** `ses_f2edd0a98ffen0y74SS0ca1EqL` — its session was lost to a provider rate limit before reporting; the lead reviewed every diff line-by-line, re-ran its gates and **LANDED `ba6fac3`** (28 + 7 passed, dup 0, 0 new ruff; nullable kill-scores/oracle confidence/benchmark scores, fail-closed JSON+YAML syntax guard, the two fabricated tools deleted)
- [x] **Wave-2 audit S1-D (projects AVO fabricated scores F12 + cognitive bootstrap beliefs F18)** `ses_f2edd0a80ffexYVKuRvazIGZZ0` — session lost mid-flight; the lead finished the verification and **LANDED `0de236d`** (26 passed incl. the projects-router regression; dup 0)
- [x] **Known-failures trio S2** `ses_f2f07ce40ffelqSm3VzajjyAQS` — session lost mid-edit, leaving 6 broken middleware tests; the lead root-caused them (the mock patch outliving a non-cancelled worker; a policy pin that encoded a race), fixed production + tests and **LANDED `f882542`** (77 passed)
- [x] **Universal Dynamic Workflow synthesis** `ses_f2edd0a76ffeHwLiTl6yMibL3t` — **REPORT RECEIVED: full implementation blueprint** — 65 merged features (DW-01…65) with path:line grounding map, K1–K22 scenario-dynamism matrix (trigger→state→act→persistence→log→fallback), L1–L9 end-to-end loop requirements (L2/L6/L7 durability gap confirmed: run state is process-local), fence waves W-F1…F8 + build waves W-N1…W-N12 ordered, 14 enhancements (durability-first, estop-in-`execute_step`, archetypes-as-data, ScenarioContext knob carrier, lease-wiring, evidence-coverage metric, fake-output linter, command⊆catalog test); drove W-N1 dispatch
- [x] **Master Mission Modules A–D1** (user's pasted mission) — **all landed**: A `0448a50` (intent/goal engine; tri-state `registered` honesty, honest GENERAL fallback, fail-closed corrupt-goal store; 52 passed), B `0dcae51` + the live runtime hook `85b7629` (System-1 reflex: 25 + 18 passed), C `0c96673` + tool registration `0d673c7` (OS computer use: 46 passed, tool schemas 124/124), D1 `a905d97` (3 clone modes, ephemeral manager hardened fail-closed, skills/MCP proofs: 11 passed). Registrations + regenerated manifest `883b44d` (orphan gate 6 passed, wiring 13 passed)
- [x] **W-N1 durable event log & plan graph** `ses_f2ebf5f9affebo3HBgCEH2SCro` — its session was lost after `schemas.py`; **the lead built and landed the rest** — `650437e`: append-only fsync'd event log with idempotency keys and corrupt-tail refusal, atomic run projections, graph-revision store with optimistic concurrency, kill-and-resume hydration, additive REST surface, dispatcher sink seam; 37 passed
- [x] **W-N3 registries wave** `ses_f2edd0a76ffeHwLiTl6yMibL3t` — **REPORT RECEIVED + central line-by-line review DONE (honest; 0 inversions) + central gates ruff 0 / `ast_dup_gate_dups=0` / 14 passed (119.52s) → LANDED `b331c04`** (9 files, +1155): `workflow/registry/` package — shared `CapabilityDescriptor`/`RegistryHealth` protocol with fail-closed `RegistryUnavailable` (unreadable source never reads as "zero entries"), read-only views of the capabilities catalog (per-entry isolated `find_spec` probes), `BUILTIN_TOOLS`, skill storage, MCP config (never connects a transport), memory modules (never constructs a store); `WorkflowRegistry` facade + never-raising per-kind-isolated `health()` + locked singleton; `test_dynwf_registries.py` 14 tests pin the honesty invariants (`health="unverified"` and `version=None` enforced by test, `unavailable ⇒ non-empty reason`, broken source ⇒ fail-closed with real text, one broken registry never hides the rest); catalog shared-append `+6` lines verified entry-alone; agent gates: 20 passed incl. orphan (354s); disclosed: one assertion fix in its OWN new test (Windows `str(Path(...))` portability, production unchanged), `default_enabled=False` repo pattern (loader-mediated import chain — the catalog entry IS the production import, no allowlist used), BUILTIN-tools-only scope documented in docstrings; **blueprint leftover: the explicit command⊆catalog⊆capability parity test was NOT among the 14 → owed in P1**
- [ ] **Module A's five spec commands remain honestly unregistered** — `/boost`, `/schedule`, `/grill-me`, `/teamwork-preview`, `/self-heal` resolve with `registered=False` and say so (the DY-R3 no-phantom-command canon holds; nothing pretends they are executable). Registering them needs real per-command handlers — a feature wave, not a doc fix
- [~] **Full-suite + recovery + env/prod verification** — re-running from the final tip (`logs/pytest_full_stage4.log`), then `scripts\verify_recovery.ps1` (30/30) and the formal secrets/env/dup sweep
- [x] **Central deferred pass (lead-owned)** — all four items closed: `enterprise/heartbeat.py` invented per-dept token burns (`4500/1200/800/600/700/500/400`) that fed treasury burn + circuit breakers → measured-only recording with a `token_burn_measurement` disclosure; `company/self_improvement.py` fabricated retrospective defaults (`5 tasks`/`1 incident`), invented playbook lines and fabricated "calibrated" claims → measured-only counts, no invented synthesis, "not yet measured" KPI summary; `routers/company.py` invented discover-work items when signals omitted → `signals=[]` + `signals_source`/`disclosure`; `company/organization.py` `sleeping_bots_count=3` → measured from the attendance ledger with an `attendance_measured` marker. **LANDED `46dbca8`** (token burns + attendance with the fixer batch) and **`4179e34`** (retrospective + discover-work); central 53 + 21 passed, dup 0
- [~] **Other-agent landing adjudication (lead, read-only DONE)** — README+2 / docs-README+1 / FAQ / llms / ChatView+107 / freeModels verified honest end-to-end: `tsc` exit 0; `createThread` `project_id` → backend validated (`threads.py:470,859`); `moveThread` + projects lib real; `GET /models/free/catalog` exists with honest tri-state contract; Composer inserts transcript **before** `onDictate` so the flash message is accurate; claims: 24 skills ✓ / 89-engine table ✓ / nginx 2026 + langgraph rewrite ✓ / make targets ✓ / `pnpm verify` ✓ / compat endpoint ✓ → **corrections required at landing: FAQ:41 + llms:44 "51 routers" → 57** (dir + manifest agree); recommended `60+ tools` → `120+` (manifest = 121); queued: "sub-50ms emergency stop" (README:177 + SECURITY.md:416 + FAQ:49) has **no measurement anywhere** → soften or add measured evidence; `freeModels.eligible` fallback nit
- [ ] **Queue #5 full backend suite — restart fresh** — **gate EXTENDED**: run only after ALL in-flight batches land (runtime `ses_f2f751076ffe…` · fixer `ses_f2f2d517cf…` · audit A/B1/B2 `ses_f2f20b87bff…`/`ses_f2f20b86cff…`/`ses_f2f20b858ffe…` · central deferred pass above); previously cancelled by server restart at ~19% (`logs/pytest_full_stage4.log`); expect known F/E clusters 19–24%; then triage F/E → recovery `scripts\verify_recovery.ps1` 30/30 → env/prod checks
- [x] **WorkSwarm gap 7 (unified execution-mode switch)** — DONE, landed via `316af9f` (gates + live endpoint honesty payloads recorded in §4 row 7)
- [x] **Known-issues honesty queue** (§9 items 1–5) — DONE via `d4b7af5` (central re-run 85 passed, ruff 0, dup 0): disclosed neutral-0.5 defaults for `enterprise.py`/`enterprise/models.py`/`rfc.py` seeds/`memory.py:562` + `GateRequest.autonomous_mode` router default → opt-in False
- [x] **RSI plan main-agent review** — done; adjudication + Wave-1 dispatch recorded in §5
- [x] **web-search port from AgentEye** `ses_f30cc28caffemKoC0Nhrg5PCHy` — **REPORT RECEIVED, all gates green**: NEW `keyless_web_search_tool.py` (8-engine httpx-first keyless chain → optional ddgs fallback; honest ok/no_results/failed + `missing_package` names the real package) + NEW `deep_web_search_tool.py` (expansion → dedup → lexical rank → findings/citations/local-verify/confidence ≤1.0/summary; failed searches emit NO research fields) + registry hooks in `builtins/__init__.py`/`tools.py` + 9 offline tests; gates: **9 passed**, ruff 0 on new files (I001×2 in registry files proven pre-existing at HEAD via `git show` baseline), `ast_dup_gate_dups=0`, tool schemas 118 ok, official manifest regen → `test_feature_manifest_wiring` all passed (tools 119→121); clone at temp `agenteye`; **central line-by-line review DONE — honest** → **LANDED `fa328d8`**; manifest still held for the batch landing its other pending referents (multimodal + workflows routers)
- [x] **UI/UX + agent-status dashboard** `ses_f30cc28b5ffeDE7uo73NH4VxdA` — **REPORT RECEIVED, gates green** (tsc exit 0; node:test **62/62**, baseline 51 → +11): new `AgentStatusPanel` (real `GET /api/plan-mode/mode` badge, `/supervision/fleet` workers, `/subagents/control` — loading→"unavailable"→error honesty, zero fabricated numbers, enforced by test), watchdog "all healthy" lie fixed (real error shown), strict throwing parsers added while existing swallowing helpers preserved for dependents, theme/density system + typed `?view=` routing; backend read-only, no `package.json` edits; **deferred: live browser verification → gateway-restart window**; voice-owned findings: `VoiceControls.tsx`/`voice*.ts(m)` **do not exist** (voice is backend-only `/api/multimodal`), `SettingsSection.probeAll().catch(()=>[])` can render "0/0 subsystems online", `MessageItem` confidence unguarded (NaN% risk), SettingsSection `"dark"` default may fight the new system-preference default; **LANDED `7ce9060`** (central re-run: tsc exit 0, node:test 62/62)
- [x] **remaining-plan phase map** `ses_f3196c25dffea2hjV8vyRACIir` — **REPORT RECEIVED**: full evidence map of self-evolving §75 Phases 2-7 + WorkSwarm §46 Phases 0-12 with per-row verdicts + path evidence; recommends 6 disjoint wave bundles (updater core · issue-intake+dedup+peer-review · contribution manifests/timeline · workflow IR+node types · perpetual worker generation · harness A/B + ExternalAgentAdapter); corrections vs quick greps: goal management + perpetual session + auto-harness EXIST (renamed), `team.*` modes + SwarmFlow IR truly zero; the Wave-4 workflow-IR bundle folds into the Universal Dynamic Workflow program below
- [x] **OpenClaw 2.0 deep analysis + port** `ses_f30cc26e4ffeaEt9km5fCsKVkr` — **REPORT RECEIVED**: inventory + reality-map with path:line proofs (14 rows; probes EXIST, event-loop sampler/Prometheus/queue-health/support-bundle MISSING at baseline); 4 items implemented in lane: NEW `alpha\ops\event_loop.py` (window-max delay/CPU sampler, omission-until-first-window honesty) + NEW `alpha\ops\metrics.py` (bounded Prometheus registry, MAX_SERIES=2048, drop-counter absent=zero drops) + NEW `app\gateway\ops_readiness.py` (failing[] detail, always-200, no_signal≠failure) + NEW `app\scheduler\queue_health.py` (payload-free counts/ages) + additive `/api/ops/{event-loop,readiness,metrics}` + `/api/scheduled-tasks/queue-health` (registered before `{task_id}` wildcard; queue-health DOES authenticate) + 3 test files; agent had no shell post-restart so gates ran CENTRALLY: first run 4 failed → **root-cause fixes applied centrally** (render now skips declared-but-empty metrics so `remove_all()` honestly means "absent"; `_event_loop_check` treats a missing delay figure as no_signal not ok; series_count test pin corrected to 3 = 2 user + cap-exempt drop counter; `authenticated` docstring claim corrected to actual posture; orphan allowlist entries added with reasons) → central gate re-run **54 passed, ruff 0, dup 0** → **LANDED `3844da7`**; queue-health test file was never written (report-disclosed) — follow-up test writer spawned `ses_f304cd424ffeIsn1i6Um4wCik7`
- [~] **Hermes deep analysis + port** `ses_f30cc26d2ffebzqyfD5m0qjSV7` — **REPORT RECEIVED** (read-only post-restart session honestly deferred all writes): full learning-loop inventory with source cites + 9-row reality map (nudge cadence GAP; session-search bounds PARTIAL; user-model PARTIAL; experience bank + evidence store = Alpha ahead) + **4 spec-complete items** (S1 skill creation nudge · M1 session-search safety bounds · M2 turn-counter persistence nudge · E1 skill-usage evidence bridge) → **implementation dispatched** to fresh session `ses_f306d9f3affeIOMBRprxdmM7VC` (may-edit: `skills\creation_nudge.py`, `memory\session_search.py`, `memory\persistence_nudge.py`, `evidence\skill_usage_evidence.py` + 4 NEW tests; `learning/**`, `evolution_engine.py`, `tools\builtins/**` read-only); clone at temp `hermes-agent`

- [x] **Universal Dynamic Workflow — capability audit** `ses_f30cf0146ffesR0f9sGhTIG7a7` (explore) — **REPORT RECEIVED**: 89-capability inventory (**29 static / 37 partially dynamic / 23 fully dynamic**) + 13 integration points + full teardown of the other agent's dynamic-workflow workstream (adjudication row below)
- [x] **Universal Dynamic Workflow — architecture research** `ses_f30cf0133ffeNmjtzdWPYAz01d` — **REPORT RECEIVED**: research doc at `C:\Users\PREM KUMAR\AppData\Local\Temp\opencode\dynwf_research.md` (pattern matrix A–I with sources/licenses, 14 design lessons, 7 ranked reference architectures; composition = event-sourced kernel + approval gates + capability plane + topology plane + planner + RSI lanes; license cautions: Restate BSL / Inngest SSPL / n8n SUL / Windmill AGPL / Mastra+LiteLLM `ee/`; unverified items flagged)
- [x] **`references/ALPHA_UNIVERSAL_DYNAMIC_WORKFLOW_PLAN.md` written** — v1 plan fusing audit + research + user spec + phase-map Wave-4: non-negotiable constraints, audit summary, research synthesis, target architecture, typed schemas, runtime decision loop, 12 protocols, governance model, integration map, phases P0–P15, test plan, feature-only applicability list, risks
- [x] **Dynamic Workflow P0 remediation waves** — **RESOLVED via `7ed591f` (DY-R1/R2/R3, each centrally adjudicated at landing) + P1 runner wiring `564f004`**: **DY-R2 REPORT RECEIVED** `ses_f3098f55cffea3bBfZuT0D0vb3` — lease call-site repaired (real `bot_name` arg; `profile_override` honestly has no parameter and needs none — lease metadata only) + ERROR-level failure surfacing with real traceback; edge-aware base graph (legacy `depends_on` → `WorkflowEdge`) + full edge sync-back + honest unsupported-op rejection + tool-side `update_edge_condition` + non-vacuous OCC version bookkeeping; NEW `test_bot_cloning_lease.py` (3) + `test_workflow_dag_edges.py` (5); disclosed: spurious `patch_committed` event for unknown edge refs, `wf.graph_edges` dynamic-attr not in snapshots, in-process-only OCC · **DY-R3 REPORT RECEIVED** `ses_f3098f54effefUoBp0CPCWqabC` — phantom `/boost` removed (catalog-validated suggestions only), real `RetryPolicy`/`NodeType` per-category mapping table, mock skill body + mock MCP spec deleted (hub spy `added == []`, honest "unavailable" disclosures), bridge rewritten vs real models/runtime/replanner APIs w/ injectable `node_runner`/`compensation_runner` + honest saga; NEW `test_dynamic_layer.py` (13); `workflow/__init__.py` additive exports · **DY-R1 LANDED `7ed591f`** `ses_f3098f567ffer5whmyBRNdUYMs` (runtime honesty incl. BOT nodes + engine-test rewrite) — **central re-run: 24 passed, ruff 0 on both file sets, dup 0**; 2 failures = `test_bot_dynamic_workflow::test_workflow_with_dynamic_bot_cloning` + `test_dynamic_workflow_router::test_workflows_router_crud_and_execution` pinning the old fabricated completion (structural attribution: `runtime.py` rewritten between DY-R2's runs; both paths call `execute_step` with no runner) → **root-cause fix at DY-R1 landing: bind a real `node_runner` into `routers/workflows.py`, pin honest semantics in both tests — never disable**; **central line-by-line review DONE for DY-R2 (4 files) + DY-R3 (6 files) — both honest**; workstream LANDED `7ed591f` with the 2 pinning tests carrying honest updates (never disabled), P1 runner wiring LANDED `564f004`
- [~] **USER DIRECTIVE: complete dynamic workflow in NORMAL mode + BOT mode** (2026-09-23) — enhance the partially-implemented dynamic workflow and finish it across both execution modes; step 1 = read-only mode-integration mapper (session below) → plan §13 mode-completion section → **P1 kernel + DY-R4 LANDED `564f004`** (7-paradigm §14 mapper with honest swarm not-expressible, both modes through the one kernel, turns + replay REST endpoints; central gates 78 passed) — remaining plan phases P2+ (model/tool executors, perception) queued behind the phase-map bundles; DY-R1 owned `runtime.py`, DY-R2 owned `bots/cloning.py` — no overlap remained after they landed
- [x] **Mode-integration mapper (report received; findings persisted to dynwf plan §14)** `ses_f308642e7ffeWYj6r39d6rj7Ck` — exact path:line map of normal-mode agent execution (factory → client → agent loop → tools) vs bot-mode execution (bots router claim/dispatch, ephemeral leases, BOT nodes, message-mode, work_discovery, teammate_mesh) + where `runtime/execution_mode.py` (work.normal/code.normal/plan) intersects workflow execution → integration points for completing dynamic workflow in both modes
- [x] **Workflow-workstream adjudication** — RESOLVED: remediation landed via `7ed591f` (all 7 fabrication sites fail honestly through the single `node_runner` seam, lease call-site repaired, dynamic layer importable + wired, router test adjudicated centrally with honest-unbound REST pin + HITL roundtrip; central gates 58 passed / ruff 0 / dup 0 / orphan 6). Original verdict captured: other agent's UNCOMMITTED `alpha/workflow/*` (15 files) + `routers/workflows.py` + `bots/cloning.py` + 3 tests: audit verdict = wired DWE core is REAL but **3 honesty bugs** — (1) fabricated node executors (`runtime.py` MAP `:257`/RACE `:285`/QUORUM `:297`/BOT `:345`/default `:405` emit fake strings + fake evidence, and `test_dynamic_workflow_engine.py` **canonizes them**), (2) `cloning.py:116` lease call always TypeErrors + `:123` swallows it (silent lease loss), (3) `workflow_dag_tool.py:129` builds edgeless graph → all patch edge-ops silently dropped; plus the `dynamic_*` 4-file perceive→decompose→assemble→bridge layer is **UNIMPORTABLE** (`NodeType.TASK` @ `dynamic_decomposer.py:52`, 9+ model-API mismatches in bridge) and **unwired** (zero callers, phantom `/boost` suggestion) → **HELD uncommitted** until remediation waves land; do NOT sweep into a mega-commit again
- [x] **Deep Agents guide integration** (user directive 2026-09-24) — read `references/ALPHA_DEEPAGENTS_INTEGRATION_GUIDE.md` (52-section research on `langchain-ai/deepagents` 0.7.18 / `deepagentsjs` 1.14.0) and implement the improvements it prescribes for Alpha: §44 feature mapping, §48 must-have + high-value lists, §49's ten immediately-reusable ideas (virtual workspace + composite storage routing, large-output artifact offload, automatic context compaction, progressive-disclosure layered skills, scoped persistent memory, hierarchical planning, isolated/parallel/async subagents, approval+resume, rubric-based verification, model capability profiles, durable checkpoints, event observability) — audit-first against what already exists (zero orphan / zero unwired canon), implement only genuine gaps; catalog + manifest fenced while W-N3 runs (registrations reported to the lead). **REPORT RECEIVED + central line-by-line review DONE (honest; 0 inversions) + central gates ruff 0 / `ast_dup_gate_dups=0` / 12 passed (51.50s, independent re-run with isolated `AGENT_WORKSPACE_HOME`) → LANDED `64201ee`** (5 files, +893): audit-first table (§44/§48/§49 EXISTS/PARTIAL/MISSING with path:line evidence) then ONLY the genuine gaps implemented — new `alpha/deepagent/` = `VirtualWorkspace` storage-only Protocol + `LocalWorkspace` (root-confined; `..`/drive-component/symlink-escape fail-closed BEFORE any IO; atomic tempfile+rename writes; `asyncio.to_thread` non-blocking; utf-8; `size_bytes` measured-or-None) + `CompositeWorkspace` (declaration-order first-match routes, deny routes fail-closed — `/secrets` never touches disk asserted in test; duplicate + glob prefixes rejected; prefix-strip + `_refix` re-mapping so callers only see composite addresses) + honest `execute()` → `WorkspaceUnsupported` pointing at `alpha.sandbox` bash; reachability via `sandbox/__init__` re-export seam (agent gates: orphan 6 passed 338s + manifest-wiring 13 passed 354s — **NO catalog/manifest/allowlist edits needed**); disclosures: HEAD moved during run (honest), §41/§42 report-only no `config_version` bump, `agents/memory/**` fence respected, **real `_refix` bug found by its own round-trip test, fixed + pinned by test**; backlog honestly report-only (no code): delete FS tool (future manifest regen → lead), GPU-aware model routing, agent marketplace, Agent Protocol workers, team-layer skill scoping, §42 event-name adoption
- [ ] **Real-task live run, end-to-end** (user directive 2026-09-24) — AFTER production-ready + full suite green + recovery 30/30 + one gateway-restart window: run Alpha on a REAL task through UI→API→runtime→tools→persistence→logs→recovery, verify every hop honestly, fix whatever surfaces. Gated on the §2 full-suite row + §8 verification backlog
- [~] **Electron build + easiest Windows install** (user directive 2026-09-24) — **PHASE 1 RESEARCH RECEIVED** (read-only; full report at `C:\Users\PREM KUMAR\AppData\Local\Temp\opencode\electron_install_research.md`; lead spot-checked the key claims — stale .exe mtimes ✓, README:470 wrong filename ✓, `desktop-v2.1.0` vs CI `v*` conflict ✓, no LICENSE ✓, bundled runtime present ✓): **headline = Alpha ALREADY ships the easiest path** — per-user NSIS `Agent-Workspace-Setup-2.1.0.exe` (170,447,340 B) bundling portable Node 22.17.0 (85 MB) + uv (42 MB) + backend sources + Next.js standalone, supervised by `electron/main.js` (single-instance lock, identity-verified health check `service === 'agent-workspace-gateway'`, `taskkill /T /F` tree-kill, auto-start, `asInvoker` = no UAC) — architecturally AHEAD of Hermes, whose "easy installer" is an 8 MB Tauri shim that downloads a 256 KB script to clone the repo and BUILD on the user's machine; gaps = stale .exe (17:51 vs win-unpacked 23:14), no `latest.yml`/auto-update, unsigned → SmartScreen, first-launch Python download, docs mismatches, **no LICENSE → USER DECISION NEEDED (pick a license)**; recommendation = **Option 1: harden the existing NSIS** (explicit non-goals: Tauri shim, PS one-liner, PyInstaller, MSIX); **PHASE 2**: E1 regenerate installer + `npm test` in `electron/` — **DISPATCHED**; E2 clean-profile smoke (no-UAC install, `:8201` identity health, first-launch timing, silent `/S` install+uninstall, no orphan processes) — **gated on the suite's gateway-restart window**; E3 doc fixes — **DONE in this commit** (root README installer filename, electron/README tag example → `v2.1.0` matching CI `v*`); E4 auto-update decision (A: manual, B: electron-updater; Hermes lesson: never freeze updater logic inside the installer); E5 conditional offline-venv pre-seed (measure first); E6 signing + VirusTotal; E7 CI deltas; E8 config sweep; E9 non-goals
- [x] **Memory system full verification + improvement** (user directive 2026-09-24) — audit every memory path (write→persist→retrieve→prompt-injection→UI) for real function vs fake success, prove with tests, fix gaps root-cause; includes the scheduler/cron memory requirement below. **REPORT RECEIVED + central line-by-line review DONE (honest) + central gates ruff 0 / `ast_dup_gate_dups=0` / 179 passed (688.93s central re-run) → LANDED `0fd04bc`** (6 files, +1442): Part A — full memory-path map with `path:line` verdicts, every hop proven by REAL round-trip tests against a real DeerMem store (tool/manager write → fact `.md` + `memory.json` **bytes on disk** → fresh-manager retrieve with restart semantics via `get_memory`/`search`/`get_context` → `<memory>` prompt block + gate → `GET /api/memory` HTTP `MemoryResponse`); failure hops honest: corrupt store ⇒ typed `MemoryCorruptionError` ⇒ HTTP 500 stable detail with **bytes never repaired**, degraded read ⇒ ERROR log carrying the original exception + no success-shaped block, `fail_closed` policy re-raises, gate off ⇒ `""`; disclosed: redaction seam read-verified only (dedicated tests not in run set), passive middleware LLM extraction not executed (no LLM in tests), cross-process journal locking implemented but only same-process threads exercised
- [x] **Advanced scheduler/cron memory, Hermes-style** (user directive 2026-09-24) — scheduled/cron jobs gain durable memory: every run journals its real outcome/facts to scoped memory, later runs read prior context before acting, disclosures for what was remembered vs not (Hermes reference: temp clone `hermes-agent` + its scheduler memory design); **IMPLEMENTED in `0fd04bc` (Part B)**: new `backend/app/scheduler/job_memory.py` `JobMemoryStore` — append-only JSONL per job (flush+fsync + msvcrt/fcntl advisory lock, thread lock), **dispatch journaled BEFORE launch** (write fail ⇒ occurrence fails closed, no run exists), outcomes journaled with **measured duration or `null` + honest note** (e.g. "launched before a restart"), `requeued`/`launch_failed` statuses journaled (no dangling dispatch), corrupt line ⇒ typed error naming file + 1-based line with **bytes untouched**, retention/TTL prune **read-time-only with verbatim disclosure**, dispatch-without-outcome renders `NO OUTCOME RECORDED`; prior context injected into every scheduled prompt inside `<memory>` (2nd-dispatch test pins real disk content + measured duration); post-launch write failures ⇒ ERROR log + capped (4+1) prompt disclosures, **never a fake outcome entry**; constructor default `job_memory=None` keeps prompts byte-identical (back-compat test); `app.py` 2-line wiring (disclosed fence note); agent gates: 20 job-memory + 9 e2e tests (initial failures were genuine findings, fixed not weakened), 150 scheduler + 150 memory regressions, orphan 6; disclosed: Hermes source unavailable (`NO HERMES`) — designed from first principles + integration guide
- [ ] **Architecture organization master plan** (user directive 2026-09-24) — plan WRITTEN: `references/ALPHA_ARCHITECTURE_ORGANIZATION_MASTER_PLAN.md` (uncommitted like all references): honest current-state verdict (engine layer sound, knowledge layer scattered), feature differentiation map (workflow vs loop vs subagent vs swarm vs bot vs company vs kanban vs plan/think — shared substrate, distinct contracts), terminology reconciliation (war room/think mode/ASI/git-worktree → real features + honest gaps), **Discovery Plane completion — generated one-file INDEXes** (`docs/INDEX/{TOOLS,COMMANDS,PLUGINS,BOTS,LOOPS,MODES,SKILLS,CAPABILITIES,TRIGGERS,LIMITERS,PARALLEL,FEATURES}.md` + `contracts/feature_index.json`, generator-produced, never hand-edited) over 12 runtime registry kinds (5 landed W-N3 + 7 new), trigger/parallel/limiter maps, unused-vs-underconnected map (wire never delete), end-to-end flow, and waves **P0–P7** with fences/gates. P0 = this registration; **P1 = discovery-plane completion (dispatch when memory/deepagent fences clear — owns `workflow/registry/`)**; P3 dormant wiring (`alpha.evidence`+`alpha.ledger`), P4 memory-boundary + duplicate-tool alias, P5 Module A 5 handlers, P6 connection waves (defaults-off), P7 CI drift gates. Inventory ground truth recorded: 125 slash-command strings (authoritative source = `commands/catalog.py`), both memory trees exist, 97 engine dirs, extensions engine complete
- [ ] **USER DIRECTIVE: expose ALL unnoticed backend features in the frontend — "I need to see everything in the frontend"** (2026-09-24) — measured coverage table built (56 routers × real frontend consumption): **ZERO-visibility = workflows (17 routes), skills_workshop/forge (8), deliveries (7), goal_contracts (7) + goal_integrity (1), a2a (4), agent_messages (5), input_polish (1), missions (5, shallow), checkpoints (4, shallow); AutonomySupervisor `status()` has NO API; Sentinel has NO API + NO report history; MoA has NO REST + NO UI**; near-invisible = evolution (8), policy (7→1 hit), benchmarks (3→2), swarms/plan_mode/council shallow; verified FALSE NEGATIVES (UI actually exists via sections): scheduled_tasks, subagent_batches, uploads; **excluded by standing operator constraint: auth sign-in UI, github_webhooks; disclosed internal-no-UI: openai_compat, assistants_compat, mcp_tasks plumbing**. 4 fenced exposure waves DISPATCHED (lead applies NavTabs/workspace-view/page.tsx wiring at each landing to avoid shared-file races): **W-A Supervisor + Sentinel + fix-controls** `ses_f2c9b9bc2ffei65g67TQ7ajlD7` (backend: new `routers/autonomy.py` — status/run-with-auto_heal-default-off/reports; new `runtime/sentinel/report_store.py` fail-closed JSONL history wired at the `sentinel_tick` choke point; **mount snippet reported, lead edits app.py**; UI SupervisorSection: 6-loop table + sentinel history + Run/auto-heal controls, disabled-default shown honestly); **W-B Workflows/Goals/Missions/Checkpoints/Jobs** `ses_f2c9b9ba6ffeXKAQ0GAmi4s3c9` (frontend-only, consumes existing 34 routes); **W-C Skill-Forge/Evolution/Policy/Benchmarks** `ses_f2c9b9b92ffe9shfH4eJpE7z7j` (frontend-only); **W-D Protocols(A2A)/Agent-messages/Deliveries/Input-polish/MoA** `ses_f2c9b9b7fffekbCGflBPXLRnpd` (adds MoA endpoints to mounted `models.py` only; compat/webhook/auth UI excluded by canon). Standing gates per wave: tsc 0 in own files + targeted lib tests + (backend waves) ruff 0/dup 0/targeted pytest/orphan gate; central review + gates before every landing\n- [x] **W-B Workflows/Goals frontend exposure** `ses_f2c9b9ba6ffeXKAQ0GAmi4s3c9` — **REPORT RECEIVED + central line-by-line review DONE (libs verified: strict `=== true` booleans, null-preserving `typeof` checks for `test_passed`/`started_at`/`exit_code`, failures reject instead of fabricating rows — 0 inversions) + central gates tsc 0 (project-wide independent re-run) / 29 passed (run twice: agent + lead) → LANDED `dc578fd`** (8 files, +2890): `lib/workflows.ts` (17 DWE + 4 checkpoint + 5 job routes consumed), `lib/goals.ts` (6 contract + integrity audit + 5 mission routes), 29 honesty tests, `WorkflowsSection.tsx` (1,339 lines, 3 panels: Workflows · Goals & Missions · Checkpoints & Jobs; 0 direct fetch, unknown status → neutral gray, real parse errors surfaced); **lead wiring applied** (waves are fenced from shared files): `NavTabs.tsx` (Workflow icon + `"workflows"` union + operations entry), `workspace-view.ts` (`?view=workflows` deep-link), `ChatView.tsx` (named lazy import + ternary case); disclosed backlog (UI states each honestly): no `GET /workflows/{id}/runs` route (runs shown via hydrate/durability + in-session), no list-plans/list-attempts routes, checkpoints in-memory per process, job submit 403 surfaces server error verbatim\n- [x] **W-C Skill-Forge/Evolution/Policy/Benchmarks frontend exposure** `ses_f2c9b9b92ffe9shfH4eJpE7z7j` (resumed sessions `…92f…`) — **REPORT RECEIVED + central line-by-line review DONE (4 libs: API-sourced labels, `autonomous_mode:false`+`human_approved:false` on omission, router-clamped limits, matched_rule:null preserved — 0 inversions) + central gates tsc 0 (independent) / 39 passed (incl. 26 contract pins asserting client paths against real router decorators) → LANDED `0925d36`** (12 files, +2671): `lib/workshop.ts` (8 routes) + `lib/evolution.ts` (8) + `lib/policy.ts` (7) + `lib/benchmarks.ts` (3) + 4 test files (39 tests) + `ForgeSection.tsx` (1,227 lines, 4 panels: Skill Workshop · Evolution · Policy · Benchmarks; scan: 0 mock/fake/direct-fetch, only an honesty hint string) + lead wiring (`NavTabs` Hammer icon/union/entry, `workspace-view`, `ChatView` lazy/ternary); disclosures carried: D1 unwired-until-lead (applied), **D2 none of the 4 routers emit `basis`/`evidence_kind` today → BasisChips renders nothing, will surface fields automatically if API adds them (no fabricated labels)**, D3 one export unwired-by-design (redundant single-fetch), D4 benchmark results + policy approvals are **in-process gateway state** (UI says so verbatim), D6 its own pre-restart fixture bug repaired to match verified backend contract (backend unchanged)\n- [x] **W-A Supervisor + Sentinel visibility & fix control** (backend by wave session `ses_f2c9b9bc2ffei65g67TQ7ajlD7` + **lead-executed frontend after the subagent pool hit provider rate limits**) — backend central-gated green before the frontend existed (ruff 0 / `ast_dup_gate_dups=0` / `test_sentinel_report_store.py` 16/16 / `test_autonomy_api.py` 10/10) → **LANDED `c5e1be8` (root-cause fix) + `f2ce940` (13 files, +1945)**: new `routers/autonomy.py` (`GET /api/autonomy/status` live supervisor counters + request-time config flags with availability disclosure, `GET .../sentinel/reports` capped fail-closed journal read, `POST .../sentinel/run` → `asyncio.to_thread(sentinel_tick)` with `auto_heal` default **false**, `GET .../sentinel/signals` observe-only `collect()`), new `runtime/sentinel/report_store.py` (append-only JSONL + fsync + advisory lock, typed corrupt-error naming file+line, bytes never repaired, capped read with verbatim disclosures) wired at the ONE `sentinel_tick` choke point so supervisor ticks and API ticks share the journal, `app.py` mount applied by lead (verified live: all 4 routes present in the real app), frontend `lib/supervisor.ts` + `SupervisorSection.tsx` + 13 tests (never-run loops render "never" not epoch 0, empty error/summary → null, unavailable config keeps flags null + real error, corrupt journal 500 rejects, `auto_heal` ALWAYS sent explicitly — omission can never request repairs, contract pin against the real router) + lead nav wiring; **root-cause honesty fix `c5e1be8`**: `ApiClientError` now carries the server's `detail` (`readonly detail`, appended to the message) — before this, every "surfaces the server error verbatim" surface in the app silently showed only "Request failed (HTTP N)."; proved regression-free by the FULL lib suite **191/191**; gates at landing: ruff 0, dup 0, tsc 0, full lib 191/191, mount check OK\n- [x] **W-D Protocols + MoA exposure** (session `ses_f2c9b9b7fffekbCGflBPXLRnpd`, all follow-ups rate-limited; **lead executed audit + review + completion**) — **LANDED `2fc87f9`** (13 files, +2611): `GET /api/models/moa` (live engine/command/limits/orchestrator/redaction-probe/tool state with PER-FIELD error disclosures — a broken capability registry degrades one field only) + `POST /api/models/moa/run` (real round, engine PII/secret redaction, per-candidate `model:use` authz reuse, MAX_ADVISORS cap, 400/422/404 validation, honest `evidence_kind` real/simulated/failed via a production-client identity check), lead-authored `test_models_moa_api.py` **7/7** (all three evidence branches, isolated-failure disclosure, validation contract, **lead root-fix: advisor errors now pass through `redact_pii_and_secrets` so a provider error carrying an API key can never leak** — the previous code contradicted its own docstring), `lib/protocols.ts` + `lib/moa.ts` + 22 tests + `ProtocolsSection.tsx` (A2A cards/delegate/register · agent messages roster/send/inbox with `mark_as_read=false` default · delivery ledger claim/mark + blueprints + incidents · input polish · MoA status/run with evidence badge) + lead nav wiring; **intervention recorded**: the rate-limited session had also built an in-flight "advanced free provider registry" (652-line module with invented quotas/model names/retirement dates stamped `last_verified`, provider deletions that broke 3 existing blockrun failover tests, a `get_model_config` change that synthesized models for ANY `free:*` name and weakened 404s, zero tests) — the lead **reverted the fabricated/destructive parts** (restoring the 3 blockrun tests: 26/26 free-router suite green again) and kept only the honest slice: the pre-existing `free_models_sync_tick` loop now REGISTERED in the supervisor (it existed but was never wired), the real free-model discovery augmentation of `list_models`, the live free-catalog refresh that was already at HEAD, and the operator probe script `backend/scripts/check_free_models.py` (useful for the upcoming free-LLM smoke); manifest regenerated by the OFFICIAL generator (58 routers, 7 loops); gates: ruff 0, dup 0, backend 33/33, tsc 0, full lib suite 213/213\n- [x] **Module A's five spec commands** (subagent attempts all rate-limited; **lead-executed end-to-end**) — **LANDED `fb7a7fc`** (5 files, +741) + `4c0658d`: audit found the five were absent from the catalog entirely (the resolver reported `registered=False`), and the in-repo spec was `alpha/orchestration/intent.py` + `autonomous_engine`'s trigger rules; new `alpha/commands/module_a_handlers.py` — `/boost` runs the router's own `classify_domain→classify_complexity→resolve_thinking_mode` derivation and reports tier/token-budget/model-hint as a PREVIEW (changes nothing, budgets labeled as hints), `/schedule` creates a REAL cron job through `alpha.scheduler.cron_manager` (5-field cron or deterministic `every Nm|Nh|Nd`; inexpressible schedules REJECTED with the reason — 4-field crons are never silently shifted), `/grill-me` runs one REAL `oneshot_llm` turn whose questions are returned verbatim (empty/failed model → honest error or "no questions produced"), `/teamwork-preview` is a READ-ONLY preview over the real roster file + group-run service + swarm coordinator (every unreadable source disclosed; nothing assigned/started), `/self-heal` is REAL DIAGNOSIS ONLY (estop `get_status`, durable Sentinel journal with verbatim pass errors, corrupt journal reported as unreadable — never "no passes" — plus the real repair control endpoints; `applied_fix: false` in the payload); 5 catalog entries (core, real categories MODEL/EXECUTION/PLANNING/SWARM/AUTONOMOUS_OPS) + bindings; 23 new tests (**75/75** with the intent suite) incl. real-cron persistence, interval determinism, rejection reasons, model failure/empty paths, roster honesty, corrupt-journal fail-closed; gates: ruff 0, dup 0, orphan 6; **disclosed test change**: `test_intent_goal_engine`'s five parametrized `registered=False` expectations flipped to True with a comment (the resolver behaviour, command, confidence window and reason shape are unchanged — only the honest catalog-membership flag moved, which is P5's entire purpose); lead findings during the wave: multi-word cron args were mis-parsed and a 4-field cron could be silently shifted (both fixed + pinned), estop key is `is_engaged`; also **fixed `4c0658d`**: `test_local_sandbox_command_timeout.py::test_timeout_output_carries_authoritative_failure_marker` was platform-incorrect (patched only the POSIX runner, so on Windows it really spawned a nonexistent `/bin/sh` → `WinError 2`) — now platform-correct like its sibling, assertions untouched; disclosed: `test_windows_foreground_timeout_is_wall_clock_bound` exceeded its 2s bound under full-suite CPU contention (3.0s observed) — re-checked on a quiet machine before the final suite (no bound loosened)
- [x] **P3 dormant-module wiring (plan G5)** (subagent rate-limited; **lead-executed**) — **LANDED `0c36d45`** (6 files, +412): audit corrected the plan's premise (both packages already had builder/ledger tests; what was missing was REAL CONSUMERS) · `alpha/evidence` gains `default_evidence_store()` and its first real consumer: the Finish-First verifier middleware records the actual violation event (code writes with no verification step) as `kind="observation"` evidence with the real counts, fail-safe so an evidence-store failure can never break a turn · `alpha/ledger` gains `default_action_ledger()` and its first real consumer: the **ESTOP action path** writes a real `ActionIntent` before the mutation and a real `ActionReceipt` after it (outcome `succeeded`/`failed`, `error_category="internal"` on the filesystem-error path, measured timestamps, sentinel path as `exit_ref`; `status` actions write nothing, and a broken ledger is logged at WARNING, never silent) · NEW `test_evidence_store.py` (the package's first DIRECT store suite: on-disk round trip via a fresh instance, validation rules, owner scoping, candidate→evaluation→promotion, corrupt-state refusal with bytes untouched) + `test_dormant_wiring_p3.py` (6 call-site proofs incl. the broken-ledger and broken-evidence paths); gates: ruff 0, `ast_dup_gate_dups=0`, **69 passed** (evidence+ledger+skill-usage+RSI+estop regression set) + 8/8 re-run; lead-found real bug fixed: the receipt's `started_at` was sampled BEFORE `record_intent`, so the ledger's own invariant (`started_at >= intent.created_at`) rejected every receipt — the broad `except` was hiding it until the assertion was made strict, the start clock now runs after the intent lands, and the swallow was replaced with an explicit warning log
- [x] **P4 memory clarity** (subagent rate-limited; **lead-executed**) — **LANDED `2adc39b`** (7 files): (1) **openviking fail-open → honest disclosure**: the default `read_failure_policy="fail_open"` returned `""` from `get_context` and `[]` from `search`, i.e. a backend outage looked exactly like "this user has no memories" — now a degraded read returns an explicit `[degraded_memory_unavailable]` block carrying the real exception text (and a degraded search one explicit failure row, never an empty result set); the `raise` policy still raises `MemoryReadError` (fail-closed untouched), and healthy reads are byte-identical (pinned) · (2) **canonical memory-search tool registered**: `search_session_memory` (SQLite FTS5 + BM25 with cron demotion) was DEFINED in `code_agentic_core` but never re-exported from `tools.builtins` nor registered in `tools.py` — i.e. completely unreachable; it is now exported + registered (verified live: `SEARCH_SESSION_MEMORY_REGISTERED=True`, 129 tools) with the old bash name documented as the alias (nothing deleted) · (3) **memory-tree boundary documented** on both `alpha/memory/__init__.py` (cognitive engines) and `alpha/agents/memory/__init__.py` (agent-facing backends), stating the no-cross-import rule and that unification is a separate reviewed decision; NEW `test_openviking_degraded_read_honesty.py` (4 tests: degraded context discloses, degraded search row, raise policy still raises, healthy path unchanged) reusing the sibling suite's exact fake transport; **disclosed test updates** (behaviour intentionally changed, assertions updated NOT weakened): `test_unreachable_context_read_fail_open_returns_no_injected_context` and `test_unreachable_async_context_read_honors_policy[fail_open]` now pin the disclosure instead of the bare `""`; gates: ruff 0, dup 0, **40 passed**; disclosed unrelated: `test_durable_context_middleware.py::test_registered_before_summarization` fails on this host from pre-existing local-config bleed (`voice.tts.voice=None`), unrelated to P4 — tracked for the final suite triage
- [~] **P6 connection waves (defaults-off)** (subagent rate-limited; **lead-executed, in slices**) — audit: the plan's "worktree capability" premise is OUTDATED — a real implementation already exists (`alpha/sandbox/worktrees.py`), so the gap there is exposure/documentation, not construction; the genuine gaps are the connections between surfaces. **SLICE 1 LANDED `1ecddae`** (3 files, +301): the P3 surfaces become visible over REST — new `routers/evidence.py` with `GET /api/evidence/records` (owner-scoped, optional kind filter), `/api/evidence/candidates` (candidate→evaluation→promotion chains), `/api/action-ledger/intents`, `/api/action-ledger/receipts` (outcome/tool/thread/intent filters, real timestamps, error categories, exit refs), mounted by the lead; honesty: `owner_id` is REQUIRED on every route (owners never merged), a corrupt store fails closed with the store's real reason (never an empty list), a readable-empty store answers an honest `count: 0`, invalid filter values answer 422 with the store's own message; 6 new contract tests, gates ruff 0 / dup 0 / 6 passed. **REMAINING P6 slices (queued)**: kanban↔workflow state mirror (defaults-off), forge↔trace-store exposure, swarm↔company-KPI measured-only (no invented numbers), worktree capability exposure (build already exists)

## 3. Free-LLM router phase checklist (all code items closed)

- [x] package + endpoint + config (see §1 `cac8d0d`)
- [x] targeted pytest **26/26** + ruff 0 + `ast_dup_gate_dups=0`
- [x] regression quartet **160** green (`test_model_factory`, `test_model_fallbacks`, `test_models_authorization`, `test_feature_manifest_wiring`)
- [ ] **live smoke (after suite completes)** — restart gateway, `GET /api/models/free/catalog` honest view, one real routed chat, observe union-alpha→alpha-free chain failover

## 4. WorkSwarm gap implementation (inventory: 15 EXISTS / 7 PARTIAL / 0 MISSING)

| # | Gap (PARTIAL) | Commit / Owner | Status |
|---|---|---|---|
| 1 | Skill evolution engine | `b121ff9` | [x] done + centrally re-verified |
| 2 | Skill retrieval scoring + relationship graph | `4fa9a79` | [x] done + centrally re-verified |
| 3 | Hierarchical scoped budgets | `f96dad6` | [x] done + centrally re-verified |
| 4 | HITL human-gated workflow steps | `f96dad6` | [x] done (wired to `alpha/projects/approval_queue.py`, not `orchestrator/approvals.py` — see plan row 4b) |
| 5 | FACT/TIP experience bank + hygiene | `ecc4f1a` | [x] done + centrally re-verified |
| 6 | Pre-memory-write secret filter | `5a1f401` (sub `ses_f32032dd3ffeCtWF3sNzZcAvcD`) | [x] done + centrally verified (34/34, ruff 0, dup 0; orphan flag on `memory_redaction` cleared) |
| 7 | Unified execution mode switch (plan/code) | sub `ses_f31af9edcffeojRL1dth0CHx5M` | [x] done — landed via `316af9f` (report + gates read: Gate-1 90 passed, regressions 76+7+1, manifest wiring 13, ruff 0, dup 0; live endpoint payloads verified incl. real 422/403/persist-failure-500 honesty; zero edits to policy/app/manifest) |

## 5. RSI features ("implement all possible RSI features")

- [x] `references/RSI_AGENT_ARCHITECTURE.md` present (80,717 B, user-supplied, read-only)
- [x] `references/ALPHA_RSI_IMPLEMENTATION_PLAN.md` — reality-mapped plan complete (27-feature inventory: 11 EXISTS / 13 PARTIAL / 3 MISSING; WP-A1…D3 packages; phases A→B→C; 8 spec↔code mismatches disclosed)
- [x] main-agent review of RSI plan against real code — **COMPLETE, APPROVED WITH AMENDMENTS**: explore agent verified all **27 verdicts correct** (11/13/3) + all **8 mismatches accurate**; A5 closed by grep-grade `git grep` (row 3 real holdout MISSING — only simulated `council.py` previews; row 19 zero hits; row 21 MISSING with `max_budget_chars` compaction-adjacent); citation corrections A1 (2 of 3 `WorktreeManager` "used by" false), A2 (`reflection/resolvers.py` mis-cited), A3 (adjacent `alpha/company` KPI subsystem — D1 must pre-read), A4 (dup citation) + 6 spec features absent from inventory (§42 cooldown, §26/§43 release dirs, §54 error taxonomy + uncited tools) all recorded in plan **§8 adjudication addendum** (plan status → APPROVED for Wave 1; binding ownership rule: **A4 owns `rsi/engine.py`, A3 module-only**)
- [~] **RSI Wave 1 dispatched** (6 parallel disjoint agents per plan §4): A1 lineage+archive `ses_f31aa4c06ffecNmlGIKFZ9T24k` · A2 evaluator manifest `ses_f31aa4a50ffeOZWihH2d69N6GP` · A3 holdout (module-only) `ses_f31aa4a32ffeUTcoh9A64T81NJ` · A4 state+switchboard+engine.py guard `ses_f31aa4a1bffeL2X8knMu6HQ52R` · D1 opportunity miner (observe-only) `ses_f31aa4a07ffeXegoyIya48OLD6` · D3 strategy memory `ses_f31aa494cffe64sqt43XYQB0rd`
- [x] central review + staged commits for Wave 1 — **COMPLETE**: A1/A2/A3/A4/D1/D3 all reported; every module read line-by-line centrally; gates re-run centrally (66/66 + 42/42 + 20/20 across holdout/state/kill-switch/strategy-memory/lineage/opportunity/cycle); commits: bulk via `316af9f` sweep (other actor — disclosed), A4 tail `8e19098`, D3 tail `e7e0de4`, A2 determinism-test hardening `6eb74b3` — all pushed; `test_rsi_cycle.py` green in every run
- [x] **RSI Wave 2 — COMPLETE 4/4 (B1+B2+B3+D2; unblocks Wave 3)**: **B3 LANDED `eb778c7`** (`ses_f30bf4b9dffedoZ9F7vE77IStW` — 15/15 pins; central re-run 15 passed/ruff 0/dup 0; code-constant rules, import-time validation, fail-closed deny even with `reviewed=True`, human-only review channel) · **D2 LANDED `8b96b4d`** (`ses_f30bf4b89ffeLirSCLa8CMa5ia` — NEW `rsi/budgets.py` + `state.py` additive `budget` key; central line-by-line review honest: true figures everywhere, candidate-payload override pin, clock-seam determinism, abort payload with no score/confidence/pass; central re-run 51 passed/ruff 0/dup 0; one wording fix applied centrally) · **B2 LANDED `950716a`** (`ses_f30bf4bb0ffe1ZDpJwlxQYFfPU` — NEW `rsi/generator.py` 436 L + 16 tests; central line-by-line review honest: imported `_normalize_evidence`/`FORBIDDEN_SURFACES`/`_canonical_payload_hash`/`prior_for` (no second lists), no score/confidence/improved/diversity in serialization, lineage-before-file, disclosed heuristic prior with exploration never disabled; central re-run 24 passed/ruff 0/dup 0) · **B1 LANDED `0ed64ce`** `ses_f30bf4bdbffevqMkw3eo6cfUVN` — NEW `rsi/workspace.py` 521 L + `test_rsi_workspace.py` 49 tests; reported 64 passed (incl. `test_managed_worktrees` regression), ruff 0, dup 0; honest mechanical main-branch guard (validated before any git call), single protected-path seam (absent → real ImportError text), copy-fallback `assurance=lower` in record + every evidence line, orphan report-not-drop, no fabricated metrics; 7 disclosed limitations (incl. `review_required` disclosed-not-blocked — no human channel inside candidate loop; production-layout mutating-git blocked by SelfRepoGuard, dir rename NOT done — flagged); **central line-by-line review DONE (honest: mechanical main-branch guard validated before any git call; single seam; copy-fallback disclosed as `assurance=lower` in record + every evidence line; orphan report-not-drop) + central gate re-run 64 passed/ruff 0/dup 0 → LANDED `0ed64ce`**; B2 additionally got an independent read-only review corroborating all 5 honesty pins (no fabricated score fields, lineage-before-file, atomic write, imported-payload-hash dedup, forbidden-check-first); disclosed limitations stand: dir-naming drift `wt-<sha256>`, `review_required` disclosed-not-blocked, production-layout mutating-git blocked by SelfRepoGuard (fail-closed, dir rename deliberately not done)
- [~] **RSI Wave 3 dispatched** (Wave 2 complete): C1 shadow harness `ses_f304025f9ffe1nw23GiM6oz4Sr` (owns NEW `rsi/shadow.py` + `test_rsi_shadow.py`) · C2a evidence bundle `ses_f304025e7ffeKYf6UrHZ4gDHbv` (NEW `rsi/evidence_bundle.py` + test) · C2b review gate `ses_f304025deffeYfUQXEe7YkzteX` (NEW `rsi/review.py` + test) — **C2c queued LAST** (serialization point: `rsi/promotion.py` + `evolution.py` router; the `GateRequest.autonomous_mode` default flip already landed centrally via `d4b7af5` → C2c only verifies that pin + optional additive approve endpoint); scope of the un-inventoried spec features (§42 cooldown, §26/§43 release dirs, §54 error taxonomy + uncited tools) decided at Wave-3 review → **status: C1 LANDED `0175150`, C2a LANDED `a7364de`, C2b LANDED `3a415e1`** (each centrally read line-by-line; central gates 74 / 80 / 25+42 passed, ruff 0, dup 0, orphan 6); **C2c LANDED `0b5bfcf`** (central line-by-line review honest; central gates 114 passed / ruff 0 / dup 0; duplicate-dispatch strays discarded + disclosed in §2) → **WAVE 3 COMPLETE 4/4 (C1+C2a+C2b+C2c)**. **Wave-3 final review DONE — un-inventoried-features scope decision: IMPLEMENT ALL three spec features as Wave-4** (standing directive "implement all possible RSI features"): spec §42 promotion cooldown / update-loop prevention + §26/§43 immutable `releases/` dirs with a single active pointer + §54 RSI-E000–E018 failure taxonomy, plus honest consume-or-cite of the uncited surfaces (`variation_operator_tool`/`run_nvidia_avo_step`, `avo_lineage_tool`, `self_improvement_tool`/`ralph_loop_tool`, `skills/security_static_scanner.py`) — **Wave-4 LANDED `13c446e`** (`ses_f2fa40239ffeUmmkJY2iTdni8H`: NEW `rsi/{cooldown,releases,errors}.py` + gate-append to `rsi/promotion.py` + one-line `EXPECTED_GATES` literal + NEW `test_rsi_wave4.py`; central line-by-line review DONE honest — literal spec pins, real-module binding identity, fail-closed byte-untouched stores, composed-reason-only §54 codes; central gates **40 passed / ruff 0 / dup 0 / orphan 6 with no allowlist edit**; 6 disclosed deviations adjudicated acceptable; scanner genuinely consumed, the three LangChain tools cited-not-consumed with the incompatibility disclosed) → **RSI §8-ADDENDUM WAVE-4 SCOPE CLOSED — PLAN WAVES 1–4 COMPLETE**
- [ ] matrix entries + commit per wave
- [ ] implement RSI phases per plan with targeted tests + gates, disjoint subagents
- [ ] matrix entry + commit per phase

## 6. Voice & multimodal (user request: mic, speaker, wake word — completely free with provider→keyless→local fallback)

Research done this session (web): openWakeWord (server-side wake word), edge-tts (keyless network TTS) → Piper/Kokoro (offline neural TTS), faster-whisper already integrated at `alpha/media/stt.py` (local STT — reused, not duplicated), RapidOCR (lightest modern OCR) → Tesseract (system binary), AI Horde anonymous (keyless image gen, already proven in free_router), Pollinations **now requires an API key** → excluded from keyless tier, AI Horde has **no** STT/TTS endpoints → that tier honestly `skipped_no_provider`, EasyOCR/PaddleOCR excluded as not-lightweight.

- [x] plan written: `references/ALPHA_VOICE_MULTIMODAL_PLAN.md` (research + reality-mapped: T1 configured `model.capabilities` → T2 keyless → T3 local; single seam; honest `MultimodalUnavailableError` attempts; WS voice stream on browser.py auth pattern; authorized exceptions: one new router + manifest regen with tools=119 byte-identical gate)
- [x] implementation subagent `ses_f31c2a451ffemUDOb17zjh4oPI` — **REPORT RECEIVED** (phases P1–P6 complete; install evidence: edge-tts/faster-whisper/openwakeword/rapidocr/piper all `uv pip install` exit 0 with disclosed click 8.3.3→8.5.0 drift; live smoke evidence: TTS edge-tts 41,184 B mpeg, OCR rapidocr on a real 1,937,883-byte image, wake scores [0..4.1e-05] below threshold with arm-honesty proven, image-gen 3× honest HTTP 429 exhaustion; 3 real bugs found+fixed that only a live smoke could catch)
- [x] main-agent central review of its diff + gates + staged commit — **DONE, honest, LANDED `5155124`** (central gates: 103 passed [chain/router/wakeword/cold-imports/manifest wiring] + orphan 6 run separately = the recorded 109; ruff 0; `ast_dup_gate_dups=0`; `tsc --noEmit` exit 0; lib tests 83/83 incl. voice.test.mjs 21)
- [x] main agent adds `voice:` block to `config.example.yaml` + live `config.yaml` — **DONE**, exact `voice_config.py` defaults (`enabled: true`, `wake_word{engine: openwakeword, threshold: 0.5, armed_default: false}`, `tts{autoplay: false, voice: null}`, `stt{model_size: small, language: null}`); no `config_version` bump; example file rides `5155124`, live file untracked
- [ ] live HTTP smoke after suite/gateway restart: `GET /api/multimodal/capabilities`, one TTS + one STT + one OCR + one WS wake session, UI mic→transcript→speaker loop (deferred to the §8 gateway-restart window — recorded deferral, not skipped)
- [x] matrix entry + commit — **#46 written** (rides this docs commit)

## 7. Documentation deliverables

- [x] `docs/TASK_LIST.md` — this file (update on every wave)
- [x] plan docs (kept uncommitted in `references/`, per standing rule): `ALPHA_WORKSWARM_ONLY_INTEGRATION_PLAN.md`, `ALPHA_SELF_EVOLVING_GITHUB_ARCHITECTURE.md`, `RSI_AGENT_ARCHITECTURE.md` (user-supplied), `ALPHA_RSI_IMPLEMENTATION_PLAN.md` (80,718 B), `ALPHA_WORKSWARM_GAPS_IMPLEMENTATION_PLAN.md` (63,006 B — 7 gaps reality-mapped, gap 7 design included), `ALPHA_VOICE_MULTIMODAL_PLAN.md`, `ALPHA_UNIVERSAL_DYNAMIC_WORKFLOW_PLAN.md`
- [x] `docs/IMPLEMENTATION_MATRIX.md` session fix log **#15–#22** written with real SHAs (same docs commit as this file)
- [ ] final honest report (second full audit per 87-section spec)

## 8. Verification backlog (gated on suite completion)

- [ ] queue #5 fresh full-suite run + triage F/E (known clusters 19–24%); record evidence in matrix
- [ ] recovery suite `scripts\verify_recovery.ps1` must end 30/30 (blocked until suite done)
- [ ] env/prod checks re-run (`secrets scan`, env audit, dup gate repo-wide) — *preliminary secrets pre-scan CLEAN 2026-09-24 (only scanner regexes, secret-filter fixtures, and env-sourced config plumbing; no tracked `.env`/keys); formal post-suite re-run still required*
- [x] frontend `tsc --noEmit` + node tests after voice wave lands — **DONE centrally at voice landing**: `tsc --noEmit` exit 0, lib tests **83/83** (incl. new `voice.test.mjs` 21/21)
- [ ] free-LLM live smoke (§3) + voice live smoke (§6) in one gateway-restart window
- [ ] **user:** reboot verification — `powershell -ExecutionPolicy Bypass -File scripts\verify_reboot.ps1`
- [ ] **user:** tray icon state behind `^` chevron visual confirmation
- [ ] **REAL-TASK LIVE RUN (user directive) — runbook ready; gates: suite green → recovery 30/30 → one gateway-restart window (this run SHARES the window with the two live smokes above)** — protocol: (1) pick ONE genuine user-shaped task that exercises the full loop (chat request → plan/tool use → file artifact → follow-up); (2) drive it through the real UI (browser on :3000/nginx 2026) — never curl-first, UI is hop 0; (3) verify each hop with measured evidence, in order: **UI send** (message rendered, request actually left the browser) → **API** (gateway log line + thread/run ids) → **runtime** (step/tool events in the run, journal/event-log entries) → **tools** (real side effect on disk — read the bytes back, hash them) → **persistence** (thread/run survives a gateway restart; rehydrate and confirm state) → **logs** (gateway + agent logs contain the run with honest error text if any) → **recovery** (Estop or interrupt mid-run → honest stopped/failed state, no fabricated completion, resume path works) → **UI close-the-loop** (final result rendered from PERSISTED state, not local memory); (4) capture every hop's evidence (ids, timestamps, paths, log excerpts — measured only, no invented timing); (5) fix whatever surfaces — root-cause + targeted tests + central review per the standing rules; every fix lands before the run is re-attempted. A hop that cannot be verified is reported as UNVERIFIED, never assumed working

## 9. Known-issues queue (root-cause fixes only, never disable tests)

Items 1–5 DONE via `d4b7af5` (central re-run 85 passed, ruff 0, dup 0); rows 6–7 DONE via `ff88366` (86 passed + enterprise 15 re-run, ruff 0, dup 0, tsc 0, lib tests 62/62); `cloning.py:203` DONE via `013e795` (22 passed, ruff 0, dup 0) — **rows 1–9 all closed**; **ONE FAMILY was reopened below** — the `alpha/workflow/runtime.py` engine gaps both P1 sessions reported while agents were fenced from editing — **now CLOSED via `0bb2871`** (the central root-cause batch ran after Wave-4 landed; all 11 gaps fixed; see the `[x] runtime.py engine-gap family` row below):

- [x] `backend/app/gateway/routers/enterprise.py:52` — hardcoded `epistemic_confidence 0.85` (+ same-family `epistemic_weight 0.8`) — DONE `d4b7af5`
- [x] `backend/packages/harness/alpha/enterprise/models.py:138` — fabricated default (+ `DebateArgument.epistemic_weight 0.8` same family) — DONE `d4b7af5`
- [x] `backend/packages/harness/alpha/enterprise/rfc.py:45,52,59,100,107` — fabricated seed RFC confidences → disclosed `seed_demo_data` basis, arithmetic untouched — DONE `d4b7af5`
- [x] `backend/app/gateway/routers/memory.py:562` — fabricated confidence default → neutral baseline + disclosure — DONE `d4b7af5`
- [x] `GateRequest.autonomous_mode=True` (`routers/evolution.py:27`) — engine default is False (opt-in, `test_run_acceptance_criteria.py:28` pins it) → router default aligned to opt-in False — DONE `d4b7af5`
- [x] `test_no_orphan_modules` / `alpha.security.memory_redaction` — RESOLVED by `5a1f401` (central run flags only `alpha.rsi.state` now; re-measure the full RSI orphan set when Wave 1 lands — wire it or add an ALLOWED_ORPHANS entry with a reason, never suppress)
- [x] `alpha/enterprise/heartbeat.py:206` (+ `:202`/`:205` sibling fallbacks) — fabricated fallbacks `98.5`/`99.0`/`"v2.1.0"` presented as measurements when no scan/release exists → now `None`; `SecurityScanReport.security_score` + telemetry security/holdout/release fields nullable with zero fabricated defaults; frontend renders "No scan"/"No active release"; two dead heartbeat locals removed (F841) — DONE `ff88366` (`test_enterprise_autonomous_software_company.py:311` real-scan assertion verified honest-unchanged and still passes)
- [x] `alpha/evolution/retrospective_engine.py` — nonexistent `get_postmortem_store` import + swallowed `except: pass` + nonexistent `symptom` key meant heuristics could NEVER see a real postmortem (the generic fallback always fired) → wired to the real `get_postmortem_engine` → `list_postmortems()` → `error_summary`; load + approval-queue failures now log at WARNING with the real text — DONE `ff88366` (new pins `test_retrospective_postmortem_wiring` 3/3)
- [x] `tests/test_thread_id_route_contract.py` sweep — hard-coded router list missing `agent_messages` (its `{thread_id}` routes were absent from the runtime 422 sweep; the disclosed pre-existing failure) → root-cause: added to the import + routers lists — DONE `ff88366` (contract suite green including the newly covered parametrized cases)
- [x] `alpha/bots/cloning.py:203` — `reputation_score=min(1.0, source_profile.reputation_score + 0.1)` bumped reputation a fixed +0.1 on EVERY `evolve_bot` call regardless of `performance_delta` sign, laundering unverified caller-claimed telemetry into a score (fabricated-improvement family like heartbeat 98.5) → root-cause fix: reputation now **inherits the source profile unchanged** and moves only through observed task outcomes via the single existing authority (`alpha.bots.performance`), with a `reputation_basis` metadata key disclosing the provenance; the canonizing assertion (`> 0.8`) replaced with honest inheritance + provenance pins — DONE `013e795` (22 passed incl. `test_bots_extended_inventory` reputation regression, ruff 0, dup 0; matrix entry #42)
- [x] `alpha/workflow/runtime.py` engine-gap family — **CLOSED via `0bb2871`** (all 11 gaps root-cause-fixed centrally after Wave-4; gap 11 fixed not deferred; the six disclosure pins were inverted to the fixed behavior so they fail loudly if `runtime.py` regresses; central 129 passed / ruff 0 / dup 0; new `test_runtime_fail_closed.py` + `test_runtime_event_integrity.py`): reported independently by BOTH P1 sessions (agents were fenced from `runtime.py`; each gap is either compensated at the kernel/REST layer or pinned as a disclosed test expectation — never suppressed): (1) no fail-closed transition when nodes fail — runs stay RUNNING — **mitigated** by `ExecutionKernel._fail_closed` in `564f004` (drives to FAILED + honest `workflow_failed` naming the ids), root cause open; (2) `node_failed` payload key `error` (exception path) vs `reason` (`_fail_node`) inconsistency + exception path sets `node.output = None` instead of the reason dict; (3) follow-up `execute_step` clobbers `budget_exhausted` → `failed` (early-return guard omits that status); (4) deadlock replan `propose_evidence_remediation_patch("stagnation_recovery")` targets a nonexistent node id → every recovery patch rejected; (5) payload aliasing — `state` passed by reference into `workflow_started`/`workflow_completed`, event payload dicts shallow-copied so nested values are shared/aliased across listeners; (6) replay never registers patched graphs in `engine.graphs` → post-replay dispatch falls back to the definition graph (pinned as disclosure in `test_orchestrator_persistence`); (7) approval events carry no per-node status → replayed gated node diverges from live WAITING/READY (pinned in persistence + router replay tests); (8) approval denial doesn't append to `failed_nodes`; (9) `run.history` and `run.waiting_nodes` never populated; (10) evidence/`iteration_counts` absent from node event payloads (digest evidence recomputable, `alpha.tool` evidence not journaled — disclosed in `replay.py`); (11) loop nodes emit `node_completed` while the live node stays READY (mid-run replay over-state — disclosed in `replay.py`). Ground rules for the fix batch: root-cause only, update every disclosure pin to match the fixed behavior (they fail loudly by design if `runtime.py` changes — e.g. the patched-replay disclosure pin is written to FAIL once graphs are registered), never disable a pin to make a fix land.
- [x] **Fresh-audit batch A (8 HIGH central-verified before dispatch)** — `perpetual/daemon.py:90-98` marks tasks `completed` + counts/progress with zero work; `enterprise/discovery.py:48-80` hardcoded latency profiles served as telemetry (+ lying "simulated jitter" comment); `enterprise/council.py:50` `score = 96.4` constant → `routers/enterprise.py:364` `holdout_passed = score >= 90` over HTTP with the `evidence_kind=simulated` caveat dropped; `enterprise/governance.py:100-101` ROI floor `<0.1 → 0.85`; `benchmarks/arena.py:104-106` caller-asserted `simulated_pass=True/8.5s/1420tok` canned runs appended as real + `:156-158` zero-attempt roster `90.0/12.0/85.0` defaults; `supervision/recovery.py:130` HOT_REPLACE claims hot-swap+rehydration that never happens — **LANDED `b21d62b`** (central 54+1 passed, ruff 0, dup 0; dispatched `ses_f2f20b87bfferXra2PAiSyoVMp`)
- [x] **Fresh-audit batch B1** — `epistemics/engine.py:154-162` fabricated CI receipts (`"All 29 unit and integration tests passed in 27.21s"`, `"CI green receipt verified"`) seeded as FACT@0.98; `bots/performance.py:119` zero-run `success_rate=100.0` + `routers/bots.py:64` reputation `1.0` default; duplicate `POST /{name}/clone` (`bots.py:398` profile-copy vs `:812` clone-engine → first-registered shadowing makes the engine route unreachable over HTTP) — **LANDED `de6f1ce`** (central 136 passed, ruff 0, dup 0; dispatched `ses_f2f20b86cffesgE7wbHg82N9LE`)
- [x] **Fresh-audit batch B2** — **LANDED `23ce45f`** (central 164 passed, tsc 0, node 110/110, ruff 0, dup 0; the commit deferral cleared when the fixer's heartbeat pin landed in `46dbca8`; R2's shared system-monitor files rode this landing) — `ops/monitor.py:55` load-avg/`cpu_count` served as `cpu_percent`; `quality_gate.py:59,88` empty criterion auto-passes; `suggestions.py:146` LLM failure → 200 with `[]` (indistinguishable from empty); `system_monitor.py:651` AttributeError → zero-filled process "data"; `calibration.py:48` Brier `0.0` ("perfect") on empty + `:103` silent corrupt-record drops; `critic/rubric.py:47` zero criteria → `APPROVED`; `evaluation/benchmark.py:63,66` `1.0` perfect defaults; `SystemMonitorSection.tsx:130` swallowed fetches rendered as host properties — **REPORT RECEIVED + central review DONE (1 inversion disclosed: suggestions model-error → HTTPException 502) + gates green (131); commit DEFERRED until the fixer lands** (`test_bots_extended_inventory.py` carries its heartbeat pin; R2's 3 shared system-monitor files ride this landing; dispatched `ses_f2f20b858ffeesEmD3UncXe4hy`)
- [~] **Central deferred fixes (lead; blocked on fixer releasing the files)** — `enterprise/heartbeat.py:97-116` fabricated per-dept token burns driving treasury + circuit breakers; `company/self_improvement.py:17-41` fabricated retrospective defaults + invented playbook-change history + fabricated "calibrated" claims; `routers/company.py:104-107` invented discover-work items on omitted signals; `company/organization.py:388-389` `sleeping_bots_count=3` contradicting attendance PRESENT-all (lead spot-check)
- [x] **Fresh-audit KPI-seed finding** — folded into the fabricated-number fixer dispatch (`kpi.py`/`archetypes.py` seeds → `basis="seed_demo_data"` disclosure, `update_metric` → `measured`, digest ⚪ seed rows)
- [ ] **Manifest-disclosed dormant/unwired inventory (wiring debt, honestly disclosed — not fabrication)** — `contracts/feature_manifest.json` discloses: 2 dormant packages — `alpha.ledger` (ActionLedger/ActionReceipt implemented + tested in `test_action_ledger.py`, but ZERO receipt-writing call site in the run path) and `alpha.evidence` (EvidenceStore evidence→candidate→evaluation→promotion cycle implemented, NO consumer and NO test suite) — wiring both is a feature-design wave needing a lead scope decision before dispatch; 1 intentionally-unwired re-export shim (`app.gateway.services.system_monitor_service`, guarded by `test_gateway_services_resolution`); 6 operator-constrained local-only exclusions (no sign-in/OIDC in the integrated local system, no IM channel bridges, no remote MCP/ACP, no cloud sandboxes, no remote tracing, no GitHub webhook ingress — the webhook route stays 404 unless env-gated on, exactly as the env audit logs). README §3.7/§6 still describes the channel adapters + webhooks as existing code — both statements are true (the code exists; the operator constraint disables it in this local build); the final report must carry this nuance

## 10. Standing rules (apply to every item above)

- Centralized commits only (main agent), Conventional Commits subject, no-BOM `git commit -F`; commit-msg hook enforced.
- No auth changes · no hardcoded secrets · no disabling/weakening/skipping tests · root-cause fixes only · honest failed statuses over fabricated success.
- Background full suite owns `logs/pytest_full_stage4.log` — no parallel full-suite runs, no server restarts until it completes.
- Test runs isolate `AGENT_WORKSPACE_HOME` to a tmp dir (test env does NOT isolate it globally).
- grep/glob tools broken in this environment → `git grep` / `Select-String`; quote paths with spaces (`PREM KUMAR`).
- Subagents: disjoint file ownership, no git mutations, targeted tests only, no new builtin tools, no manifest hand-edits (official regen only), `ast_dup_gate_dups=0` + ruff clean before reporting done.
- `references/*.md` (user-supplied and subagent-drafted plans) stay uncommitted; only `docs/**` rides documentation commits.
- Parallel subagents are the DEFAULT working mode from 2026-09-24 (user speed directive): dispatch disjoint waves concurrently and land them as reports arrive; central line-by-line review + gates + centralized commits are unchanged.

- [~] **Full-suite failure triage (stage-4: 19,348 passed / 150 failed / 10 errors, 5h38m)** - root-cause oriented: failures cluster by shared cause. **Cluster 1 -> `1bd5312`**: all 28 `test_middleware_documentation` failures had ONE cause - the four frontend doc pages the test pins did not exist at all (the canonical English text lived in `backend/CONTRIBUTING.md`, which passes). Wrote the four pages to the test's exact contract (it `exec`s the example), pipeline order verified against real `build_middlewares`; 34/34 pass. **Cluster 2 -> `238b32d`+`bc88d05`**: 29 failures (dev-entrypoint 22, deploy-uv-extras 5, serve-pick-python 2) shared one cause - the suites spawned a bare `["sh", ...]` and this Windows host has Git Bash installed but NOT on PATH, so the shell existed and the tests could not find it (`WinError 2`). New `backend/tests/_posix_shell.py` resolves a shell that provably works (excludes the `System32\bash.exe` WSL launcher, which HANGS rather than fails; bounded probe; exposes the shell's own `usr/bin` and the running interpreter to the child so scripts calling `python3`/`chmod` work). 32/32 pass with NO coverage lost (no skips added). **Cluster 3 -> `ca14215`** (triage agent, centrally reviewed): REAL PRODUCT BUG - `LocalSandbox` substituted container->host paths UNQUOTED on Windows, so any path containing a space (this host's `PREM KUMAR`) silently split the command; now shell-aware quoted (PowerShell/cmd/POSIX) with already-quoted paths respected. Plus 20 Windows-correct test-harness fixes. 86 passed, 1 honest POSIX-only skip. **`8af9f37`**: REAL PRODUCT BUG - seven real channel commands (`approve`, `plan`, `project`, `reject`, `standup`, `swarm`, `team`) were absent from `RESERVED_SLASH_SKILL_NAMES`, so a skill named `team` would have shadowed `/team`; product constant + cross-language contract fixture updated together, plus two stale expectations corrected (completion-critic verification keys the example now publishes; the pre-rename `astra_security_manage` alias). **`8dc79ee`**: 9 failures - six channel tests hard-coded the pre-rebrand "AI Workspace" while the product derives `DISPLAY_NAME` (now derived, so a future rename cannot break them); three Windows tests asserted POSIX `0o600` bits Windows cannot express (now assert the strongest platform-expressible property, POSIX assertion kept for POSIX hosts); and the live-collection policy subprocess dropped `SystemRoot`/`WINDIR`, without which Winsock cannot initialize ANY socket, so the live-test module died at import with `WinError 10106` instead of skipping cleanly (proven by controlled experiment, then fixed with a platform-variable allowlist that still excludes credentials and any real `.env`). 48/48 + 4/4 pass. **`2b5b2a7`**: REAL SECURITY-CONTRACT GAP - the projects router grew to ~75 implemented routes while the PAT allowlist still listed four, leaving 68 implemented routes default-denied for token callers even though every one carries `@require_permission`; the lead first verified programmatically that all 69 contract-required routes are scope-guarded (0 unguarded), then enumerated the allowlist per implemented path shape with exact methods so `archive`/`restore` stay POST-only, `threads` stays GET-only and unimplemented neighbours stay denied (PAT admission only decides reachability; the scope check still runs), and fixed a compose test that parsed `${VAR:-default}:/container/path` with `split(":")[1]`, landing on the `:-` separator and never matching. 37/37 + 7/7 pass. **Time-bomb + machine-speed cluster**: `test_scheduled_task_queue_health` froze its reference time at a literal `datetime(2026, 9, 24, 12, 0)` while the handler measures queue age against the real clock, so its "5 seconds old" row was hours old and the handler CORRECTLY reported `degraded`; the fixture now derives `NOW` from the real clock. `test_process_manager` slept a fixed 0.4/0.6s and polled once, which a cold interpreter start outlasts; it now waits for the real terminal state with a bounded deadline. 27/27 pass. Remaining (queued): `test_agent_guidance_check` (3 - AGENTS.md inventory drift: three new backend files unlisted, two expected frontend files absent, `backend/AGENTS.md` over the 32 KiB cap), `test_three_way_skills_mount_e2e` (2 - POSIX-vs-Windows path expectations), `test_serve_frontend_skip_build` / `test_wave6_integration` / `test_buzz_channel` (1 each), plus redis/docker/nginx/macOS-only classes; the 10 collection errors + gateway-services/lifespan/cleanup cluster is with a parallel triage agent. **DISCLOSED - concurrent session in the same repo:** a second session is actively writing ~70 files (free local-voice/speech pipeline, System One Laya, swarm consensus/reflection, nginx voice websocket, README/Makefile/pyproject edits) and has `backend/app/gateway/routers/swarms.py` in a NON-IMPORTABLE state (FastAPI rejects a `Request | None = None` route parameter, which kills the whole `app.gateway.routers` package import; the file changed under observation twice). The lead stopped editing that file and moved its own verification into an isolated `git worktree` at HEAD (`%TEMP%\opencode\alpha-clean`), leaving the dirty tree to its owner. No foreign file was reverted, staged or committed; every lead commit is path-scoped. The endgame (recovery 30/30, gateway-restart window, voice/free-LLM smokes, installer rebuild, E2 install/run, real-task 8-hop run, second audit) cannot be honestly executed until that tree settles.
- [x] **Review-guard repair (safety guard silently broken since the rebrand)** - **LANDED `9a92ae5`** (2 files): the middleware called `self._extract_new_content(...)` but **no such method existed anywhere** - `git log -S` shows the call site survived the `agent_workspace` -> `alpha` rename while the method did not. Consequence in production: every write that passed the role check was **applied, then raised `AttributeError`**, so the agent saw a failure for an edit that had already landed and the comment-density policy never ran. Two further defects fixed in the same pass: (a) `enforce_on_extensions` was never consulted, so the density policy was applied to every extension the comment-pattern table knew, including `.md`/`.go`/`.rs` an operator had explicitly excluded; (b) the `isinstance(result, ToolMessage)` gate meant enforcement vanished whenever the handler returned anything else (a `Command`, a wrapper object) - a guard must not depend on the concrete return class, so the error check is now duck-typed. The extracted content is read from the tool's own arguments (`write_file` -> `content`, `str_replace` -> `new_str`), deliberately NOT from the result body, because write tools report a confirmation string like `Wrote 12 lines to x.py` and density-checking that would reject good writes; an unexpected shape logs a warning and skips the check rather than failing a write on a guess. Explicitly NOT changed: the middleware still does not test `config.enabled` itself, because the assembly layer (`tool_error_handling_middleware.py`) only builds the guard when `app_config.review_guard.enabled` is true - verified before touching it. Test-side repairs in the same file, each a real staleness bug: the `config`/`runtime`/`state` fixtures were defined **inside** `TestReviewGuardMiddleware`, so sibling classes requested a fixture pytest could not resolve and 5 snapshot/integration tests ERRORed at setup (promoted to module scope); all 14 middleware tests set the role on the `runtime` fixture while the middleware reads `ToolCallRequest.runtime`, so **every role assertion silently evaluated as `default_role="coder"`** (now wired); one test called the keyword-only constructor positionally; the factory test depended on a `config.yaml` existing in the project root (now injects and restores the app-config singleton, keeping the no-arg path covered). 30/30 pass, ruff 0, and the middleware documentation suite (34) still passes.
- [~] **LangGraph Studio route tests (1 failure + 5 setup errors) classified ENVIRONMENTAL, not a regression**: the fixture launches the real pinned `langgraph-api 0.10.0` in-memory dev server, polls `GET /ok` for 45s and fails with `ConnectTimeout`. Verified NOT a launch artifact on our side (re-ran with the repo's documented `PYTHONIOENCODING=utf-8`/`PYTHONUTF8=1`; identical), NOT load sensitivity (reproduces on an idle machine), and NOT a general socket limitation (a loopback HTTP probe on this host returns 200 and the hostname resolves). The server process starts, reaches its final startup log line (`Using custom authentication`) and never serves. The remaining failure expects `.langgraph_api/.langgraph_ops.pckl` from that same unstartable server. No fix attempted: Alpha's Studio routes are not what fails, the upstream pinned dev server simply does not come up here, and faking it would be dishonest. Disclosed for the final report.- [x] **Triage batch for the remaining named clusters (lead, direct)** - **LANDED `d635d06` + `4dd8941` + `5301189` + `e12ad87` + `89e0a7b`** (matrix rows 86-87). `test_agent_guidance_check` 12/12: the two "expected" frontend guides did not exist, so the guidance contract never covered the frontend; both written as real documents (the split-section test pins `### Data Flow` in `frontend/src/AGENTS.md`), the three unregistered harness guides registered, and BOTH oversized guides trimmed under their hard budgets without losing an operative rule. `test_gateway_services` 159/159: three start-run tests built a `SimpleNamespace` body instead of the real `RunCreateRequest`, so each new model field surfaced as an `AttributeError` from inside `start_run` before any assertion - bodies are now derived from the model's own defaults (overrides still allowed, so the anti-forgery tests keep their forged payloads), and the file's 33 pre-existing `I001` findings (confirmed at HEAD, in-tree, same config) were cleared. `test_tool_error_handling_middleware` 39/39: the ordering guard was correct and the TEST was wrong - five middlewares were patched to one shared `FakeMiddleware`, and the isinstance-based invariant then counted every stub as `SandboxAuditMiddleware`, so the four stubs that legitimately sit outside the receipt layer read as violations; verified the real order first (ToolReceipt 7, SandboxAudit 8, ReadBeforeWrite 9, ToolErrorHandling 10), then gave each stub its own type. `test_three_way_skills_mount_e2e` 21/21: a POSIX substring was asserted against a real Windows bind-mount source; now parses `src=` and compares `Path`s. `test_serve_frontend_skip_build` 9/9: it searched for `& node ...next build` while start.ps1 runs the node binary it resolved and validated (`& $nodePath`), so the build-failure guard was never actually being checked; now anchored on the `next build` argument plus an assertion that the line is a real invocation. `test_wave6_integration` and `test_buzz_channel` needed no change - they pass at this tip.
- [~] **OPEN with a measured root cause (not suppressed): the lifespan readiness bound is unreachable on this host** - the two gateway-lifespan timing tests stay RED and are reported as such. Instrumented with `faulthandler` and a main-thread stack sampler: the lifespan pulls a large import graph (`alpha/agents/features.py:11` 5.1s, `alpha/models/factory.py:4` 3.5s, `alpha/mcp/session_pool.py:50` 1.6s, `alpha/persistence/engine.py:17`, `app/gateway/routers/__init__.py:1`) INSIDE the readiness path, ~75s of an 80s wall, and does it from `asyncio.to_thread` calls it awaits - `to_thread` is not cancellable, so `wait_for(timeout=1.0)` cannot interrupt the import. With the graph already loaded the identical scenario starts in 0.42s, so the retrieval-warm non-blocking property the tests actually guard is sound. A test-side import warm-up was implemented, measured (14.2 -> 12.5s, 12.6 -> 6.3s - still red) and REVERTED rather than left in as massaging. The real fix is product-side (hoist the import graph off the readiness path - module scope/`create_app`, or a non-awaited pre-warm task mirroring the existing `retrieval_warm_task`) and must be re-measured on the settled tree with a full-suite run behind it.- [~] **P6 slice 4 (worktree exposure) deliberately NOT built as a quick wiring task** - matrix row 88. The capability is real and tested but **repo-global with no owner concept**, and `remove_worktree(delete_branch=True)` is destructive; ~10 internal subsystems (projects, swarm, rsi, self-repo guard, benchmarks, company) already depend on its synchronous semantics. Exposing it over HTTP without an owner model would repeat the PAT default-deny gap fixed in `2b5b2a7`. Open decisions recorded in the matrix: operator-internal vs per-owner, owner->repo-root mapping, whether branch deletion is ever permitted over HTTP, and whether the UI gets read-only listing only.