"""Multimodal tier engines: T1 provider models, T2 keyless, T3 local.

Modules are imported lazily by ``alpha.multimodal.chain``'s tier hooks so
``import alpha.multimodal`` stays cheap (``test_cold_imports``).
"""
